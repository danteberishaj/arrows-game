import { ArrowsBoardViewProps } from './ArrowsBoard.types';

// ArrowsBoardView is not available on the web platform.
export default function ArrowsBoardView(_props: ArrowsBoardViewProps) {
  throw new Error('ArrowsBoardView is not available on the web platform.');
}
