# Arrows next-level — progress ledger

- **Plan:** Arrows next-level execution plan (Track A: calm attention / visual-search game)
- **Branch:** `next-level` (P-00 done: the old working tree landed in `77f73d9`, `ec03073`, `da93dcd`)
- **Base commit:** `da93dcd` (`npx jest` 11 suites / 61 tests, `npx tsc --noEmit` clean at this commit)
- **Execution index:** `docs/next-level/TASKS.md` (phases, one row per task, serial order, owner queue)
- **Task specs:** `docs/next-level/tasks/{P,W0,W1,W2,W3,W4,W5,W6,W7,W8}.md`; source plan `docs/NEXT-LEVEL-PLAN.md`
- **Release rule:** nothing is merged to `main`, pushed, published or released without an explicit owner yes.
- **Integration record (2026-09-16):** 145 task ids across the ten files: READY 91, BLOCKED-ON-OWNER 38, BLOCKED-ON-DISK 7, BLOCKED-ON-DEVICE 2, CUT 7. The serial order holds 91 READY tasks. The index generator's checks ran with 0 errors: every depends-on id exists and none is CUT; no dependency cycle; no hard dependency points to a later phase except W1-11 → W3-12/W3-17 (the assist flag only); the serial order satisfies every READY dependency and never moves back a phase; only P-01's Files list `PersistenceKeys` or the `storage.test.ts` key assertion; only P-02/P-02b/P-02c list capture scripts; the owner queue equals the set of BLOCKED-ON-OWNER tasks. Negative control: an injected cycle (W0-02 ↔ W0-06), an unknown id (W9-99) and a swapped serial pair were each reported, so the checks can fail. The generator's dependency table was also diffed against every task's prose "Depends on" line; every difference is a consumer mention, a coordination note or a release coupling, recorded as soft in `TASKS.md`. What these checks do not prove: that the task specs are technically right. That stays with each task's own pre-fix reproduction and review.

## Pre-flight rulings

Each line reads: decision — why — cost if wrong. Ids: O = owner (2026-09-16, binding), C = controller, P/W0…W8 = that file's consolidator, I = integrator (this pass). A later ruling that overrides an earlier one says so.

### Owner rulings (binding)

- Ruling: [O-1] Arrows is Track A, a calm attention / visual-search game; PRODUCT.md is rewritten to say so, with no new rules, no ordering mechanic and no Track B work; difficulty means legibility, density, clearable fraction and silhouette novelty, never "puzzle depth" — measured: no tap order can brick a board and `canExit` is monotone — cost if wrong: every W3 curve and W7 listing decision optimises the wrong game, and a later ordering mechanic would break the no-dead-end test P-03 adds.
- Ruling: [O-2] All work lands on `next-level` as reviewed commits; nothing is merged to main, pushed, published or released without an explicit owner yes; P-00 is done — one owner, public repo, live store listing — cost if wrong: an unreviewed release reaches players, or work stalls waiting for pushes nobody asked for.
- Ruling: [O-3a] Approved: persist the interstitial counter across cold start (it is module-level today) — the paced interstitial almost never shows in the one-level session shape — cost if wrong: interstitial frequency rises for existing players on update.
- Ruling: [O-3b] Rejected and removed from the plan: an interstitial after a loss; a free hint when no rewarded ad can be served; a wall-clock cooldown, per-session cap or new-player grace — owner's monetisation and brand call — cost if wrong: less ad revenue or a harder first session than those levers would give; revisiting needs a new owner ruling.
- Ruling: [O-3c] The honest "no ad available right now" state for a rewarded button is a defect fix and stays, but it grants no free hint — silent no-ops are a defect, free grants were rejected — cost if wrong: players without fill get no hint at all, only an honest label.
- Ruling: [O-4a] Approved: a daily board with a forgiving streak (free automatic freezes), a non-tappable shape gallery with no level numbers that cannot be played from, and a store-review prompt at a perfect clear — the retention meta the plan found missing, inside the calm register — cost if wrong: menu clutter or a review quota spent at a bad moment; every piece is flagged OFF until accepted.
- Ruling: [O-4b] Rejected and removed: the evening notification and everything that exists only for it (`POST_NOTIFICATIONS`, `expo-notifications`, notify keys) — a pressure channel in a no-pressure product — cost if wrong: one retention lever unused.
- Ruling: [O-4c] The share artifact was not ruled on: keep it behind a flag defaulted OFF, marked "owner has not ruled" — owner has not been asked — cost if wrong: wasted preparation (W4-12) if the owner later cuts it.
- Ruling: [O-5] Every new player-visible feature ships behind a flag defaulted OFF until the owner accepts it from a screenshot or recording; W0 defect fixes need no flag unless risky — taste criteria close on eyeballs, not on math — cost if wrong: finished features sit dark, or a defect fix hidden behind a flag keeps players in the broken state.

### Controller rulings

- Ruling: [C-1] Under OS reduce motion, information-carrying feedback (blocker flash, blocked arrow, hint) renders a static equivalent and honours the setting; never force it with `ReduceMotion.Never`; decorative motion (heart pop, star pops, Play breathing, splash) may simply be skipped. This is W0-04's mechanism — respect the user's accessibility choice without losing the only explanation of a failed tap — cost if wrong: reduce-motion players either lose the explanation or get motion they opted out of.
- Ruling: [C-2] W2's reduce-motion step extends the census to `withDecay` and the native exit protocol (header token 3; under reduce motion the shipped exit is a stationary fading dash) and gets that look signed off; one task (W0-04) owns the six feedback `withTiming` sites — the drafts split the same sites across tasks — cost if wrong: conflicting edits at six sites, or an unsigned native reduced-motion look.
- Ruling: [C-3] The TEST AD strip gates on `__DEV__` only (W0-01's spec); W7-03 draft merged in — only build-time `__DEV__` removes the strings from the bundle — cost if wrong: simulated ads unavailable in a web production export, which does not ship.
- Ruling: [C-4] initAds retry is one task, and the rewarded-honesty and retry drafts ship as one task (W0-02) — neither is verifiable alone — cost if wrong: a larger single task and review.
- Ruling: [C-5] Pacing is one task that only persists the counter (W0-05); W7-11's other levers are cut — owner ruling O-3 — cost if wrong: none beyond O-3.
- Ruling: [C-6] Contrast repair is one task (W0-06) with one test file `src/ui/__tests__/contrast.test.ts` that includes border/surface 1.16:1 — three drafts had different failure counts and files — cost if wrong: one large L task on global tokens.
- Ruling: [C-7] Persistence has one owner (P-01): every key any approved task needs, one `arrows_schema_version`, one additive migration that never deletes (a future version is inert), the new `saveSystem.test.ts`, one exact-array update; the store stays int-only; every other task depends on P-01 and never touches `PersistenceKeys` — five drafts could not all pass the same assertion — cost if wrong: a registry gap means a key silently never hydrates.
- Ruling: [C-8] One capture instrument owner (P-02): boot into splash or menu, set and read back motion scales, pixel-difference floor, phase recording and an effect-rendered gate; W2 and W5 consume it and their harness scripts are cut — three drafts duplicated the adb plumbing — cost if wrong: P-02 is XL and gates many W1/W2/W4/W5 closures.
- Ruling: [C-9] W2 owns the theme transition and the heart-refill pop; W5 fixes the exit trail's resting width; visual combo escalation is cut (+0.08 cell ≈ 2 px); the press-preview ramp is cut — contradictory mechanisms across W2 and W5, a sub-pixel payoff, and a brand hidden-timer objection — cost if wrong: small feel improvements not pursued.
- Ruling: [C-10] Delete DESIGN.md's "Solved!/Perfect!" toast from the doc; do not build it — the win panel already says it — cost if wrong: none measurable.
- Ruling: [C-11] `app.json` `ios.supportsTablet` → false — a universal build needs iPad screenshots for a layout never run on an iPad — cost if wrong: iPad support must be re-added before an iPad launch.
- Ruling: [C-12] W1's tutorial boards are a hand-authored lane that never invokes the generator; W3's level-1 rebuild includes a joint re-measurement with W1 — the too-easy hazard when both soften level 1 — cost if wrong: the stacked first session overshoots into boredom.
- Ruling: [C-13] Analytics: typed schema, no-op sink and per-level aggregation are READY; transport code may be written but ships flag OFF; no byte leaves the device until consent exists and the policy and Data safety form are updated; endpoint deployment is BLOCKED-ON-OWNER — legal exposure and owner hosting account — cost if wrong: retention claims stay unmeasured longer.
- Ruling: [C-14] CMP: the GATE 0 compile spike on RN 0.86.3 + new arch (release variant) is agent work; vendor account and integration after it are BLOCKED-ON-OWNER — the spike's answer reshapes W7/W8 — cost if wrong: an integration planned on an SDK that does not compile.
- Ruling: [C-15] Any task touching BoardView keys memos on `arrowCount`, because `board.arrows()` returns the live spliced array whose reference never changes — measured code fact — cost if wrong: a memo silently never recomputes.

### P consolidator rulings

- Ruling: [P-f] On Android, Reanimated 4.5.1 decides reduced motion from `transition_animation_scale == 0` (read once at start), not `animator_duration_scale`; P-02 sets all three scales, relaunches and records the read-back — read from `NativeProxy.kt:324-333` — cost if wrong: captures claim a motion variant the process is not running.
- Ruling: [P-1] P-04 split into P-04a/b/c, each with one status; a dependency on "P-04" means all three — mixed statuses — cost if wrong: none (bookkeeping).
- Ruling: [P-2] Registry key names follow the consolidated task files, not the drafts; the separate daily streak keys are dropped — W4 cut that feature — cost if wrong: a consumer reads a name P-01 never registered (checked by I-1).
- Ruling: [P-3] Shape collection uses two 30-bit ints, not one 26-bit int — W3 may add shapes; JS bitwise is 32-bit signed — cost if wrong: one extra key.
- Ruling: [P-4] Keep `arrows_gen_switch_level`, drop `arrows_gen_version`; absent reads −1 because 0 is a real value — skeptical red team rejected the fork — cost if wrong: a v3 needs one more additive key.
- Ruling: [P-5] Register the three consent keys from draft W7-01 (SUPERSEDED by I-1: W7-01 uses one packed `arrows_consent`) — consent was not rejected — cost if wrong: n/a.
- Ruling: [P-6] P-01's migration only stamps the schema version; FTUE classification, collection fold and switch-level stamp stay with their tasks — stamping derived values early misclassifies installs made between P-01 and the feature — cost if wrong: each feature must handle its own first-run state (they do).
- Ruling: [P-7] No migration write when hydration failed (`SaveSystem.persistenceHealthy` guard) — an unread save must never be marked migrated — cost if wrong: a failed hydrate followed by a stamp would hide a pre-versioning save.
- Ruling: [P-8] `resetProgress()` unchanged, with a test locking today's six-key delete set; the key "Class" column informs the owner's future reset decision — no UI caller; W1 asks the owner — cost if wrong: a future reset surface must re-decide the set.
- Ruling: [P-9] The clock seam `useClock`/`today()` belongs to W4-02, not P-01 — avoid two tasks editing the same code — cost if wrong: none.
- Ruling: [P-10] Telemetry and remote-config keys excluded from the store as strings (SUPERSEDED by I-1: W6 converted them to ints, now registered) — int-only store — cost if wrong: n/a.
- Ruling: [P-11] P-02 builds one capture tool that also works on non-perf builds and sets/records screen size; the `PERF_FULL_MOTION` flag is cut and the real device setting is the only mechanism — W1-10/W4 need real save data at 360×640 dp — cost if wrong: harness scope grows.
- Ruling: [P-12] The "blocked tap drew nothing" control must use the base-commit APK or the empty-board build, never scale 0 on the current build — after W0-04 scale 0 draws a static equivalent — cost if wrong: the gate's control silently stops controlling.
- Ruling: [P-13] P-03 adds a no-dead-end jest test and a tier-band script so the doc claims are reproducible, and says bends are the stroke's round join (0.072 cell) — claims from deleted scratchpads — cost if wrong: P-03 is M instead of S.
- Ruling: [P-14] P-05 CI runs jest and tsc only; the 16 `node:test` harness tests are an optional one-line addition — scope — cost if wrong: harness tests only run locally.

### W0 consolidator rulings

- Ruling: [W0-1] W0-04 (reduce motion) closes on hand-run emulator captures and does not wait for P-02; P-02 re-checks it later — the defect is live — cost if wrong: one-off scratchpad analysis instead of the calibrated instrument.
- Ruling: [W0-2] EAS linkage moved to P-04 (P-04b carries it; confirmed by the integrator) — shipping precondition — cost if wrong: none.
- Ruling: [W0-3] Two new tasks: W0-07 (verify all fixes on one artifact with over-install) and W0-08 (release, BLOCKED-ON-OWNER); P-01 should ship alone first or W0-05 is left out — nothing in the drafts released the fixes — cost if wrong: fixes verified separately but never together.
- Ruling: [W0-4] Static hint under reduce motion is a constant 1.45× stroke (the pulse peak); static blocker stays 1.6× for the flash lifetime; owner accepts the look — reuse shipped values — cost if wrong: an owner re-pick from the recording.
- Ruling: [W0-5] Ad init retries on app-active; timed retries `[5000, 15000, 45000]` and `SPLASH_MAX_MS = 4000` are OWNER-PICKED STARTING VALUES; "Hint unavailable" clears on the next event, not a timer — no invented numbers without labels — cost if wrong: retry cadence too eager or too slow; tunable by constant.
- Ruling: [W0-6] Criteria may watch ads load but never display a live ad on the emulator unless the owner registers it as a test device — ad account risk — cost if wrong: real interstitial after cold start stays UNVERIFIED-DEVICE.
- Ruling: [W0-7] W0-02 lands before W0-06 (both edit `HeaderButton.tsx`) — avoid conflict — cost if wrong: a rebase.
- Ruling: [W0-8] The `initSaveSystem` `.catch` is hardening, not a reproduced defect — `hydrate()` already swallows failures — cost if wrong: none.
- Ruling: [W0-9] Contrast size classes are a judgement: Fredoka SemiBold counts as not bold, so "LEVEL N" at 18 fails; recommendation raise to 24 px; owner picks — WCAG size classes do not name this font — cost if wrong: one label over- or under-corrected.
- Ruling: [W0-10] "Next level"/"Continue" (white on violet, 16 bold) in Ink Night at 4.46:1 is judged by the committed script — computed, not in drafts — cost if wrong: one button tone change.

### W1 consolidator rulings

- Ruling: [W1-1] No migration value for the FTUE stage; new-player status is derived at route time from `currentLevel`/`totalSolved` and nothing is written — a one-off migration misses flag-OFF-then-ON, backup restores and failed hydrates — cost if wrong: a player who lost level 1 without clearing sees the tutorial after update (intended).
- Ruling: [W1-2] The assist ends at the first perfect clear, not after a fixed count; the "done" write happens even with the assist flag OFF; the assist flag stays OFF until the joint W1+W3 re-measurement — counts were invented; KDD evidence favours generous — cost if wrong: a never-perfect player keeps pre-first-removal forgiveness.
- Ruling: [W1-3] The stall hint runs only on T1/T2, never on a real board — owner ruling O-3 — cost if wrong: stuck players on level 1 get no free help.
- Ruling: [W1-4] Losing all hearts on a tutorial board re-deals it with no lose panel — keeps "Continue +♥ (ad)" out of the first 90 seconds — cost if wrong: none.
- Ruling: [W1-5] Tutorial boards are authored for cells ≥ 48 dp (Material guideline, labelled as sourced, not measured), measured at three emulator geometries — Android is the shipping platform — cost if wrong: boards re-authored smaller or larger.
- Ruling: [W1-6] One viewport instrument shared with W3 (whichever lands first) (SUPERSEDED by I-18: W1-01 is the sole owner) — avoid duplicate logging — cost if wrong: n/a.
- Ruling: [W1-7] W1 adds no level-0 checksum; its generator guard is a 0-calls spy plus an empty generator diff — W3 owns goldens — cost if wrong: none.
- Ruling: [W1-8] Flags are build-time `EXPO_PUBLIC_*` constants in `src/ui/ftueConfig.ts`; `FTUE_STALL_HINT_MS` is `null` in committed code — no shared flag module existed — cost if wrong: flag conventions differ across workstreams (see I-29).
- Ruling: [W1-9] Tutorial clears do not count as solves or toward the streak, and `resetProgress()` stays unchanged; both go to the owner in W1-11 — stats the player never earned — cost if wrong: one owner re-decision.
- Ruling: [W1-10] Proposed copy has no colour words and a test enforces it; a wordless version is offered — colour-independence rule — cost if wrong: copy tone, decided by the owner.

### W2 consolidator rulings

- Ruling: [W2-1] "Perfect!" panel title cut (W2-17) — it rebuilds the deleted toast on another surface — cost if wrong: a small celebratory beat absent.
- Ruling: [W2-2] Theme toggle uses the shared scrim, not a doubled menu (W2-08) — one primitive; a doubled menu runs two breathing loops — cost if wrong: a luminance dip instead of a content cross-fade; owner can reopen.
- Ruling: [W2-3] W2-12 added: owner listens to the shipped audio combo ladder on and off — the brand red team's replacement for the cut visual escalation — cost if wrong: none until the owner rules.
- Ruling: [W2-4] One owner for the post-clear timeline (W2-06) with a reserved slot for W5's reveal; final-exit factor 2.0 dropped; recommendation 1.0 — three steps split one 450 ms window; 640 ms contradicts "flick, not a drift" — cost if wrong: the owner picks from candidates anyway.
- Ruling: [W2-5] Motion flags live in `src/ui/motionFlags.ts`, parsed from `EXPO_PUBLIC_MOTION_FLAGS`, default OFF — capture builds need no code edits — cost if wrong: another flag convention (I-29).
- Ruling: [W2-6] Panel exit animates only on continue-with-ad; Next/Retry are covered by W2-04's scrim — the overlay covers the header, so the draft's leave-mid-panel case cannot happen — cost if wrong: none.
- Ruling: [W2-7] Lose delay is 690 ms (`BLOCKER_FLASH_MS` + 40 ms cleanup margin), under the 800 ms the harness waits — 650 would still cover 40 ms of the static blocker — cost if wrong: owner may call it a hang; fallback shortens the flash by derivation.
- Ruling: [W2-8] Heart-refill pop is triggered by a refill token, not "pip became filled" — pips are not remounted, so the naive rule pops on every Retry and Next — cost if wrong: none.
- Ruling: [W2-c] Corrections recorded: `withDelay` skips its delay under reduce motion (splash finishes on first frame, stars out of step with chirps); web exit trail vanishes under reduce motion while native shows a stationary dash (W2-01 aligns web); the 64 ms press-preview delay was a mechanism fix, not measured — read from Reanimated 4.5.1 source and memory — cost if wrong: census rows mis-described.

### W3 consolidator rulings

- Ruling: [W3-1] No copied v1 modules: `generate(levelIndex, version = 1)` keeps v1 default; one key `arrows_gen_switch_level` — skeptical red team rejected the fork; five goldens stay untouched — cost if wrong: v1 and v2 share a file, guarded by the v1 corpus fingerprint.
- Ruling: [W3-2] Existing players switch at current level + 1, so the board they are on stays v1 — owner rule protects the current board; gallery backfill needs past levels v1 — cost if wrong: an existing player meets a discontinuity one level later (reported in W3-14 (g)).
- Ruling: [W3-3] Add a checksum over all v1 boards for levels 1–300 (W3-02) — the five goldens miss Bolt/Square edits — cost if wrong: slower jest.
- Ruling: [W3-4] The perf ceiling is 250 arrows (level 3827), not 249 — 249 was the max over 1–1000 only — cost if wrong: none.
- Ruling: [W3-5] v2 shape bag draws only shapes whose capacity covers the target, instead of tier pools; Bolt thickening optional — measured: only 10 of 26 shapes hold 1000 cells at the 46 clamp — cost if wrong: fewer late shapes (W3-18 adds high-fill shapes).
- Ruling: [W3-6] The clearable-fraction knob works in both directions; "premise does not hold" is a valid W3-11 outcome — the level-1 target is only reachable by keeping boards more clearable — cost if wrong: curve relies on size only.
- Ruling: [W3-7] Three curve spacings (ceiling at level 100, 400 or 1000) are exploration options for the owner, not shipped values — no invented endpoints — cost if wrong: none.
- Ruling: [W3-8] W3-15 lets the single-level perf run take any level and version, reading rows and cols from the level — the harness refuses non-3827 levels today — cost if wrong: a harness edit to `benchmark.mjs`.

### W4 consolidator rulings

- Ruling: [W4-1] Collection uses two 30-bit keys (`_lo`/`_hi`) plus `arrows_shapes_through_level` — room for ~40+ shapes — cost if wrong: one extra key.
- Ruling: [W4-2] Catalogue lives in its own file `src/core/shapeCatalogue.ts`, append-only; retired shapes stay; a reshaped silhouette keeps its id — collection bits are a save format — cost if wrong: a reorder relabels every player's collection (order-lock test guards).
- Ruling: [W4-3] The collection catches up incrementally from the resume level on menu open, capped per call; recording is invisible with a kill switch; only the gallery is flagged — keep derived work off P-01's migration and never drop bits — cost if wrong: a slow first menu open for very deep players (measured in W4-07).
- Ruling: [W4-4] One forgiving day streak; each missed day spends one freeze; earn 1 per 7 days, cap 2 (OWNER-PICKED STARTING VALUES); forgiveness shown once as "streak saved" in the stats line — brand red team: forgiveness, not prominence — cost if wrong: owner re-picks values.
- Ruling: [W4-5] The daily adds no ad exposure (never registers a game or shows an interstitial); rewarded Continue and Hint stay — no new placements — cost if wrong: none.
- Ruling: [W4-6] Daily pool excludes Square and Rectangle (24 shapes, OWNER-PICKED STARTING VALUE); past days never change; new shapes arrive only via a future-dated version — no-picture fills on the one board everyone sees — cost if wrong: owner confirms at flag acceptance.
- Ruling: [W4-7] A finished daily cannot be replayed; entry and gallery stay hidden until the first solve — keeps them out of W1's first run — cost if wrong: none.
- Ruling: [W4-8] Flags live in `src/featureFlags.ts`, created if absent — no flag owner named — cost if wrong: flag conventions differ (I-29).
- Ruling: [W4-9] Review prompt: 3 lifetime prompts, 90-day gap, 25-solve minimum (numbers OWNER-PICKED STARTING VALUES), flag OFF — stricter than Apple's per-year cap; brand red team wants the minimum from telemetry — cost if wrong: one of three lifetime prompts spent at a poor N.
- Ruling: [W4-10] W4-10 owns the combined menu layout and the new-word count (its DESIGN.md toast deletion is SUPERSEDED by I-7) — nobody owned the composite — cost if wrong: menu clutter.

### W5 consolidator rulings

- Ruling: [W5-1] The contrast task lives in W0 (W0-06), which carries W5's inputs (split `heartLost`, Retry border, wrong DESIGN.md numbers); W5-05 adds only composited-scrim rows — controller ruling C-6 — cost if wrong: none (checked: W0-06 carries them).
- Ruling: [W5-2] Store screenshots first set belongs to W7 (W7-09); W5-18 only recaptures after art flags flip — one recipe owner — cost if wrong: none.
- Ruling: [W5-3] Sound-off state: W5-02 owns the icon shape, W0-06 owns the colour (and ships a shape strike first) — split by concern — cost if wrong: W5-02 must keep a shape-different off state.
- Ruling: [W5-4] Silhouettes are traced cell edges filled even-odd — row-runs risk seams; winding can fill the Ring's hole — cost if wrong: a tracer bug; round-trip test over all 26 shapes guards.
- Ruling: [W5-5] Header badge replaces the shape-name word and the header must not grow; kill criterion if unrecognisable — no extra chrome — cost if wrong: feature killed.
- Ruling: [W5-6] Paper grain is menu only — the native board view is transparent, so grain on the game screen sits behind arrows — cost if wrong: none.
- Ruling: [W5-7] Icons are flagged though the yellow hint emoji is partly a defect; the counter fix is unflagged — icon shapes are taste; the counter is a defect — cost if wrong: emoji stays until W5-20.
- Ruling: [W5-8] Exit-trail candidates use a width override that works only in PERF builds — follows the existing `EXPO_PUBLIC_PERF_EXIT_DURATION_MS` pattern — cost if wrong: none (test guards leakage).
- Ruling: [W5-9] The cleared-board outline is decorative and skipped under reduce motion; the win-panel badge carries the information — C-1 — cost if wrong: none.
- Ruling: [W5-10] W5-09 (ad modal font) kept though the modal is dev-only after W0-01 — cheap, keeps dev captures honest — cost if wrong: trivial.
- Ruling: [W5-11] `ios.supportsTablet` is left to the platform workstream (done by P-04c) — not art — cost if wrong: none.

### W6 consolidator rulings

- Ruling: [W6-1] Telemetry storage stays int-only: install id as two 31-bit keys; session count, JS-fatal records and kill-switch bits as ints registered by P-01; the transport queue is in memory — C-7; a second storage system would lack coverage — cost if wrong: unsent events lost on process death (fatals survive).
- Ruling: [W6-2] Auto Backup cannot exclude telemetry keys (all AsyncStorage keys share `RKStorage`; `android/` is gitignored); a restored device keeps its install id and counts as the same install — excluding the file would delete progress backups — cost if wrong: restored installs merge in metrics (documented).
- Ruling: [W6-3] Draft W6-06 (ad privacy calls) moved to W7-01 — two privacy seams failed their own grep check — cost if wrong: none.
- Ruling: [W6-4] The kill switch can only turn things off; config versions only move upward; remote pacing and sample-rate keys cut — O-3 and C-5 — cost if wrong: re-enabling needs a higher published version.
- Ruling: [W6-5] Call-site logic goes in a pure `levelAggregator.ts` — node jest cannot import `GameScreen.tsx` — cost if wrong: wiring proven only on the emulator until W6-11.
- Ruling: [W6-6] W6-11's second component test pins "no free continue or hint when no ad is available" instead of `AdHost` — W0-01 removes `AdHost` from production — cost if wrong: none.
- Ruling: [W6-7] Event fields trimmed (`difficulty`, `first_run`, `installDay` dropped; `heart_lost` folded into `level_end`; `error` gains `count`) — overlapping tiers; per-tap cost — cost if wrong: one schema version bump later.
- Ruling: [W6-8] Numbers left to the owner (OWNER-PICKED STARTING VALUES): config body cap 4096 B, fetch timeout 3000 ms, 100 buckets, queue cap 500, the A/B minimum detectable effect, retention, sample minimums and windows — no measurement exists — cost if wrong: tunable constants.

### W7 consolidator rulings

- Ruling: [W7-1] Consent gate flagged OFF (it was ON in the draft) — a failing gate stops all ad revenue; O-5 — cost if wrong: EEA/UK exposure persists until W7-03 flips it with the owner.
- Ruling: [W7-2] Consent is one packed int key `arrows_consent`; `resetProgress()` must not delete it — a progress reset is not a consent withdrawal — cost if wrong: none (P-01 row 21).
- Ruling: [W7-3] A GDPR refusal means ads never start; W7-07's legal review decides whether that reading is required — conservative until reviewed — cost if wrong: lost non-personalised revenue from refusing EEA players.
- Ruling: [W7-4] CMP work split into an agent spike (W7-02) and owner-blocked integration (W7-03) — C-14 — cost if wrong: none.
- Ruling: [W7-5] Reset progress cut from the Settings sheet — destructive, not needed for compliance, unspecified — cost if wrong: no in-app reset.
- Ruling: [W7-6] Revenue and experiment figures stay out of the public repo unless the owner agrees — commit 62de5ab calls the repo public — cost if wrong: less shared context.
- Ruling: [W7-7] W7 owns iOS ads privacy prerequisites (W7-14) (SUPERSEDED by I-10: W8-12 is the single owner) — avoid double-cutting — cost if wrong: n/a.
- Ruling: [W7-8] W7 owns `supportsTablet: false` (W7-15) (SUPERSEDED by I-9: P-04c) — nobody else had it — cost if wrong: n/a.
- Ruling: [W7-9] Listing copy checker bans "find the order", "logic", "strategy", "brain teaser", "gets harder", "infinite", "endless" (plus "plan your", "think ahead") — Track A and the measured flat curve — cost if wrong: copy loses a hook until W3 ships a measured curve.
- Ruling: [W7-10] Flags in `src/featureFlags.ts`, the same file W4 uses — one shared file for W4/W6/W7 — cost if wrong: none.

### W8 consolidator rulings

- Ruling: [W8-1] W7-17 draft merged into W8-12, using LevelPlay's exported `ATTrackingManager` (no `expo-tracking-transparency`) — same work; dependency already present — cost if wrong: none.
- Ruling: [W8-2] Draft W6-14 iOS capture split into script (READY now) and calibration (after the first build), ahead of smoke and parity tasks (MOVED to P-02b/P-02c by I-11) — the instrument must exist before its consumers — cost if wrong: n/a.
- Ruling: [W8-3] The iOS perf harness (W8-10) is BLOCKED-ON-OWNER until iOS is decided to ship — it would only measure the Mac — cost if wrong: no iOS perf numbers until tranche 2.
- Ruling: [W8-4] New tasks W8-01 (static Swift check that fits in 10 GB) and W8-16 (iOS pass over checks other workstreams deferred) — nobody owned those checks — cost if wrong: none.
- Ruling: [W8-5] No feature flags in tranche 1; only the ATT prompt sits behind `IOS_ATT_PROMPT_ENABLED` (OFF); W8-08 can veto the renderer change — iOS never shipped; visual-identical refactor — cost if wrong: a bad refactor caught only by W8-08.
- Ruling: [W8-6] Memory is measured in both the app and the simulator render process — shape layers move drawing into the render server (INFERRED false-win risk) — cost if wrong: extra measurement effort.
- Ruling: [W8-7] Invented memory and repeatability gates removed; remaining sample counts are OWNER-PICKED STARTING VALUES — measure first — cost if wrong: gates written after measurement.
- Ruling: [W8-8] W8-17 (backup asymmetry doc) stays in the deferred tranche, although Android backups already affect P-01 (handled by P-01's inert-future-version rule) — plan placement — cost if wrong: support confusion on an iOS launch.

### Integrator rulings (this pass, 2026-09-16)

- Ruling: [I-1] P-01's registry is reconciled against all nine files: 21 new keys, 29 total; W7's single packed `arrows_consent` replaces three consent keys; W6's eight int keys (`arrows_rc_kill_bits`, `arrows_rc_version`, `arrows_tel_install_hi/lo`, `arrows_tel_session_count`, `arrows_tel_fatal_pending/hash/screen`) are registered; W2, W5, W8 need none — C-7 requires every approved key; W6 moved them to ints — cost if wrong: an unregistered key silently reverts on cold start (P-01 acceptance 2–3 test all 29).
- Ruling: [I-2] W3-05 no longer edits `resetProgress()`; P-01's six-key reset lock stands — W3-05's draft deleted `arrows_gen_switch_level` in reset, which would fail P-01 acceptance 8 — cost if wrong: a future reset surface leaves a stale switch level (part of the owner's reset-set decision).
- Ruling: [I-3] W3-05 owns the fresh-install classification (`currentLevel === 0 && totalSolved === 0`, written only when `persistenceHealthy`); P-01 does not provide it — P-01 non-goal; W3-05 had cited a P-01 deliverable that does not exist — cost if wrong: a daily-only player (totalSolved > 0, level 0) gets switch level 1; board 0 stays v1, which is safe.
- Ruling: [I-4] Reduce-motion owner id corrected to W0-04 in W1, W2 and W5 (they cited "W0-03", which is the boot backstop); W2-01's splash-backstop dependency corrected to W0-03 — draft-to-consolidated renumbering — cost if wrong: tasks sequenced on the wrong prerequisite.
- Ruling: [I-5] W2-01's policy test allow-lists `ReduceMotion.Never` only for web `ExitTrail`; W0-04's six sites are exempted by name and never edited by W2-01 — the draft allow-listed Never at the six information-carrying sites, contradicting C-1, and editing them would give the six sites two owners (C-2) — cost if wrong: an exemption list that drifts from W0-04's components.
- Ruling: [I-6] Web `ExitTrail` may keep `ReduceMotion.Never` because the component itself renders a stationary fade under reduce motion, matching native — it is not forcing translation on reduce-motion users — cost if wrong: if the owner rejects the reduced-motion exit (W2-01 criterion 6), both renderers change together.
- Ruling: [I-7] P-03 is the single owner of DESIGN.md's Motion rewrite (bands, cited constants, accelerating exit), the toast deletion, and the fillet and `border` corrections; W2-02 is narrowed to recording the post-W2-01 reduce-motion contract and signed-off exit look; W5-10 is CUT; W4-10 no longer deletes the toast — four READY tasks edited the same DESIGN.md passages — cost if wrong: P-03 grows; W2-02 must not re-add bullets P-03 removed.
- Ruling: [I-8] W0-06 owns DESIGN.md's contrast paragraph and ratio columns; P-03 does not touch them — two READY tasks; W0-06 has the committed audit script — cost if wrong: DESIGN.md shows stale ratios between P-03 and W0-06 (one phase).
- Ruling: [I-9] P-04c owns `ios.supportsTablet: false`; W7-15 CUT; its owner check (no prior iPad submission) moves to the owner queue — duplicate change — cost if wrong: none.
- Ruling: [I-10] W8-12 owns ATT, SKAdNetwork and the privacy manifest; W7-14 CUT; W8-12 now depends on W7-01, W7-03, W7-12, W8-03 — duplicate spec with the same owner gate; iOS tranche lives in W8 — cost if wrong: none.
- Ruling: [I-11] The iOS capture script and calibration move from W8-02/W8-04 into P.md as P-02b (READY) and P-02c (BLOCKED-ON-DISK); W8-02, W8-04 and W6-17 become CUT stubs; W8-05/06/08/09/10/16 depend on P-02b/P-02c — "no task other than P-02 builds a capture harness", and three tasks specified the iOS path — cost if wrong: P.md grows; W8 readers follow stubs.
- Ruling: [I-12] W6-01 owns the visibleMask rebuild measurement; W3-07 CUT with its level-11 point and null-run floor carried into W6-01; W3-15 depends on W6-01; W6-01 must precede W6-06 but does not gate W0-04 — duplicate measurement; W0-04 step 5 leaves `excludedArrows` and the mask memo untouched and its criterion 7 A/B covers the blocked phase — cost if wrong: if W0-04 did change rebuild count, the "before" baseline would be post-W0-04 (W6-01's doc records which commit it measured).
- Ruling: [I-13] W5-01's `silhouettePath` is the single mask-to-path drawing; W4-09's `shapeTilePath.ts` row-run merge is CUT; W4-08 renders through `silhouettePath`; W4-08 and W4-09 depend on W5-01 — duplicate function, and row-runs were rejected for seams — cost if wrong: gallery tiles inherit any tracer bug (round-trip test covers the gallery rows).
- Ruling: [I-14] W4-01 owns the catalogue (`SHAPE_CATALOGUE` in `src/core/shapeCatalogue.ts`, `shapeNameForLevel`); W3-05 versions `shapeNameForLevel`, W3-10 adds the v2 branch and hard-depends on W4-01 (no fallback copy of the order-lock test), W3-18 appends ids there; W4-01's context no longer claims a v1 module fork — W3 referenced a `ShapeLibrary.Catalog` that W4 does not create, and "lands that exact test first" made two owners — cost if wrong: W3-10 cannot start before Phase 4 (it is Phase 5).
- Ruling: [I-15] W3-05 wires W4-07's collection fold through `levelGenVersion(index)` — W4-01's contract made it W3's obligation but no task step did it — cost if wrong: v2 levels would record v1 shapes in the gallery.
- Ruling: [I-16] W4-03's daily pool lists Square and Rectangle exclusions by id and must not read `ShapeDef.fillsGrid` — W3-03 (Phase 5) deletes that field after W4-03 (Phase 4) lands — cost if wrong: W3-03 breaks the daily build.
- Ruling: [I-17] W4-11's review gate excludes clears while `ftueStage === 2`, and depends on W1-03 — W1-06's assist makes pre-removal blocked taps free, so an assisted clear is not a real perfect clear (W1-06's risk note) — cost if wrong: one of three lifetime prompts spent on an assisted level 1.
- Ruling: [I-18] W1-01 is the sole viewport-measurement owner; the level-0 cross-check between W1-01's metrics script and W3-01's probe moves to W1-01, because W3-01 (Phase 2) lands first — W3-01's acceptance cited a script that would not exist yet — cost if wrong: none.
- Ruling: [I-19] W6-15 depends on W7-03 (CMP integration) and W7-07 (legal review and published policy), not W7-02 (the spike); W6/W7/W0 cross-references to draft ids W6-08, W7-09 and W7-14 corrected to W6-02/W6-03, W7-06/W7-07 and W8-12 — stale draft ids pointed at unrelated consolidated tasks — cost if wrong: telemetry turned on without the integrated CMP.
- Ruling: [I-20] W2 is split across phases: W2-01, W2-02, W2-03 in Phase 3 (reduce-motion census and the lose panel cutting off the fatal-tap explanation are first-session legibility defects, and they need P-02); W2-04…W2-12 in Phase 6 — the phase list did not place W2; under Track A feel work is supporting, and W3 legibility comes first — cost if wrong: level→level cut and panel motion arrive late.
- Ruling: [I-21] W5-01 and W5-04 are Phase 4 ("silhouette celebration"); every other W5 task, including the W5-03 counter-reflow defect, is Phase 6 — follows the given phase list; W5-03 needs P-02 and has no first-session urgency — cost if wrong: the counter jitter ships longer.
- Ruling: [I-22] W7 compliance drafts (W7-06, W7-05, W7-08, W7-09, W7-04) sit at the head of Phase 6 as the phase list says, not in Phase 0/1 as W7's consolidator asked — the given phase list is binding structure; within Phase 6 legal exposure goes first — cost if wrong: the false policy text and dead policy URL stay live through Phases 1–5; the owner can pull W7-06/W7-05 forward (they depend only on W0-01/W0-02/P-03).
- Ruling: [I-23] W7-11 (ad baseline, BLOCKED-ON-OWNER) is Phase 2, and the owner export should precede the W0-05 release — the baseline must predate the pacing change it measures — cost if wrong: no clean before/after for W0-05.
- Ruling: [I-24] Serial order runs W0-07 before W6-02, so the W0 release candidate does not include the kill-switch client — W6-02's flag is OFF with no URL until W6-03, so shipping it early buys nothing; W0-07 verifies exactly the W0 set — cost if wrong: W6-03 needs a later release containing W6-02 anyway.
- Ruling: [I-25] P-04a runs first and its non-perf `da93dcd` release APK is kept as the shared base artifact for P-01's over-install and W0's pre-fix reproductions — three tasks each asked for the same base build on a 10 GB disk — cost if wrong: one extra 4 GB build.
- Ruling: [I-26] W3-21 hard-depends on W0-08 (the owner release that carries P-01) — W3-21 required "P-01 released" but named no task — cost if wrong: v2 freeze waits on an owner release decision.
- Ruling: [I-27] W1-11's dependency on W3-12/W3-17 is the only backwards-phase edge and gates only the `FTUE_ASSIST_ENABLED` flip — C-12 joint re-measurement — cost if wrong: the assist flag cannot flip before Phase 5 completes.
- Ruling: [I-28] W1-07's stall hint on tutorial boards does not contradict O-3b: it is not conditioned on ad availability, never runs on a real board, ships OFF with a null delay, and the owner rules in W1-11 — the rejected item is "free hint when no rewarded ad can be served" — cost if wrong: if the owner reads O-3b more broadly, W1-07 is cut in W1-11 at no player cost.
- Ruling: [I-29] Per-workstream flag modules stay as specified (`ftueConfig.ts`, `motionFlags.ts`, `artConfig.ts`, `generatorVersion.ts`, `featureFlags.ts`); `featureFlags.ts` is created by the first task in serial order that needs it (W6-02) and appended by W4/W6/W7 — unifying would rewrite five task files without changing behaviour, and each module has one owner per flag — cost if wrong: five places to audit before a release; W5-19 and W0-07-style packets must list all of them.
- Ruling: [I-30] W0-04's hand-run `screenrecord` plus an uncommitted scratchpad analysis script is not a capture harness; P-02 (with P-02b/P-02c) remains the only committed instrument — C-8 targets committed duplicate harnesses; W0-04 cannot wait for Phase 2 — cost if wrong: W0-04's durations are measured with an uncalibrated recorder (it reconciles against the display frame period).
- Ruling: [I-31] "`animator_duration_scale`" wording corrected across W0, W1, W3, W4, W5, W6, W7 to P-02's motion scale (all three scales set and read back; Reanimated reads `transition_animation_scale`) — P's read of `NativeProxy.kt` — cost if wrong: a capture sets one scale and records a variant that is not running.
- Ruling: [I-32] `TASKS.md` lists hard dependencies only; prose mentions that are consumers, coordination notes or release couplings (for example W0-01 → W0-02 release coupling, W2-06 ↔ W5-17 window, W6-06 ↔ W2-06 signature) are shown as soft notes — mixed prose would otherwise create false cycles — cost if wrong: a real ordering need hidden in a soft note (each was reviewed).
- Ruling: [I-33] Cut or moved tasks remain as stubs with their original ids (W3-07, W5-10, W6-17, W7-14, W7-15, W8-02, W8-04) — nothing silently disappears — cost if wrong: none.
- Ruling: [I-34] W0-08 depends explicitly on P-04a, P-04b and P-04c; W7-07 and W7-10 likewise — "P-04" is three parts with different statuses — cost if wrong: none.
- Ruling: [I-35] W6-11 stays READY in the serial order and is recorded CUT and skipped if W6-10 reports DOES NOT RESOLVE — a conditional task still needs a slot — cost if wrong: none.
- Ruling: [I-36] Serial order never moves back a phase; owner gates are marked "waits:" on the task they gate rather than reordering around them — one implementer, one checkout, shared emulator — cost if wrong: idle time while an owner decision is pending (the implementer continues with the next task that does not depend on it).

## Task log


## Critique fixes

Adversarial pre-execution review, 2026-09-16. Each line: task id — defect class (a–h) — what was wrong — what changed. Citations were checked against the repo at `da93dcd`.

- P-01 — (f) — the `RKStorage` / `catalystLocalStorage` names were cited to `next/StorageSupplier.kt:92`, which is the opt-in Room storage's migration from the old table; the default implementation is `ReactDatabaseSupplier.java:25,30` — citation corrected, with the "next" storage's own names noted for the `.tables` check.
- P-01, P-04a, W0 (header), W7-02 — (d) — disk gates of 3, 4 and 5 GB were unlabelled and inconsistent — harmonised to ≥ 5 GB (about 4 GB build + 1 GB margin) and labelled OWNER-PICKED STARTING VALUE; P-04a's 3 GB bundle gate labelled.
- P-03 — (b) — Change step 1 told the author to write "no 'puzzle depth'" into PRODUCT.md, which Acceptance 1's grep bans, so the task could not pass as specified — step 1 reworded ("difficulty never comes from tap order").
- P-04a, TASKS.md, W0 header, W0-04, W0-07 — (a) — the shared base APK, W0 evidence and W0-04's analysis script were to live in a session scratchpad, which later tasks in other sessions cannot reach (the plan already lost its no-dead-end script that way) — P-04a now writes `artifacts/base/da93dcd-release-arm64.apk` + `.sha256` and adds `/artifacts/` to `.gitignore`; W0 evidence and the analysis script go under `artifacts/w0/`.
- W0-01 — (b) — criterion 2's "a win followed by Next level shows no modal" passes on the unfixed build too, because one finished game never makes an interstitial due (`GAMES_PER_INTERSTITIAL = 2`) — criterion now makes the interstitial due (loss + win) and requires the base APK to show the TEST AD interstitial on the same sequence.
- W0-01, W0 header, W7-02 — (a) — release builds print none of the app's `[ads]` lines (all `__DEV__`-gated), but pre-fix and GATE 0 steps read them from logcat — temporary unconditional log (or LevelPlay native tags) made explicit.
- W0-02 — (d)/(c) — criterion 4's "twice the last delay" window labelled OWNER-PICKED; criterion 6's `Hint unavailable` copy now requires a screencap (the owner accepts copy from it).
- W0-03 — (b) — criterion 2 measured splash duration without reading back `transition_animation_scale`; at 0 (the perf harness default) `withDelay` is skipped and every value trivially beats `SPLASH_MAX_MS` — scale 1 read-back required; sample count 5 labelled.
- W0-03 — (b)/(c) — "reaches the menu" could pass with the menu mounted but invisible inside the `FadeIn` entering wrapper (`App.tsx:63`, opacity starts at 0) — criteria 3–4 now require a screencap showing the Play pill and "Level N".
- W0-03 — (b) — criterion 4 (HOME during splash) can pass through the JS timer catching up on resume, never exercising the AppState branch it claims to test — a temporary caller log in `finish()` is required, and a `timer` result records the AppState branch as unit-test-only.
- W0-04 — (d) — "within 1 s" labelled OWNER-PICKED.
- W0-05 — (b) — criterion 2's web sequence (lose, reload, lose, win) also shows the interstitial on the base commit, since the second loss and the win reach 2 in one page life — replaced by lose, reload, win (base: no interstitial; fix: interstitial); the `<P-01 key>` placeholder replaced by `arrows_finished_games`.
- W0-07 — (b)/(a) — it re-ran criteria that need temporary log patches (W0-02 c3, W0-03 c2, W0-04 c2, W0-05 c3, and a "temporary log" for the schema version) "on the candidate APK with the recorded SHA-256", which is impossible without changing the artifact — log-free substitutes specified (screencaps, `settings get` read-back, `ffprobe` timestamps, `sqlite3` dumps of `RKStorage`), with a separately hashed diagnostic build as the labelled fallback.
- W0-08, TASKS.md owner queue — (e) — ruling I-23 needs the W7-11 LevelPlay export before the W0-05 release, but W7-11 sat in Phase 2 behind the Phase 1 release with no note on the release step — the export is now an explicit owner step inside W0-08.
- W6-02 — (b)/(e) — `App.tsx:35` calls `initAds()` before `initSaveSystem()` resolves, so a persisted kill always reads 0 at init time and `LevelPlay.init` runs anyway; the acceptance never checked init under a cached kill — boot order fixed in the spec (init ads only after hydration settles) and a "cached kill at boot → no init line" criterion added.
- W6-02 — (a) — the two-direction emulator flip cannot run with the committed `REMOTE_KILL_SWITCH = false`, and the URL's source was unstated — local uncommitted flag flip and `REMOTE_CONFIG_URL = process.env.EXPO_PUBLIC_REMOTE_CONFIG_URL ?? ''` specified.
- W6-02 — (g) — "ads return" in the second direction invited reaching a due interstitial with the network on, against W0-02's never-display-a-live-ad rule — return is now observed by init/`onAdLoaded` logs and an enabled-button screencap only.
- W4-11 — (d)/(a) — lifetime `count < 3` was an unlabelled picked number; `shouldRequestReview`'s signature lacked the `assisted` input its own rule and test require — labelled, and `assisted` added to the signature.
- W7-02 — (d) — "at most two candidates" labelled OWNER-PICKED.
- TASKS.md — (a) — the binding rulings cite draft ids (for example "W7-11 is CUT", "W0-03 reduce motion") that now name different consolidated tasks — an id-mapping note was added to "How to read this file".
- TASKS.md — (h) — plan §4 "web-renderer regression coverage" had no task and no CUT record, and "sound design" is only partly covered — both listed under "Plan items with no task and no CUT record" for a controller or owner decision. Neither was cut or given a task by this critique.

## Controller rulings on the critique's unresolved concerns (2026-09-16)

- Ruling C-1: commit `docs/next-level/` before Phase 0 — every brief must have a committed base the reviewer can diff against — cost if wrong: none (docs only).
- Ruling C-2: `/artifacts/` added to `.gitignore` — the critique asserted it was already ignored; `git check-ignore` proved it was not, and P-04a writes release APKs signed with the real Play upload key into it in a public repo — cost if wrong: none.
- Ruling C-3: W1-05/W1-06 first-session free blocked taps stay flagged OFF and go to the owner at W1-11. It is FTUE mercy, not an ordering mechanic, so owner ruling 1 does not forbid building it — cost if wrong: the flag and its code are dead weight until removed.
- Ruling C-4: W4-07 invisible shape-collection recording may default ON — it is additive, invisible, and its keys are registered and migrated by P-01; ruling 5 covers player-VISIBLE features — cost if wrong: extra int keys in saves, inert for older builds, removable.
- Ruling C-5: W1-07's tutorial stall hint stays flagged OFF pending the owner at W1-11 — owner ruling 3 rejected a free hint as an ad-failure fallback on real boards; whether a tutorial-only hint is covered is the owner's reading, not ours — cost if wrong: one flag flip.
- Ruling C-6: W0-04's web reduce-motion check uses a local headless browser with media emulation ONLY if one is already installed; no new download. Otherwise it is recorded UNVERIFIED and the emulator is the primary evidence — cost if wrong: the web SVG twin's static state ships unobserved.
- Ruling C-7: owner acceptances inside READY tasks (P-03 PRODUCT.md diff, W0-02 copy, W0-04 reduced-motion look, W0-06 screenshots) do NOT pause execution — superpowers forbids per-task check-ins. The work lands on `next-level` (never main), and acceptances are collected into one owner review at each phase end — cost if wrong: rework of an unaccepted diff before merge.
- Ruling C-8: Reanimated 4.5.1 decides reduced motion from `Settings.Global.TRANSITION_ANIMATION_SCALE` (verified `NativeProxy.kt:324-329`), read once at app start. On the booted `fleet_floor_api31` (emulator-5556) all three scales currently read 0, so Reanimated reports reduced motion ON until a task sets them and relaunches the app — cost if wrong: every motion capture silently shows the reduced variant.

### Phase 0 — 2026-09-16 (workflow wf_2646ae5e-68e)

- Task P-04a: complete (commits 3a9c06a..2487fa7, review clean) — targetSdk 36 read by aapt from a locally built release APK and by bundletool from the AAB; base APK kept at artifacts/base/da93dcd-release-arm64.apk (built at a1e2b43, app code identical). EAS cloud .aab stays UNVERIFIED until P-04b.
- Task P-04c: complete (commits bfa67eb..bba6da2, review clean) — `ios.supportsTablet: false`; Expo's own device-family mod prints "1".
- Task P-03: complete (commits 8be5441..0809145, review clean) — PRODUCT.md rewritten for Track A; DESIGN.md claims cited; no-dead-end jest test and tier-bands script added. 4 minors deferred to final review (two remaining "puzzle" words, stale App.tsx:56 comment, "full grid cell" tap-target line vs 22 pt disc, DESIGN typography/layout rewrite beyond list).
- Task P-01: complete (commits 574768c..51318c9, review clean) — 29-key registry, SCHEMA_VERSION 1, additive migrate() guarded by persistenceHealthy. Controller eyeballed artifacts/P-01/menus-step5-to-9.png: base, upgrade, base reinstall, P-01 reinstall and version-99 all show Level 38 / 412 solved / 4-day streak / best run 9 / Ink Night / sound off. 4 minors deferred (dayStreak getter not asserted; one surviving mutation on setPersistenceHealthy(false) before hydrate; public registeredKeys/setPersistenceHealthy; negative version inert).
- Task P-05: complete (commits 51eb9a5..7c83d4d, review clean) — .github/workflows/ci.yml; first GitHub run pending after push.
- Task W7-02: BLOCKED (commit 1e431c6 records npm and certified-CMP checks only) — disk fell to 1.8 GB during the run; nothing installed or built. Rerun in Phase 1.

### Controller and owner rulings after Phase 0

- Ruling D-1 (owner, 2026-09-16): PRODUCT.md Track A wording APPROVED as-is — owner answered "Approve as-is" — cost if wrong: none (docs).
- Ruling D-2 (owner): push `next-level` to origin APPROVED (branch only, no merge/PR/release). Pushed at 1e431c6. Each later push is a separate yes — cost if wrong: public docs/reports exposure; controller checked no artifacts/credentials/APK/keystore in the 18 commits.
- Ruling D-3 (owner): prior App Store Connect submission with iPad support: "Not sure". Keep supportsTablet false; OWNER QUEUE: check App Store Connect before any iOS submission, revert bfa67eb if an iPad build ever shipped — cost if wrong: an iOS update rejected for dropping iPad support.
- Ruling D-4 (owner): approved freeing disk. Controller removed repo `ios/` (gitignored, 0 tracked files), `~/.gradle/caches` (no Gradle daemon running), AVD Pixel_6_Pro via avdmanager. tablet_api36 was ALREADY absent before the command (removed by another process). Freed 8.9 GB → 11 GB free — cost if wrong: next Android build re-downloads deps; an iOS prebuild must re-run.
- Ruling D-5: a separate Claude session in the owner's `geoguesser-app` project runs on this Mac at the same time. It booted Pixel_8_Pro on port 5580 with `-tcpdump` at 16:42 (script under that project's scratchpad) and competes for disk and emulators. Every capture must name `-s emulator-5556` explicitly and re-check `df -h /` immediately before a build — cost if wrong: a capture taken on the wrong emulator, or a build failing mid-way.
- Ruling D-6: W7-02 rerun may use a LOCAL scratch branch `scratch/cmp-gate0` cut from next-level, never pushed, deleted afterwards; `npm install` is allowed there only; the ONLY thing that lands on next-level is the findings doc. After the spike: switch back to next-level, `npm ci`, confirm `git status` clean and jest+tsc green. Disk gate for the spike: build (≈4 GB) + node_modules restore (≈2 GB) + 1 GB margin = 7 GB free before starting (OWNER-PICKED) — cost if wrong: the main checkout left without a working node_modules.
- Ruling D-7: P-01 API widening (public `registeredKeys`, public `setPersistenceHealthy()`, negative stored version inert) ACCEPTED by controller — needed because initSaveSystem lives in another module and acceptance 2 walks Keys — cost if wrong: a consumer could call setPersistenceHealthy at the wrong time; documented init-only.

### Phase 1 — 2026-09-16/17 (workflow wf_08ef4cea-010, resumed once after a session limit)

- Task W7-02: complete (commits 59c7f4c..52b9240, review clean) — GATE 0: `@usercentrics/react-native-sdk` 2.31.0 PASS (release R8 build, same-pid bridge call + LevelPlay init success, no new permissions); `react-native-google-mobile-ads` 16.5.0 FAIL (Kotlin 2.3.0 metadata vs RN's 2.1.20). Scratch branch deleted, never pushed. Owner decides the W7-03 approach (docs/cmp-gate0-2026-09-16.md).
- Task W0-01: complete (commits 6f3a8b8..de2231f, review clean) — simulated TEST AD gated on `__DEV__`. Controller re-ran `artifacts/W0-01/grep-strings.sh`: base APK 1/1/1/1 test-ad strings, fixed APK 0/0/0/0.
- Task W0-02: complete after 3 fix rounds (commits 7e1fb1e..e2f480f, review clean) — honest "No ad available right now — Retry is free" / "Hint unavailable" states, no free hint, recoverable init. Owner acceptances: both strings, disabled hint opacity 0.35 (OWNER-PICKED), disabled Continue look.
- Task W0-03: complete (commits 0a8c1ee..92269e1, review clean) — splash backstop SPLASH_MAX_MS 4000 (OWNER-PICKED; measured cold starts 2663–2766 ms on emulator), font error and save-init rejection both reach the menu.
- Task W0-04: complete (commits 35be887..80959cb, review clean) — reduce-motion players see a static solid-heart blocker (1.6× stroke) and tapped arrow. Owner acceptance of the static look pending.
- Task W0-05: complete (commits e7fb855..293fe10, review clean) — `arrows_finished_games` (already in P-01's registry) now read/written through SaveSystem accessors; counter survives cold start and resets only on onAdDisplayed. No schema change, no wipe risk.
- Task W0-06: complete (commits 8b6bfc2..56c370e, review clean) — contrast instrument committed first (26 FAIL on pre-fix tokens, re-run by reviewer and controller: 76 gated rows, 0 FAIL at HEAD). Owner acceptances: before/after set, LEVEL N 24 px vs alternatives, disabled Continue white pill, greyscale blind test, strike/pip OWNER-PICKED values.
- Task W0-07: 2 fix rounds, 1 finding parked with Ruling E-1 (commits 78d327c..e4373c1) — one hashed release candidate (sha256 497e6766…5e60) passes over-install, APK grep, gates, and W0-01/02/03/05/06 re-runs; W0-04's static look, zero displacement, stroke, pip drop and criterion 4 hold. W0-04 criterion 2's absolute flash-duration windows are NOT MET on the candidate AND on W0-04's own APK (paired A/B/A/B on a SurfaceFlinger clock, overlapping ranges).
- Task W6-02: complete (commits 073a24a..7db2ebf, review clean) — kill-only remote config client, `REMOTE_KILL_SWITCH = false`, no URL; both directions flipped on emulator with a diagnostic release build. Owner acceptances: REMOTE_CONFIG_MAX_BYTES 4096 / TIMEOUT 3000 ms (OWNER-PICKED), mid-session re-enable starting init, docs/remote-config.md wording.

### Controller rulings after Phase 1

- Ruling E-1 (W0-07 finding at the fix cap): W0-04 criterion 2's absolute windows (D₁ ∈ [16P, 22.4P], D₂ ∈ [37P, 43.4P]) are parked as a fragile criterion, not a candidate regression. Evidence: spans land on whole vsync frames and the lower bounds sit exactly on 16P/37P; handler → first flash paint is ~70–110 ms, which the −2P allowance cannot absorb; W0-04's own APK scored 1/6 against the candidate's 3/6 with overlapping ranges. W0-08 must not ship on "criterion 2 passed" — OWNER QUEUE: choose (b) start the reduce-motion lifetime at the flash's first commit (W0-04 hardening) or (c) paired A/B plus an OWNER-PICKED floor (e.g. D₁ ≥ 12P, D₂ ≥ 33P). Controller recommendation: (b), because the criterion then measures the design rather than paint latency, with (c) acceptable if the owner wants W0-08 without a new build — cost if wrong: one extra W0-04 fix task.
- Ruling E-2: JS-repack captures (Hermes release bundle over the gradle-built base APK, debug-signed) are accepted as per-task evidence for JS-only changes; W0-07's gradle release candidate is the one place every W0 fix ran together — cost if wrong: a gradle-only difference, which W0-07 was built to catch.
- Ruling E-3: owner-key LevelPlay fill never happened on the emulator; the "enabled" half of W0-02 criterion 3 was shown only with Unity's sample credentials on a diagnostic APK that displayed 0 ads. OWNER QUEUE before W0-08: confirm fill from the LevelPlay dashboard or a physical device — cost if wrong: players see "No ad available" everywhere.
- Ruling E-4: Phase 2 runs JS/doc-only tasks first (W3-01, W3-02, W6-04, W6-05, W6-10, W6-12+W6-13) and emulator/build tasks after (P-02, W6-01, W6-06, W6-07, W6-08, W6-11, W6-14), because free disk is 4.8 GB, under the 5 GB build gate. Every dependency still precedes its dependent — cost if wrong: none; P-02 lands later than the serial order, and nothing in the first group consumes it.
