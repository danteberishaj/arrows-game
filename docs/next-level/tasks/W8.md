# W8 — iOS

Keep iOS buildable and honest now, at the lowest cost that stops the rot, and specify the "ship it"
work without starting it. The Swift board view and the Swift feedback module have not been through a
compiler since 2026-09-01. Tranche 1 compiles them, looks at them running on a simulator, measures
the board view's memory, replaces its board-sized `draw(_:)` bitmap with two retained shape layers,
and proves that nothing visible changed. Two preparation tasks (W8-01 here, and P-02b in `P.md`) fit on today's 10 GB
disk. Every other tranche 1 build step waits for at least 20 GB free. Tranche 2 (ATT and the privacy
manifest, per-platform ad keys, consent, the EAS/App Store pipeline, a pre-submission pass, and the
save-backup note) is deferred under the owner-approved plan until Android retention work has landed.
It also waits on owner decisions this file names. W8 changes nothing on Android, persists no key, and
alters no JS↔native protocol. Nothing here is merged to main, pushed, published, or submitted
without an explicit owner yes.

**Merged from:** W8-draft1 W8-01…W8-14 · W7-draft1 W7-17 (into W8-12) · W6-draft1 W6-14 (into
W8-02 and W8-04; `P.md` hands it to the iOS work). Red-team findings applied: brand-13 (W8-04
invented memory fractions, W8-07 invented thresholds), evidence-15 (W8-04 gates written before
W8-03 measures, W8-07/W8-08 numbers, the W2-08↔W8-04 same-file overlap), skeptical-14 (W8-07 gated
on the iOS ship decision, W8-09 gated on the monetisation ruling and on the CMP GATE 0 and
demand-source outcomes, W8-04/W8-07/W8-08 numbers), performance-16 (the Swift reduce-motion exit
branch at `ArrowsBoardView.swift:168,177-178,191-195`; the blocked tap rebuilds the visibility
mask twice).
**Id map** (ids in the Merged-from and red-team sentences above are draft ids): W8-01 ← new (preparation split out of d W8-01) · W8-02 ← W6-14 (script half; now MOVED to P-02b) ·
W8-03 ← d W8-01 + P-04c's open check · W8-04 ← W6-14 (calibration half; now MOVED to P-02c) · W8-05 ← d W8-02 ·
W8-06 ← d W8-03 · W8-07 ← d W8-04 · W8-08 ← d W8-05 · W8-09 ← d W8-06 · W8-10 ← d W8-07 ·
W8-11 ← d W8-08 · W8-12 ← d W8-09 + W7-17 · W8-13 ← d W8-10 · W8-14 ← d W8-13 · W8-15 ← d W8-12
(minus `eas init`) · W8-16 ← new · W8-17 ← d W8-14. Cut: d W8-11 (moved) and the invented
thresholds, listed at the end.

**Base commit for every pre-fix observation:** the `next-level` tip before the task's first commit
(`da93dcd` at the time of writing).

**Facts executed on 2026-09-16 that several tasks rely on:**
- `df -h /` → 10 GiB available. `docs/IOS_BOARD_PLAN.md:81-82` needs ≥ 20 GB, and the 2026-09-01
  attempt failed at 132 MB free. `~/Library/Developer/Xcode/DerivedData` does not exist.
  `~/Library/Developer/CoreSimulator` is 4.1 GB.
- `xcodebuild -version` → Xcode 26.6 (17F113). `xcrun --sdk iphonesimulator --show-sdk-version` →
  26.5. `xcrun swiftc --version` → Apple Swift 6.3.3.
- `xcrun simctl list runtimes` → iOS 18.0, 18.1, 18.3, 18.4, 18.6, 26.5. A device named "iPhone 16
  Pro" exists under **each** 18.x runtime, so the name alone is ambiguous in `-destination`. iOS 26.5
  has iPhone 17 Pro, 17 Pro Max, 17, 17e and Air. "iPhone SE (3rd generation)" exists under 18.0 and
  18.1.
- `xcrun simctl ui` offers only `appearance`, `increase_contrast` and `content_size`. It has no
  reduce-motion or slow-animation option. `simctl` has no tap command, `idb` is not installed, and
  `ffmpeg`/`ffprobe` are at `/opt/homebrew/bin`.
- `git check-ignore -v ios` → `.gitignore:40:/ios`. The on-disk `ios/` tree dates from
  2026-09-01 19:49, and `ios/Pods` is 1.2 GB. Its `Info.plist` and `PrivacyInfo.xcprivacy` are stale
  generated output. `ios/Podfile.properties.json` sets `"EXPO_USE_PRECOMPILED_MODULES": "true"` and
  `"apple.privacyManifestAggregationEnabled": "true"`.
- `git log` on `modules/arrows-board/ios/` → last change `0f7968b` (2026-09-01).
  `modules/arrows-feedback/ios/ArrowsFeedbackModule.swift` → last change `ec03073` (the eight
  `pop0..pop7` clips, committed).
- Fixture metadata from `npx tsx scripts/perf/core-benchmark.ts --metadata` (0-based level index, as
  `EXPO_PUBLIC_PERF_LEVEL` expects via `App.tsx:78`; the HUD shows index + 1, `GameScreen.tsx:210`):
  - index 0: 20×20, 60 arrows, Circle, checksum `31ac1d4e`;
  - index 3827: 39×39, 250 arrows, Flower, `b1f50ecb`, blocked cell r35 c19, free cell r3 c16;
  - index 935: 45×45, 249 arrows, Crescent, `546a69c0`, blocked cell r42 c24, free cell r2 c17.
  Every capture records the checksum it ran, because W3's generator flag would change these boards.

---

## W8-01 — Type-check both Swift modules and tabulate Kotlin↔Swift exit parity, without a build

- **Status:** READY
- **Effort:** S
- **Depends on:** none
- **Flag:** none (no runtime change)
- **Files:** `docs/IOS_BOARD_PLAN.md` (edit). Read-only: `modules/arrows-board/ios/ArrowsBoardView.swift`,
  `modules/arrows-board/ios/ArrowsBoardModule.swift`,
  `modules/arrows-feedback/ios/ArrowsFeedbackModule.swift`,
  `modules/arrows-board/android/src/main/java/com/danteb/arrows/board/ArrowsBoardView.kt`,
  `node_modules/expo-modules-core/ios/Api/Factories/PropertyFactories.swift`,
  `node_modules/expo-modules-core/ios/Api/Factories/ViewFactories.swift`. Stubs live in the session
  scratchpad and are never committed.

### Context
No iOS build has succeeded since the 2026-09-01 exit-protocol change. `docs/IOS_BOARD_PLAN.md:3-4`
says "iOS compiles and draws correctly but has never been measured". The same file at `:113` says
"iOS was NOT compiled", so the status line is false. The W8 draft author reported (2026-09-09) that
`swiftc -typecheck` of `ArrowsBoardView.swift` against a stubbed `ExpoView` produced zero errors.
That file has not changed since. `ArrowsFeedbackModule.swift` has changed (`ec03073`), and neither
module's ExpoModulesCore DSL file (`ArrowsBoardModule.swift`, the `definition()` in the feedback
module) has been checked.

While writing this task, a type-check against the real precompiled framework was attempted:
`xcrun swiftc -typecheck -target arm64-apple-ios16.4-simulator -sdk <sim SDK> -F ios/Pods/ExpoModulesCore/ExpoModulesCore.xcframework/ios-arm64_x86_64-simulator …`.
It failed inside the framework before reaching our code (EXECUTED):
- `no such module 'ExpoModulesJSI'`, a source pod that must be built first;
- `failed to build module 'ExpoModulesCore'; … built with 'Apple Swift version 6.3.1' … while this compiler is 'Apple Swift version 6.3.3'`.

So without a build, stubs are the only type check available. What the 6.3.1/6.3.3 message means for
a real build is unclear (INFERRED). W8-03 records whether it recurs.

Exit-protocol parity, READ from source:

| Aspect | Kotlin (`ArrowsBoardView.kt`) | Swift (`ArrowsBoardView.swift`) |
|---|---|---|
| header tokens | 10 (`:392`) | 10 (`:86`) |
| duration band | 160..1000 ms (`:393-394`) | 160...1000 (`:97`) |
| trail points | 2..4098 (`:175`, `:389`) | 2...4098 (`:102`) |
| concurrent slots | 2 (`:391`) | 2 (`:34`) |
| reducedMotion token | `tokens[3] == "1"` (`:163`) | `tokens[3] == "1"` (`:129`) |
| travel | exact `progress²·totalLength` (`:269`) | `CAMediaTimingFunction(0.11, 0, 0.5, 0)` approximation (`:164`) |
| dash phase | `DashPathEffect(intervals, -travelled)` (`:281`), intervals `[body, total+body]` (`:198-199`) | `lineDashPhase` `patternSum → patternSum − totalLength`, `patternSum = total + 2·body` (`:137`, `:146-147`, `:167-168`); congruent modulo one period (INFERRED), sign never observed |
| fade | 1 until 0.55, then smoothstep (`:270-276`) | keyframes `[1,1,0]` at `[0,0.55,1]`, linear then easeInEaseOut (`:190-195`) |
| reduced motion | travelled 0, linear `1 − progress` (`:269-271`) | phase and translation `toValue == fromValue`, linear `[1,0]` (`:168`, `:177-178`, `:191-195`) |
| alpha application | paint alpha set on trail and head separately (`:282-283`) | container layer opacity (`:199`); where head overlaps trail, Android may composite darker mid-fade and iOS may not (INFERRED) |

### Change
1. In the scratchpad, write a stub `ExpoModulesCore` module. It declares only the symbols the three
   files use: `ExpoView` with `init(appContext:)`, `AppContext`, `Module`, `ModuleDefinition`,
   `Name`, `View(_:_:)`, `Prop`, `Function`, `OnAppEntersForeground`, `OnAppEntersBackground`,
   `OnDestroy`. Copy each signature from `node_modules/expo-modules-core/ios/**` (version 57.0.14,
   the version `ios/Podfile.lock:46` pins). Emit it with
   `xcrun swiftc -emit-module -module-name ExpoModulesCore -target arm64-apple-ios16.4-simulator -sdk "$(xcrun --sdk iphonesimulator --show-sdk-path)" -emit-module-path <scratch>/ExpoModulesCore.swiftmodule <scratch>/stub.swift`.
   The 16.4 target matches both podspecs (`ArrowsBoard.podspec:9`, `ArrowsFeedback.podspec:9`).
2. Type-check with `-I <scratch>`:
   - (a) `ArrowsBoardView.swift` with `ArrowsBoardModule.swift`;
   - (b) `ArrowsFeedbackModule.swift`.
3. Append a section "Pre-build static check (<date>)" to `docs/IOS_BOARD_PLAN.md` containing:
   - the exact commands, toolchain versions and exit codes;
   - verbatim diagnostics;
   - the stub source (so the check can be reproduced);
   - the precompiled-framework failure above;
   - the parity table, with each row tagged READ or INFERRED and "observe in W8-05".
4. Replace the false status line at `docs/IOS_BOARD_PLAN.md:3-4` with the true one: never compiled
   since 2026-09-01, never measured.
5. If a type error is in our code, record it and do not fix it here. The fix belongs to W8-03, where
   a real build can confirm it.

### Non-goals
Any build, `pod install` or `expo prebuild`. Editing Swift, Kotlin or anything under `ios/`.
Fixing parity differences.

### Acceptance criteria
- `docs/IOS_BOARD_PLAN.md` holds both type-check commands with exit codes and verbatim output, the
  stub source, and a parity table of at least the ten rows above, with file:line on both sides.
- The status line no longer claims iOS compiles.
- `git diff --stat` lists only `docs/IOS_BOARD_PLAN.md`.

### Verification
1. PRE-FIX REPRODUCTION: `git log --oneline -- modules/arrows-board/ios modules/arrows-feedback/ios`
   shows `ec03073`, `0f7968b`, `0a1df94`, and no build log exists. `sed -n 3,4p` and `sed -n 113p`
   of `docs/IOS_BOARD_PLAN.md` contradict each other.
2. The two `swiftc -typecheck` runs check our three files' Swift against the real iOS 26.5 simulator
   SDK. They say nothing about whether the stub matches the real DSL, and the doc must say so.
3. TDD: not applicable (no logic).
4. Everything at runtime stays UNVERIFIED until W8-03 and W8-05.

### Risk and rollback
A stub that differs from the real DSL gives a false pass or fail. Copying signatures from source
limits this, and W8-03 is the real check. Rollback: revert the doc.

---

## W8-02 — (MOVED to P-02b)

- **Status:** CUT here, MOVED (integrator 2026-09-16). Only P-02 may build a capture instrument (controller ruling), so the iOS capture script is now **P-02b** in `P.md`, unchanged in substance. W8 tasks that consumed "W8-02" now depend on P-02b.

---

## W8-03 — First compile of the iOS app from a clean prebuild; record the baseline

- **Status:** BLOCKED-ON-DISK. `df -h /` must show ≥ 20 GB available (10 GB on 2026-09-16). The agent
  must not delete owner files, simulator runtimes or caches it did not create to get there.
- **Effort:** S
- **Depends on:** W8-01, P-04c (`ios.supportsTablet: false` must be in `app.json` before the prebuild)
- **Flag:** none
- **Files:** `ios/` (generated, never hand-edited), `docs/ios-build-log-<YYYY-MM-DD>.md` (new). Only
  if a compile error is in them: `modules/arrows-board/ios/ArrowsBoardView.swift`,
  `modules/arrows-board/ios/ArrowsBoardModule.swift`,
  `modules/arrows-feedback/ios/ArrowsFeedbackModule.swift`.

### Context
No iOS build output exists anywhere (the facts above). The generated `ios/` tree is from 2026-09-01
and is stale relative to `app.json`. The build uses Expo's precompiled modules
(`EXPO_USE_PRECOMPILED_MODULES`), and W8-01 found a Swift 6.3.1-vs-6.3.3 message when consuming one
of them outside a build.

P-04c left one check open: its closing command evaluates Expo's device-family mod, but the
regenerated `project.pbxproj` and a built app's `UIDeviceFamily` are UNVERIFIED.
`node_modules/@expo/config-plugins/build/ios/RequiresFullScreen.js:55-66` adds
`UISupportedInterfaceOrientations~ipad` only when tablet support is on. Whether a `--clean` prebuild
then omits the key is INFERRED.

Read the SDK 57 docs (https://docs.expo.dev/versions/v57.0.0/) for `expo prebuild` before running it.
In particular, check whether it runs `pod install` itself.

### Change
1. `df -h /` ≥ 20 GB, and no Android native build is running (`pgrep -fl gradle` is empty). Record
   both.
2. `npx expo prebuild -p ios --clean`, then `cd ios && pod install` unless step 1's docs reading
   showed prebuild already did it.
3. Confirm the scheme with `xcodebuild -list -workspace ios/Arrows.xcworkspace`. Then run:
   `xcodebuild -workspace ios/Arrows.xcworkspace -scheme Arrows -configuration Debug -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone 16 Pro,OS=18.6' -derivedDataPath ios/build/DerivedData CODE_SIGNING_ALLOWED=NO build 2>&1 | tee <scratch>/build.log`
4. Fix only compile errors, only in the three Swift files. Never edit under `ios/`. If the failure
   is inside an Expo precompiled module, stop and escalate with the verbatim error and the SDK 57
   documented switch (if one exists). Do not work around it in generated files.
5. Write `docs/ios-build-log-<date>.md` with:
   - every command, `xcodebuild -version`, SDK version, `swiftc --version`;
   - `df` before and after, `du -sh ios/Pods ios/build/DerivedData`, wall time;
   - verbatim errors, and warnings from our three files;
   - whether the 6.3.1/6.3.3 message appeared.
   These turn "needs ≥ 20 GB" into a measured number.
6. Close P-04c:
   - `grep -n TARGETED_DEVICE_FAMILY ios/Arrows.xcodeproj/project.pbxproj`;
   - `plutil -p <built Arrows.app>/Info.plist | grep -A2 UIDeviceFamily`;
   - record whether `UISupportedInterfaceOrientations~ipad` is still present (observation, not a gate).
7. For W8-12, record `plutil -p ios/Arrows/PrivacyInfo.xcprivacy` and the built app's aggregated
   `PrivacyInfo.xcprivacy`, as observation only.
8. Prove regenerability: repeat steps 2–3 once from `--clean` (reusing DerivedData).

### Non-goals
The board-view refactor. Runtime checks (W8-05). Release builds (P-02c). Any change to Android,
`app.json` or the protocol.

### Acceptance criteria
- Both builds (step 3 and step 8) end with `** BUILD SUCCEEDED **` and exit 0, with the log lines
  quoted.
- The build log contains every field in step 5.
- `TARGETED_DEVICE_FAMILY = 1` and `UIDeviceFamily => [1]` are quoted verbatim.
- `git status --short` shows nothing beyond the build-log doc and, if needed, the three Swift files.

### Verification
1. PRE-FIX REPRODUCTION: `ls ~/Library/Developer/Xcode/DerivedData` → no such directory.
   `docs/IOS_BOARD_PLAN.md:113` says iOS was not compiled.
2. `xcodebuild` is the only check that runs our Swift through the compiler together with the real
   ExpoModulesCore DSL and the whole-app link. No jest or tsc run touches Swift. The second clean
   pass proves nothing depends on hand edits in the gitignored tree.
3. TDD: not applicable.
4. Runtime behaviour stays UNVERIFIED until W8-05.

### Risk and rollback
- `--clean` deletes `ios/`, including `ios/Pods` (1.2 GB). Git cannot restore it, but it is fully
  regenerable.
- Disk exhaustion mid-build: stop if `df` drops below 3 GB (**OWNER-PICKED STARTING VALUE**). Delete
  only `ios/build/DerivedData`, which this task created.
- Rollback: revert any Swift fix commit.

---

## W8-04 — (MOVED to P-02c)

- **Status:** CUT here, MOVED (integrator 2026-09-16). The iOS calibration (recorder rate, pixel floor, reduced-motion read-back, and the four Release fixture apps W8-06 reuses) is now **P-02c** in `P.md`, still BLOCKED-ON-DISK. W8 tasks that consumed "W8-04" now depend on P-02c.

---

## W8-05 — Simulator smoke: the board renders, exits slide the right way, the reduced-motion exit is recorded

- **Status:** BLOCKED-ON-DISK (needs ≥ 20 GB, as W8-03)
- **Effort:** S
- **Depends on:** W8-03, P-02c. W2-01 supplies the signed-off Android reduced-motion exit description
  used for comparison. The iOS captures may be taken before it lands and compared after.
- **Flag:** none
- **Files:** `docs/ios-smoke-<YYYY-MM-DD>/` (new: captures plus `README.md`). Only if a defect is
  found: `modules/arrows-board/ios/ArrowsBoardView.swift`.

### Context
`setExitAnimation` (`ArrowsBoardView.swift:79-207`) has never run. Render-only unknowns, none visible
to tsc, jest or xcodebuild:
1. **Dash direction.** The phase goes `patternSum → patternSum − totalLength` (`:167-168`), and
   Android uses `DashPathEffect(intervals, -travelled)` (`ArrowsBoardView.kt:281`). They are
   congruent modulo one period (INFERRED), but the sign convention is unobserved.
   `docs/IOS_BOARD_PLAN.md:115` says to flip it if the dash slides backwards.
2. **Curve.** Exact k² (`kt:269`) against a bezier approximation (`swift:164`); smoothstep against
   easeInEaseOut for the fade.
3. **Reduced motion (performance-16).** Under OS reduce motion, both renderers draw a stationary dash
   and head fading linearly in place (`kt:269-271`; `swift:168,177-178,191-195`). W2-01 gets the
   Android look signed off, and nobody has seen the iOS one.
4. **Group opacity.** Android sets alpha on trail and head separately, while iOS fades the container
   (INFERRED visible difference where they overlap).

The W8 draft author also executed a 10-line swiftc program showing that `CATransform3D` assigned to
`fromValue` boxes as `NSValue`, so the head translation should animate. Do not re-litigate that
unless the head fails to move.

`xcrun simctl ui` has no slow-animation option. Simulator.app's Debug ▸ Slow Animations slows Core
Animation (INFERRED: not Reanimated). It is used here only to read direction.

### Change
Using P-02b/P-02c captures on "iPhone 16 Pro" / iOS 18.6, plus one still on "iPhone 17 Pro" /
iOS 26.5:
- (a) Stills of index 0 at fit zoom in Daylight and Ink Night. The theme is persisted app state
  toggled on the menu (`App.tsx:45-50`), so use a non-PERF Debug build, or a P-02 `PERF_SCREEN=menu`
  build.
- (b) Recording of one exit, reduce motion off, at normal speed and again with Slow Animations on.
- (c) The same exit with reduce motion on, with `appReducedMotion: true` read back.
- (d) On index 0, 30 exits tapped by hand (tap positions noted), then a still.
- (e) Five taps on free arrows within one second, recorded, with frames extracted.
- (f) Android counterparts of (b) and (c) on the same level index and checksum. Use P-02
  `capture.mjs` at `--motion-scale 1` and `0` on `fleet_floor_api31`, one native build at a time.

Write `README.md` with a side-by-side frame strip for (b)/(f) and (c)/(f), and list every observed
difference. If the dash slides backwards, flip the phase delta at `ArrowsBoardView.swift:168` in its
own commit and re-record (b).

### Non-goals
Changing the curve approximation or the fade shape. If a mid-flight difference is visible, raise it
with the owner and a frame strip. Replacing the animations with a `CADisplayLink` is a separate
decision. Memory or perf numbers.

### Acceptance criteria
- (b): in consecutive frames, the dash moves from the arrow's tail toward and past its head, off the
  board edge, in the same direction as the Android clip. A third party can check this from the two
  strips.
- (b): the head translates along the same ray, and no head is visible after the trail's last visible
  frame.
- (c): dash and head stay in place and fade. Any difference from the Android scale-0 clip or from
  W2-01's signed-off description is listed.
- (d): the HUD reads `30 left` (60 − 30), and no arrow is drawn at any of the 30 tapped positions.
- (e): no extracted frame shows more than 2 trails.
- (a): every arrow drawn in both themes. Removal in (b) and (d) shows no static-layer arrow morphing
  or sliding. This is the pre-refactor reference for W8-08.
- The curve and group-opacity observations are recorded as frame strips with a sentence each. They
  have no threshold, and the owner looks if they differ visibly.

### Verification
1. PRE-FIX REPRODUCTION: not possible. The code has never run, and W8-03's log is the first build.
2. Recordings from the running simulator app are the only evidence for direction, stranding, slot
   cap and reduced-motion look. All are render-only, and this repo has a scar from a transform fix
   "verified" by tsc.
3. TDD: not applicable. A sign-flip fix closes only on the re-recorded (b).
4. UNVERIFIED-DEVICE: a 120 Hz ProMotion phone (`CADisableMinimumFrameDurationOnPhone` is `true` in
   the generated `Info.plist`) and real GPU compositing.

### Risk and rollback
A wrong-direction dash is a one-line fix plus a re-record. A visibly different curve is a taste call
for the owner. Rollback: revert the fix commit.

---

## W8-06 — Measure the `draw(_:)` memory and mask-rebuild cost before refactoring

- **Status:** BLOCKED-ON-DISK (needs ≥ 20 GB, as W8-03)
- **Effort:** S
- **Depends on:** W8-03, P-02c (the four Release apps)
- **Flag:** none (measurement only)
- **Files:** `docs/IOS_BOARD_PLAN.md` (replace the table at `:14-17` and the numbers in gates A1 and
  A5 at `:64-67`, `:77`)

### Context
`ArrowsBoardView` overrides `draw(_:)` (`ArrowsBoardView.swift:209-239`) on a view whose bounds are
the whole board (`cols × CELL` by `rows × CELL`, `CELL = 40` at `src/ui/BoardView.tsx:52`). Every
mask change calls `setNeedsDisplay()` (`:54-58`), which redraws every visible arrow.

`docs/IOS_BOARD_PLAN.md:14-17`'s "~88 MB / ~122 MB" is arithmetic (bounds × scale² × 4 bytes), not a
measurement. Gate A1's 16 MB / 20 MB derives from that arithmetic, and gate A5's "≤ 10 MB" has no
source.

The blocked tap is the hot path (performance-16):
- `excludedArrows` (`BoardView.tsx:646-651`) adds the shaking and hint arrows, so the visibility mask
  memo (`:663-666`) changes when a blocked tap starts and again when it ends;
- on iOS each change runs `updateVisibleArrows()` (`:260-265`, O(n)) and a full redraw;
- the memo depends on `arrowCount` because `board.arrows()` returns the live, spliced-in-place
  internal array, whose reference never changes.

A shape-layer refactor moves rasterization into the simulator's render-server process. An app-only
measurement could therefore show a false win (INFERRED), so both processes are measured.

### Change
1. For each of P-02c's apps (a)–(d) on "iPhone 16 Pro" / iOS 18.6:
   - launch, and screenshot the mounted board (or its absence);
   - take 3 samples 10 s apart (**OWNER-PICKED STARTING VALUE**) of `footprint $(pgrep -x Arrows)`
     and `vmmap --summary $(pgrep -x Arrows)`;
   - record total footprint and the largest region in each of CoreAnimation, IOSurface and CG raster
     data;
   - do the same for this simulator's render-server process. Identify it with `pgrep -fl` filtered
     by the simulator UDID path, and record its name (INFERRED `backboardd`).
2. Board-scaling check: the bitmap arithmetic predicts (45/39)² ≈ 1.33 between 935 and 3827.
   Record the observed ratio for each candidate region.
3. CPU, paired use only (the simulator uses the host CPU):
   - run `xcrun xctrace record --template 'Time Profiler' --attach $(pgrep -x Arrows) --time-limit 30s --output <scratch>/<label>.trace`
     on (a);
   - tap 10 free arrows by hand;
   - over 5 launches, tap 2 distinct blocked arrows per launch (10 blocked taps; relaunching resets
     hearts);
   - export and report main-thread time in `ArrowsBoardView` symbols (`setVisibleMask`,
     `updateVisibleArrows`, `draw`) per removal and per blocked tap, with the sample-count
     reconciliation (window × sampling rate) printed beside it.
4. Write the table into `docs/IOS_BOARD_PLAN.md` (tool, device, runtime, level index, checksum, app
   sha256). Mark A1 and A5's old numbers as superseded, with a pointer to this table.
5. Write W8-07's memory gate from the result, in this form: "the region(s) identified here as the
   board backing store are absent, and no region in either process grows between 39×39 and 45×45
   and disappears in the empty-board build." If a numeric bound is also wanted, derive it with the
   calculation shown next to it.

### Non-goals
Code changes. Device numbers. Frame-timing percentiles.

### Acceptance criteria
- A table for 4 apps × 2 processes × 3 samples with every field in step 1.
- A written verdict "board-sized backing store present: yes/no". Yes means a region that grows
  between the two board sizes and disappears in the empty-board build.
- If the verdict is no: the doc says W8-07's justification is void, and the owner is asked whether
  W8-07 still happens. W8-07 does not start.
- The CPU table has 10 removals and 10 blocked taps, each row with its sample reconciliation.

### Verification
1. PRE-FIX REPRODUCTION: this is the pre-fix measurement for W8-07. Without it, W8-07 cannot show it
   touched the broken path.
2. Mounted vs `PERF_EMPTY_BOARD` isolates the board view's own allocation. That flag is the existing
   control (`StaticBoardSurface.native.tsx:35-36`). Two board sizes separate board-scaled memory from
   fixed overhead, and measuring the render server closes the false-win path.
3. TDD: not applicable.
4. UNVERIFIED-DEVICE: device memory (a different GPU and IOSurface backing). Every number is
   labelled "simulator".

### Risk and rollback
Sample noise: 3 samples show the spread. Rollback: revert the doc.

---

## W8-07 — Replace `draw(_:)` with two retained `CAShapeLayer`s

- **Status:** BLOCKED-ON-DISK (needs ≥ 20 GB, as W8-03)
- **Effort:** M
- **Depends on:** W8-05, W8-06 (only if W8-06's verdict is "present")
- **Flag:** none. iOS has no shipped build, this is a single native path, and W8-08 can veto it.
  Rollback is `git revert` of one file.
- **Files:** `modules/arrows-board/ios/ArrowsBoardView.swift`, `docs/IOS_BOARD_PLAN.md` (status)

### Context
- Static art is drawn in `draw(_:)` (`ArrowsBoardView.swift:209-239`) into a board-sized bitmap
  (measured in W8-06).
- `contentMode = .redraw` is at `:44`.
- `setGeometry`, `setVisibleMask`, `setInk` and `setStrokeWidth` (`:47-73`) each call
  `setNeedsDisplay()`.
- The exit slots (`:125-160`) already use `CAShapeLayer` and are the model.
- Without `CATransaction.setDisableActions(true)`, `CAShapeLayer` implicitly animates `path` for
  0.25 s, and a removed arrow morphs instead of vanishing (`docs/IOS_BOARD_PLAN.md:49-51`).
- After this change, each mask change rebuilds two `CGMutablePath`s on the main thread. A blocked tap
  does this twice (W8-06).
- Sequencing (evidence-15): any other task that edits this file's exit rendering must rebase on this
  one. The one draft that did (W2 draft 2's combo trail width) is cut.

### Change
1. In `init(appContext:)`, create `staticShaft: CAShapeLayer` (`fillColor = nil`,
   `lineCap = .round`, `lineJoin = .round`) and `staticHead: CAShapeLayer` (fill only). Insert them
   at sublayer indices 0 and 1 so exit containers (`layer.addSublayer`, `:156`) stay above. Delete
   `contentMode = .redraw` (`:44`).
2. Override `layoutSubviews()` to call `super`, then set both frames to `bounds`.
3. Move path assembly into `updateVisibleArrows()`: one `CGMutablePath` of visible shafts and one of
   visible heads (`addPath` per arrow), assigned to the two layers.
4. `setInk` and `setStrokeWidth` set `strokeColor` / `fillColor` / `lineWidth` on the static layers.
   Remove every `setNeedsDisplay()` and the `draw(_:)` override.
5. Wrap every assignment to the static layers' `path`, `strokeColor`, `fillColor`, `lineWidth` and
   `frame` in `CATransaction.begin()` / `setDisableActions(true)` / `commit()`.
6. Leave `shouldRasterize` at its default everywhere.
7. If W8-08 finds strokes soft at max zoom, check first whether the standalone layers' `contentsScale`
   (default 1.0 for non-view-backed layers) matters for `CAShapeLayer`. That is INFERRED; set it to the
   window screen scale only if a capture shows a difference.

### Non-goals
`ArrowsBoardModule.swift`, TS bindings, the protocol, exit slots, Android, and any visual change.

### Acceptance criteria
- `grep -n "override func draw\|contentMode\|setNeedsDisplay" modules/arrows-board/ios/ArrowsBoardView.swift`
  → no output.
- `grep -n shouldRasterize` → no output.
- Every assignment in Change step 5 sits inside a disable-actions block (readable in the diff). The
  behaviour is proven by W8-08's removal criterion.
- W8-06's procedure re-run unchanged on after-builds of (a)–(d): the gate W8-06 wrote holds in both
  processes and at both board sizes, with before/after totals reported side by side.
- W8-06's CPU procedure re-run: main-thread time per removal and per blocked tap is reported
  before/after with reconciliations. There is no pass threshold. An after value outside the before
  run's observed range goes to the owner with both tables.
- W8-05's (b), (d) and (e) criteria pass on the after-build.
- `git diff --stat` shows only `ArrowsBoardView.swift` and `docs/IOS_BOARD_PLAN.md`.

### Verification
1. PRE-FIX REPRODUCTION: W8-06's table on the base build (the backing-store region present).
2. The identical probe on the identical fixtures shows that region gone, and the render-server column
   shows it did not reappear there. Compilation alone is not evidence.
3. TDD: not applicable (native render path with no test harness). The visual closure is W8-08.
4. UNVERIFIED-DEVICE: render-server rasterization cost per pan/pinch frame on real hardware (W8-10,
   W8-11).

### Risk and rollback
- Per-frame shape rasterization during pan/pinch is unmeasured on hardware. The fallback from
  `docs/IOS_BOARD_PLAN.md:120-123` is to restore `draw(_:)` with a capped `layer.contentsScale`, which
  accepts softer strokes at max zoom and is an owner decision.
- Softer anti-aliasing is judged in W8-08.
- Rollback: `git revert`.

---

## W8-08 — Visual parity gate for W8-07 (this task can veto it)

- **Status:** BLOCKED-ON-DISK (needs ≥ 20 GB, as W8-03)
- **Effort:** S
- **Depends on:** W8-07, P-02c
- **Flag:** none (verification)
- **Files:** `docs/ios-parity-<YYYY-MM-DD>/` (new)

### Context
Two regressions are invisible to every headless check:
- a missed `setDisableActions(true)` makes a removed arrow morph for about 0.25 s;
- `CAShapeLayer` anti-aliasing may blur strokes at max zoom where Core Graphics was crisp
  (`docs/IOS_BOARD_PLAN.md:74-76`, gate A4).

Also, `setInk` stops calling `setNeedsDisplay()`, so a stale ink colour is plausible. Max zoom is
clamped at 3.5× fit or 64 pt per cell (`BoardView.tsx:55-58`). The theme toggle lives on the menu
(`App.tsx:45-50`), so an ink change may never reach a mounted board. Find out by reading
`App.tsx`/`GameScreen.tsx`, and record the answer.

### Change
Builds of the commit before and the commit after W8-07, on "iPhone 16 Pro" / iOS 18.6:
- index 3827 at fit, and pinched by hand to the zoom clamp (Option-drag in Simulator.app);
- index 0 at fit in Daylight and Ink Night;
- a Slow Animations recording of one removal on 3827, frames extracted;
- every theme change the app allows while a board is mounted, if one exists.

For each before/after still pair, produce a difference image with `pixel-diff.mjs`, plus the changed
fraction for the board region, shown next to P-02c's floor.

### Non-goals
Fixing anything other than a missing disable-actions wrapper or `contentsScale`. Any other change is
a W8-07 revision.

### Acceptance criteria
- Fit zoom: the difference image shows changes only along stroke edges. No difference blob has the
  shape of a whole shaft or head. A third party can check this from the attached image.
- Max zoom: shaft width in pixels, measured across the same 5 shafts (**OWNER-PICKED STARTING VALUE**)
  before and after, is reported. Softness is judged by the owner from the pair, not by the agent.
- Removal: no extracted frame shows the removed arrow partly present, shrunk, bent or displaced.
- Ink: a pixel sampled at a shaft centre in each theme matches that theme's ink token in
  `src/ui/theme.ts` (sampled hex and token both recorded). If ink can change on a mounted board, the
  same holds after the change.
- Veto: any failed criterion means W8-07 is not kept, and the owner decides the max-zoom softness
  question from the screenshots.

### Verification
1. PRE-FIX REPRODUCTION: the "before" captures are the reference, from the commit immediately before
   W8-07.
2. Paired captures from builds of the two adjacent commits, on the same device, runtime, level and
   checksum. Both regressions are purely visual and produce no error, log or failing test.
3. TDD: not applicable.
4. UNVERIFIED-DEVICE: anti-aliasing on a physical Retina panel.

### Risk and rollback
Owner taste is required at max zoom. Rollback: revert W8-07.

---

## W8-09 — Feedback module smoke: audio bundle resolution, clip loading and lifecycle

- **Status:** BLOCKED-ON-DISK (needs ≥ 20 GB, as W8-03)
- **Effort:** S
- **Depends on:** W8-03, P-02b
- **Flag:** none
- **Files:** `modules/arrows-feedback/ios/ArrowsFeedbackModule.swift` (a `#if DEBUG` log line, and a
  fix only if a defect is found), `docs/ios-smoke-<YYYY-MM-DD>/feedback.md` (new)

### Context
`ArrowsFeedbackModule.swift` changed in `ec03073` (eight pitch-stepped pops) and has never been
compiled. It loads 11 clips (`:17-19`: `pop0`…`pop7`, `fail`, `win`, `star`) from a bundle named
`ArrowsFeedback`. The lookup searches `Bundle(for: ArrowsFeedbackModule.self)` and then `Bundle.main`
(`feedbackResourceBundle()`), matching `resource_bundles` at `ArrowsFeedback.podspec:21-23`.

A nil bundle fails silently:
- `makePlayer` returns nil, and `audioIsPrepared` is still set true, so no retry happens;
- `package.json:58-62` excludes `expo-audio` and `expo-haptics` from Apple autolinking;
- `src/ui/feedback.ios.ts:27-44` falls back only when the native module is entirely absent.

A simulator video from `simctl io recordVideo` has no audio track (INFERRED), so the agent cannot
hear it. Evidence has to come from the bundle, a debug log, and an owner's ears.

### Change
1. Static: `find <built Arrows.app> -name ArrowsFeedback.bundle -exec ls {} \;`.
2. Add inside `prepareAudioIfNeeded()`:
   `#if DEBUG` `NSLog("[ArrowsFeedback] bundle=%@ clips=%d/%d", …)` `#endif`. It is compiled out of
   Release.
3. On a non-PERF Debug build, stream `xcrun simctl spawn <udid> log stream --predicate 'eventMessage CONTAINS "[ArrowsFeedback]"'`
   while you:
   - make 8 exits within the combo window;
   - make one blocked tap;
   - clear index 0;
   - go menu ↔ game 5 times, taking `footprint $(pgrep -x Arrows)` after each return to the menu.
4. The owner listens to one run of step 3 on the Mac speakers: 8 pops rising in pitch, the fail clip,
   the win and star clips.

### Non-goals
Haptics (simulators produce none). Changing clips, pitch or the combo ladder (W2-12 asks the owner
whether the ladder stays).

### Acceptance criteria
- The bundle listing shows exactly the 11 `.wav` files.
- The log shows `clips=11/11` with a non-nil bundle on each of the 5 mounts.
- The 5 footprint values are reported. Growth on every cycle goes to the owner, with no threshold.
- The owner confirms audibility in writing. Until then, this line is UNVERIFIED, not claimed.

### Verification
1. PRE-FIX REPRODUCTION: none possible (never compiled or run). The silent-nil path is shown by
   reading `makePlayer`/`feedbackResourceBundle`.
2. The bundle listing proves packaging, and the debug log proves runtime resolution. Neither can be
   inferred from compilation, because a nil bundle produces no error.
3. TDD: not applicable.
4. UNVERIFIED-DEVICE: haptics for exit, blocked and cleared (`performHaptic`), and audio routing on a
   phone with the silent switch (the session category is `.ambient`).

### Risk and rollback
If the bundle does not resolve, the fix is local to the podspec path or the search order, followed by
a re-run of steps 1–3. Rollback: revert.

---

## W8-10 — iOS perf measurement harness, or a written downgrade of every iOS perf claim

- **Status:** BLOCKED-ON-OWNER. The owner decides iOS will ship (starts tranche 2). Until then the
  harness would only measure a simulator on the host GPU (skeptical-14). It also needs ≥ 20 GB disk.
- **Effort:** L
- **Depends on:** W8-07, W8-08, P-02b
- **Flag:** none (tooling; ships in no build)
- **Files:** `scripts/perf/ios/` (new workload, runner, parser, parser tests), `docs/perf-harness.md`.
  Reference only: `scripts/perf/android/ArrowsWorkload.java`, `scripts/perf/android/soak-plan.ts`,
  `scripts/perf/android/parse-gfxinfo.mjs`.

### Context
No iOS perf driver exists: no XCUITest target, no tap tool (`idb` absent), and jest is node-only. The
Android soak is `ArrowsWorkload.java` driven by `soak-plan.ts`. `ios/` is gitignored and regenerated,
so a UI-test target added inside it vanishes on the next prebuild. It must be injected by a config
plugin, or live in a separate Xcode project under `scripts/perf/ios/` that launches the app by bundle
id (INFERRED feasible; prove it).

The generated `Info.plist` sets `CADisableMinimumFrameDurationOnPhone = true`. On a ProMotion device
the refresh rate varies, so "window × refresh" needs the per-interval rate that the trace reports.

### Change
1. Port the tap/pan/pinch sequence and level indices of `soak-plan.ts` to XCUITest, run it under
   `xcrun xctrace record --template 'Animation Hitches'`, and parse the export the way
   `parse-gfxinfo.mjs` parses gfxinfo, with `node:test` fixtures.
2. Print every percentile next to its reconciliation: frames observed vs Σ(interval × reported
   refresh rate). A percentile whose reconciliation is missing is not printed.
3. Calibrate before quoting: run the unchanged build 5 times (**OWNER-PICKED STARTING VALUE**),
   shuffled with the `PERF_EMPTY_BOARD` build. Publish the spread. That spread is the instrument's
   resolution, and a difference smaller than it is reported as "within instrument resolution".
4. Config matrix: mounted vs empty board. An inversion (empty slower) counts only if it replicates
   with non-overlapping ranges across the shuffled runs. A replicated inversion is parked, disclosed,
   and checked against every headline number.
5. **Honest alternative** if the port is judged not worth it: a hand-played 20-level soak, recorded
   and reported only as a bounded observation ("no visible stutter across a 20-level hand-played
   soak, recording attached"), with the written statement "no percentile exists for iOS".

### Non-goals
Device runs (W8-11). Any number quoted as device performance.

### Acceptance criteria
- Parser tests pass on fixture exports.
- The report shows the soak visiting the same level indices as `soak-plan.ts`.
- Every percentile carries its reconciliation line.
- The calibration table (5 shuffled pairs, raw values, spread) is published before any comparison.
- If the alternative was taken: the recording is attached, the "no percentile exists" statement is
  present, and no percentile appears anywhere.

### Verification
1. PRE-FIX REPRODUCTION: `ls scripts/perf/ios` has no workload. No iOS percentile exists anywhere in
   `docs/`.
2. Repeat runs on identical code show whether the harness measures anything. The empty-board matrix
   is the config check. Reconciliation catches a sampler that silently dropped frames, which is how
   four merge-kit numbers went wrong.
3. TDD: parser fixtures first.
4. UNVERIFIED-DEVICE: every simulator number measures the Mac, not a phone.

### Risk and rollback
Instruments automation breaks across Xcode versions: record the Xcode build in every result.
Rollback: standalone scripts.

---

## W8-11 — Physical-iPhone perf run with gates derived from that device

- **Status:** BLOCKED-ON-DEVICE. No iPhone exists for this project. Buying or borrowing one is an
  owner spending decision. The plan's escalation recommends a used iPhone SE (3rd generation) or
  iPhone 11 as the oldest realistic target, and the owner has not ruled.
- **Effort:** M (once a device exists)
- **Depends on:** W8-10 (harness, not the honest alternative), W8-07
- **Flag:** none
- **Files:** `docs/performance-optimization-knowledge-base.html`, `docs/IOS_BOARD_PLAN.md` (replace A2,
  A3, A5), `scripts/perf/ios/`

### Context
`docs/IOS_BOARD_PLAN.md:68-71` copies Android's "p95 ≤ 20 ms, p99 ≤ 34 ms, hitch ratio ≤ 10%".
Those are 60 Hz budgets. At 120 Hz a 20 ms p95 is 2.4 frames, lax enough to pass visible stutter.
Instruments' hitch ratio (ms of hitch per s) is also not gfxinfo's jank percentage. A3's 34 ms and
A5's "≤ 10 MB over 20 levels" have no source. Every perf number this project owns comes from one
API 31 emulator.

### Change
1. Before the run, write down: device model, iOS version, the refresh rate the device reports, the
   trace's timing resolution, and the gates in this form: p95 ≤ 1 frame period and p99 ≤ 2 frame
   periods at that reported rate (**OWNER-PICKED STARTING VALUE**, form and multipliers).
2. Run the W8-10 soak twice, and record thermal state before and after.
3. Report the footprint trend over the 20-level soak as absolute MB with the window stated. It
   replaces A5, together with an owner judgement or a bound derived from W8-06.
4. Felt haptics for exit, blocked and cleared, which closes W8-09's gap.

### Non-goals
Simulator numbers. Android comparisons beyond placing the numbers side by side.

### Acceptance criteria
- The report contains every field in step 1, both runs with reconciliations, and the footprint trend.
- A shortfall smaller than the trace's resolution is reported as "within instrument resolution".
- A2, A3 and A5 in `docs/IOS_BOARD_PLAN.md` are replaced by the device-derived text.
- The haptics check is recorded by the person holding the device.

### Verification
1. PRE-FIX REPRODUCTION: `docs/IOS_BOARD_PLAN.md:68-77` shows the copied and unsourced gates.
2. An xctrace export from the physical iPhone, because the simulator runs on the Mac's CPU and GPU and
   measures the wrong hardware.
3. TDD: not applicable.
4. This task is the device verification. Until it runs, no release note may call iOS perf-validated.

### Risk and rollback
One device is one sample of hardware, so state it. Rollback: revert doc edits.

---

## W8-12 — ATT, SKAdNetwork and a truthful privacy manifest, all from config

- **Status:** BLOCKED-ON-OWNER. The owner must:
  1. start tranche 2;
  2. rule how iOS is monetised: ads as on Android, premium, or free with a remove-ads IAP. Premium
     deletes this task and W8-13;
  3. approve the `NSUserTrackingUsageDescription` copy and the moment the ATT prompt appears;
  4. complete the W7 consent work: W7-03 (CMP integration after W7-02's GATE 0, with the owner's
     vendor account), because ATT comes after the GDPR step;
  5. complete W7-12 (demand sources), because the SKAdNetwork list must match the networks actually
     enabled.
- **Effort:** M
- **Depends on:** W8-03, W7-01, W7-03, W7-12 (P-01 not needed: no key)
- **Absorbs:** W7-14 (integrator 2026-09-16; W7-14 was a duplicate spec of this task, with the same owner gate)
- **Flag:** `IOS_ATT_PROMPT_ENABLED` (new, default OFF) gates the prompt call. The plist and manifest
  entries are declarations with no player-visible effect.
- **Files:** `app.json`, `plugins/withIosAds.js` (new, only if `app.json` config cannot express it),
  `src/ui/ads.tsx`, `src/ui/attPrompt.ts` (new), `src/ui/__tests__/attPrompt.test.ts` (new)

### Context
EXECUTED on the stale generated tree:
- `ios/Arrows/Info.plist` has no `NSUserTrackingUsageDescription` and no `SKAdNetworkItems`;
- `ios/Arrows/PrivacyInfo.xcprivacy` declares `NSPrivacyTracking => false` and an empty
  `NSPrivacyCollectedDataTypes`;
- `ios/Podfile.lock:87-91` links IronSourceSDK 9.4.0.0 and IronSourceAdQualitySDK.

The manifest is therefore false for any iOS build.

`plugins/withLevelPlay.js` is Android-only (its idempotency `MARKER` is at `:15`). `ios/` is
gitignored, so hand edits vanish on prebuild.

No new dependency is needed (W7-17). `unity-levelplay-mediation` 9.2.0 exports `ATTrackingManager`
with `getTrackingAuthorizationStatus()` and `requestTrackingAuthorization()`
(`node_modules/unity-levelplay-mediation/lib/typescript/src/utils/ATTrackingManager.d.ts`).

`apple.privacyManifestAggregationEnabled` is on, so the manifest that matters is the one inside the
built `.app`.

ATT is not GDPR consent, so consent comes first. The ordering is consent → ATT → `initAds`.

### Change
1. Read the SDK 57 docs for `ios.infoPlist` and `ios.privacyManifests` in `app.json`. Use them if
   they can express the entries. Otherwise write `plugins/withIosAds.js`, idempotent in the
   `withLevelPlay.js` style.
2. Declare `NSUserTrackingUsageDescription` (owner copy) and `SKAdNetworkItems` (the enabled
   networks' published identifiers, with a dated source comment).
3. Declare `NSPrivacyTracking = true`, the ad SDK's tracking domains, and collected data types that
   match what the linked SDKs do.
4. `src/ui/attPrompt.ts`: an iOS-only function using LevelPlay's `ATTrackingManager`, called after
   consent resolves and before `initAds`, at the owner-picked moment, and only when
   `IOS_ATT_PROMPT_ENABLED` is true. A denied or restricted status leaves the game fully playable.
5. `git diff` touches no Android file.

### Non-goals
The CMP itself (W7). A pre-prompt screen of our own. Any Android change.

### Acceptance criteria
- After `npx expo prebuild -p ios --clean`, `plutil -p` shows both Info.plist keys and a non-empty
  SKAdNetwork array. Two clean prebuilds produce byte-identical `Info.plist` and
  `PrivacyInfo.xcprivacy` (`shasum`).
- `plutil -p <built Arrows.app>/PrivacyInfo.xcprivacy` shows `NSPrivacyTracking => true` and non-empty
  collected data types.
- `attPrompt.test.ts` (written first; fake LevelPlay and fake consent) passes:
  - the call order is consent-resolved → ATT request → init;
  - with the flag OFF, ATT is never requested;
  - on Android, ATT is never requested;
  - a denied status still calls init.
- Simulator recordings: flag OFF, no ATT prompt from first launch through the first clear. Flag ON,
  exactly one prompt, at the owner-picked moment.
- A third party comparing `ios/Podfile.lock` with the built manifest finds no linked ad SDK left
  undeclared.

### Verification
1. PRE-FIX REPRODUCTION: `plutil -p ios/Arrows/PrivacyInfo.xcprivacy` on a fresh `--clean` prebuild
   of the base commit shows `NSPrivacyTracking => false`.
2. The regenerated and built files are what App Review reads. Checked-in source proves nothing,
   because the failure mode is generated output diverging from source.
3. TDD: `attPrompt.test.ts` first.
4. UNVERIFIED-DEVICE: SKAdNetwork attribution and the IDFA reaching mediation.

### Risk and rollback
A rejected ATT string costs an App Review round. A stale SKAdNetwork list silently loses attribution.
Rollback: flag OFF for the prompt, and revert the config.

---

## W8-13 — Per-platform LevelPlay app key and ad unit ids

- **Status:** BLOCKED-ON-OWNER. The owner must start tranche 2, rule iOS monetisation (as in W8-12),
  create the iOS app entry for `com.danteb.arrows` in the LevelPlay dashboard, and hand over the iOS
  app key plus the interstitial and rewarded ad unit ids.
- **Effort:** S
- **Depends on:** W0-01, W0-02, W8-12
- **Flag:** none (configuration)
- **Files:** `src/ui/ads.tsx`, `src/ui/adKeys.ts` (new, a pure module so jest can reach it; `.tsx`
  is outside the node jest project), `src/ui/__tests__/adKeys.test.ts` (new)

### Context
`src/ui/ads.tsx:32-34` hardcodes `APP_KEY = '27012eed5'`, `INTERSTITIAL_AD_UNIT = 'tme2lh9p1pkvk1bl'`
and `REWARDED_AD_UNIT = 'smim4g4z79173hcw'` with no platform switch (the comment at `:29-31` calls
them the Android build's). LevelPlay issues keys per platform app entry, so the Android key on iOS
fails init.

After W0-01 the simulated TEST AD modal is `__DEV__`-only. After W0-02 a failed init retries and
rewarded buttons show an honest "no ad available" state without granting anything. So the iOS
failure is now "no ads, repeated init attempts", not fake ads. That is still a silent revenue loss.

### Change
Move the three ids into `adKeys.ts` as `Platform.select`-style data keyed by platform, with the iOS
values from the owner (never guessed). `ads.tsx` imports them.

### Non-goals
Pacing, placements, and consent.

### Acceptance criteria
- `adKeys.test.ts` (written first): the iOS branch never returns any of the three Android strings, and
  the Android branch returns exactly them.
- An iOS Debug build logs `[ads] LevelPlay init success` (`ads.tsx:176`) and not
  `[ads] LevelPlay init failed` (`:180`).
- Whether LevelPlay serves test creatives on a simulator is unknown (INFERRED). If it does, a
  recording of one interstitial and one rewarded; if not, UNVERIFIED-DEVICE.

### Verification
1. PRE-FIX REPRODUCTION: on an iOS Debug build of the base commit, the console shows the init-failed
   line.
2. The init log comes from the SDK's own callback with the real key, which is the path that fails
   today.
3. TDD: `adKeys.test.ts` first.
4. UNVERIFIED-DEVICE: live fill on a phone.

### Risk and rollback
A wrong iOS id reproduces the failure, and the init log catches it. Rollback: revert.

---

## W8-14 — iOS inherits the consent solution through one shared code path

- **Status:** BLOCKED-ON-OWNER. The owner must start tranche 2, and complete the W7 consent work's
  owner steps (the CMP vendor account after the GATE 0 spike).
- **Effort:** S (on top of a working Android consent path)
- **Depends on:** W7-01, W7-03, W8-12
- **Flag:** inherits W7's consent flag; no new flag
- **Files:** `src/ui/ads.tsx`, `src/ui/consent.ts` (created by W7-01)

### Context
Today no consent or privacy call is made before LevelPlay init. The only LevelPlay call besides init
is `setMetaData('is_test_suite', …)` (`ads.tsx:165`). ATT (W8-12) is not a GDPR lawful basis.
Shipping a second store with the same gap doubles the exposure.

### Change
Route iOS through the exact consent resolution and LevelPlay privacy calls W7 builds for Android, from
one code path in `ads.tsx` with no `Platform.OS` branch around consent. Do not build an iOS-only flow.

### Non-goals
Choosing the CMP. Consent UI. Numeric targets (W7 owns them).

### Acceptance criteria
- `grep -n` shows each LevelPlay privacy call at exactly one call site, with no platform branch around
  it.
- A `__DEV__` log trace on iOS and on Android shows consent resolved strictly before the LevelPlay
  init call.

### Verification
1. PRE-FIX REPRODUCTION: `grep -rn "setConsent\|consent" src/` on the base commit finds no consent
   call.
2. Ordering is the failure mode (a consent call after init looks right in a diff and does nothing), so
   the evidence is the runtime trace on both platforms.
3. TDD: extend W7's consent-order test with an iOS platform case.
4. UNVERIFIED-DEVICE: CMP rendering on a phone.

### Risk and rollback
iOS shipping before the CMP doubles regulatory exposure, so this blocks W8-15's submission. Rollback:
revert.

---

## W8-15 — EAS iOS build and submit pipeline

- **Status:** BLOCKED-ON-OWNER. The owner must:
  1. start tranche 2;
  2. enrol in the Apple Developer Program;
  3. create the App Store Connect app record for `com.danteb.arrows`;
  4. complete P-04b (`eas init` under their account);
  5. let EAS manage iOS signing credentials under their account;
  6. answer the export-compliance question for the app (a legal declaration);
  7. say yes before any cloud build or submit.
- **Effort:** M
- **Depends on:** P-04b, P-04c, W8-12, W8-13, W8-14, W8-16
- **Flag:** none
- **Files:** `eas.json`, `app.json`, `RELEASE.md`

### Context
- `eas.json` has only `android` blocks: `preview.android` at `:13-15`, `production.android` at
  `:19-21`, and `submit.production.android` at `:26-28`. `development` has no platform block.
- `appVersionSource: "remote"` (`:4`) and `autoIncrement: true` (`:18`) apply to both platforms.
- The generated `Info.plist` has no `ITSAppUsesNonExemptEncryption`, so every upload stops at the
  export-compliance question.
- `eas-cli` is not installed; `npx` fetches it.

### Change
1. Add `ios` blocks where the profiles need them, and `submit.production.ios`.
2. Set no `ios.buildNumber` in `app.json` under a remote version source.
3. Add `ios.infoPlist.ITSAppUsesNonExemptEncryption` with the owner's answer.
4. Write the iOS half of `RELEASE.md`.
5. After the owner's yes: `npx eas-cli build -p ios --profile production --non-interactive`, then
   `npx eas-cli submit -p ios --profile production`.

### Non-goals
Submitting for review (only TestFlight processing). Store listing, screenshots or iPad.

### Acceptance criteria
- `npx expo config --type introspect` shows `ITSAppUsesNonExemptEncryption` with the owner's value.
- After the yes: the production iOS build reaches a signed `.ipa` non-interactively (build id
  recorded), and submit reaches TestFlight processing without an export-compliance prompt.
- `npx eas-cli build -p android --profile production --non-interactive` still reaches the queued state
  without a prompt. This is the regression check, because `eas.json` is shared.

### Verification
1. PRE-FIX REPRODUCTION: `grep -c '"ios"' eas.json` → 0.
2. Only a real EAS build and submit exercise signing, provisioning and export compliance. A local
   `xcodebuild` does not.
3. TDD: not applicable.
4. UNVERIFIED-DEVICE: TestFlight install on a phone (W8-11's device).

### Risk and rollback
Apple enrolment can take days. Shared `eas.json` could disturb Android, which the Android regression
check guards. Rollback: revert config.

---

## W8-16 — Close the iOS checks other workstreams deferred, on an iOS Release build, before any submission

- **Status:** BLOCKED-ON-OWNER. The owner must start tranche 2. The task also needs ≥ 20 GB disk.
- **Effort:** M
- **Depends on:** P-02c, W8-08, W8-09, and each listed task once landed
- **Flag:** tested with each feature's flag in its shipped default, and ON for any flag the owner has
  accepted
- **Files:** `docs/ios-presubmit-<YYYY-MM-DD>/` (new)

### Context
Tasks in other workstreams close on Android and web evidence and mark iOS BLOCKED-ON-DISK:
- `W0.md:116-117` (W0-01 TEST AD strip);
- `:379-380` (W0-03 boot backstop);
- `:542-543` (W0-04 reduce-motion static feedback);
- `:788-789` (W0-06 contrast);
- `:851` (W0-07 composed release);
- `W1.md:218` (W1-04 tutorial routing);
- `W2.md:87` (W2-01 reduce-motion census; the Swift mirror is read, not run) and `:416` (W2-06
  post-clear hold);
- `W4.md:668-669` (W4-06 daily entry), `:922` (W4-09 gallery) and `:1050`, `:1084` (W4-11 store-review
  prompt).

Shared TypeScript does not prove iOS behaviour: AppState, Reanimated's reduce-motion source, the
native exit renderer and the store-review API all differ per platform. Nobody owns these checks
today, and an iOS submission without them ships unverified fixes.

### Change
For each listed task that has landed, re-run its own acceptance scenario on an iOS Release simulator
build using P-02b/P-02c captures. Record one row per task: scenario, capture, result, and anything
only a device can show. Examples: grep the `.app`'s `main.jsbundle` for `TEST AD`; background the app
during the splash for W0-03; enable reduce motion for W0-04/W2-01; trigger a perfect clear for W4-11.

### Non-goals
Fixing iOS-specific failures inside this task. Each one goes back to its owning task as a defect with
the capture. Android re-verification.

### Acceptance criteria
- A table with one row per landed task above, each with a capture path and PASS, FAIL or
  UNVERIFIED-DEVICE.
- Zero FAIL rows before W8-15's submit, or an owner-signed exception per row.

### Verification
1. PRE-FIX REPRODUCTION: the cited lines show each check deferred.
2. Each row re-runs that task's original failure scenario on the iOS build, which is the only evidence
   that the shared fix holds on the second renderer and lifecycle.
3. TDD: not applicable.
4. UNVERIFIED-DEVICE: listed per row (for example W4-11's quota-limited prompt, haptics).

### Risk and rollback
It can surface several defects at once, so budget for it. Rollback: documentation only.

---

## W8-17 — Document the iOS/Android save-backup asymmetry

- **Status:** BLOCKED-ON-OWNER. It is part of the deferred tranche under the owner-approved plan; the
  owner starts tranche 2.
- **Effort:** S
- **Depends on:** none
- **Flag:** none (documentation)
- **Files:** `RELEASE.md`

### Context
- The generated `android/app/src/main/AndroidManifest.xml:15` declares `android:allowBackup="true"`
  with no `fullBackupContent` or `dataExtractionRules` (EXECUTED grep on the generated tree; `android/`
  is regenerated from Expo defaults).
- The save store is AsyncStorage (`src/ui/storage.ts:1`).
- Android Auto Backup can restore saves on reinstall. On iOS, app data returns only with a full device
  restore from backup, not on delete and reinstall (INFERRED from platform behaviour; confirm against
  Apple and AsyncStorage documentation before writing).
- No cross-platform transfer exists. An iOS launch creates an "I lost my streak" support surface.

### Change
Write the asymmetry into `RELEASE.md`, naming the manifest attribute and the confirmed iOS behaviour
with sources.

### Non-goals
Configuring backup rules. Cloud save. Any `SaveSystem` or `PersistenceKeys` change. W8 registers no key
and needs no migration, and the P-01 registry is the only place keys are added.

### Acceptance criteria
`RELEASE.md` names `android:allowBackup="true"`, states the iOS behaviour with a cited source, and says
cross-platform transfer does not exist.

### Verification
1. PRE-FIX REPRODUCTION: `grep -n -i "auto backup\|allowBackup" RELEASE.md` → no output (the only
   "backup" hit, `:44`, is about signing credentials).
2. The attribute is read from the regenerated manifest (re-run `npx expo prebuild -p android --clean --no-install`
   first if disk allows), because config defaults have misled this project before.
3. TDD: not applicable.
4. UNVERIFIED-DEVICE: an actual restore on either platform.

### Risk and rollback
None beyond accuracy. Rollback: revert.

---

## CUT and moved (nothing silently disappears)

- **W8-02 and W8-04 (iOS capture script and calibration).** MOVED by the integrator (2026-09-16) to P-02b and P-02c: the capture instrument has one owner (P-02). W6-17 (the same iOS capture path) was CUT for the same reason.
- **W7-14 (iOS ATT, SKAdNetwork, privacy manifest).** MERGED by the integrator into W8-12.

- **d W8-11 — Decide iPad support.** Moved: the controller ruled `ios.supportsTablet: false`, and
  P-04c makes the change. Its regenerated-project proof is W8-03 step 6. Option (b) (iPad screenshots
  and a landscape layout) is cut until the owner schedules iPad.
- **d W8-12 — the `eas init` / `extra.eas.projectId` half.** Moved to P-04b.
- **d W8-04 memory gates "≤ 20% of baseline delta" and "≤ 25% of largest region".** Cut (brand-13,
  evidence-15, skeptical-14): invented fractions written before the baseline existed. Replaced by
  W8-06's measured, qualitative scaling gate.
- **d W8-07 "two runs within 15%" and "frame reconciliation within 5%".** Cut (same three red teams).
  Replaced by W8-10's measured run-to-run spread and a mandatory printed reconciliation.
- **`docs/IOS_BOARD_PLAN.md` gates A1 (16 MB / 20 MB), A2 (Android 60 Hz budgets), A3 (34 ms), A5
  (≤ 10 MB).** Superseded by W8-06 (A1, A5 memory) and W8-11 (A2, A3, A5 trend).
- **d W8-09's `expo-tracking-transparency` dependency.** Cut: `unity-levelplay-mediation` 9.2.0
  already exports `ATTrackingManager` (W7-17, verified in `node_modules`).
- **d W8-02's `xcrun simctl ui booted` slow animations.** Cut: no such option exists (EXECUTED).
  Replaced by Simulator.app Debug ▸ Slow Animations.
- **d W8-01's "free disk to ≥ 25 GB".** Replaced by the ≥ 20 GB gate, with actual consumption measured
  in W8-03's build log.
- **W6-14's dependency on W6-13 and "do not start before gates A1–A5 pass".** Cut: the capture
  instrument has to exist before the smoke and parity gates that consume it.
- **d W8-10's rationale "an iOS release would show fake ads".** Obsolete after W0-01/W0-02. The task
  remains, with the corrected rationale.
- **W2 draft 2 combo trail-width escalation (which edited the Swift exit width).** Already cut by
  controller ruling. It is listed here because evidence-15 flagged its overlap with the draft W8-04 refactor (now W8-07).
