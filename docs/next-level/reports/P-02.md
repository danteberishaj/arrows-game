# P-02 report: one capture instrument (boot any screen, motion read-back, record, prove rendered)

- Task: P-02 (brief `.superpowers/sdd/TASKS/task-P-02-brief.md` plus its controller amendments F01, F02, F03, F23, F29, F34).
- Branch `next-level`. Base `a8fb42147a008c9d8588f83381b18c194abc951a`, clean tree at start (EXECUTED `git status --porcelain` empty).
- Device: `emulator-5556` / AVD `fleet_floor_api31` / API 31. Booted for this task without a wipe and shut down at the end.
- Evidence root: `artifacts/P-02/` (gitignored), plus `artifacts/captures/P-02-*`.

## Status summary

All five stages are built and committed, one commit per stage. The previously missing A1 fresh-Gradle
leg was run on `emulator-5556` and passed in the Codex continuation below. The emulator evidence is
not literally complete for one older A2 bullet: the animated splash never reached uiautomator's idle
state, so A2 has a screenshot plus the app's diagnostic logcat line instead of a splash-time dump
excerpt. Two implementation deviations from the brief remain:

1. **The exit motion metric is not the brief's literal one.** The literal metric (centroid of all changed pixels) was measured and does not separate scale 1 from scale 0: 141 px vs 126 px. The one used instead is the trail centroid outside the removed arrow's footprint.
2. **Floors include a third control, B0.** B0 is the base APK at scale 0, which the brief itself names as a valid non-rendering control. The A ∪ B floor of 0 px let the scale-0 blocked tap PASS on 6 px of redraw antialiasing.

The owner accepts or rejects each of these below.

## Commits

| sha | stage |
|---|---|
| `bd3368a` | A: PERF builds boot into splash, menu or game via `EXPO_PUBLIC_PERF_SCREEN` |
| `bb1461b` | B: motion scales set, relaunched and read back; app-reported reduced motion |
| `8ad2524` | C: `capture.mjs`, `benchmark --record`, recorder calibration |
| `71c04bf` | D: pixel-diff floor calibrated; exit motion measure; sets separate |
| `33dcd35` | E: `benchmark --assert-rendered blocked|exit`, validated per phase |
| (this report) | docs |

## Pre-fix reproduction (before any code change, on da93dcd JS)

`artifacts/P-02/prefix/PREFIX-SUMMARY.txt`

- **APK.** The base PERF APK `fea8a939…` was built as a JS repack. The 17 JS files that differ between da93dcd and HEAD were checked out at da93dcd (`git diff --diff-filter=M`; the 26 added files are not imported by da93dcd code), bundled with the harness env (`EXPO_PUBLIC_PERF_LEVEL=3827`, `FEEDBACK=1`, `EXIT_DURATION_MS=180`, …), then restored. `git status` was clean afterwards (EXECUTED). Bundle grep confirmed da93dcd JS: `ensureIdentity`, `createSplashBackstop` and `blockedFlashMixAt` each 0 (EXECUTED).
  - A first attempt from a `git archive` copy with symlinked `node_modules` produced an APK that stayed blank (fonts never resolved, INFERRED: asset paths outside the project root). It was discarded.
- **EXECUTED:** the PERF build boots to the game only. The dump has `perf-game-screen`/`perf-board`, the screenshot shows LEVEL 3828 (`prefix-scale0-pre-small.png`), and da93dcd `App.tsx:24` has `useState<Screen>(PERF_MODE ? 'game' : 'splash')`.
- **EXECUTED:** `grep -c animatorDurationScale` on `artifacts/W0-04/perf/aba-1-base-perf-repack.json` and `aba-2-fix-perf-repack.json` gives `0` and `0`.
- **EXECUTED (the blind spot, observed).** Blocked tap on (35,19) with `input tap`, then `sleep 0.1; screencap` in the same shell.
  - Scales `0 0 0` (read back): **0** flash-magenta px in the board.
  - `1 1 1` after a force-stop and relaunch: **2371** px.
  - Crop: `prefix-scale0-vs-scale1-after-100ms-crop.png` (left no flash, right magenta blocker; I looked at it).

## Stage A: boot into any screen

- `src/perfMode.ts`: `parsePerfScreen` and `PERF_SCREEN`. `PERF_SCREEN` is read only when `PERF_MODE`, and an unknown value throws. `App.tsx` uses it for the initial PERF screen.
- `benchmark.mjs`:
  - the build env moved into the exported `perfBuildEnv()`, which adds `EXPO_PUBLIC_PERF_SCREEN`;
  - `benchmarkConfiguration` writes `perfScreen` for fresh builds and caller APKs;
  - `splash` and `menu` are in `VALID_PHASES` only. A splash/menu build measures its own screen by default. Board phases need `game`;
  - `validateScreen` / `measureScreenPhase` / `startScreen` handle non-game builds;
  - `main()` no longer runs on import, so node:test can cover `parseArgs`.
- **A1 (EXECUTED fresh Gradle build in Codex continuation; earlier repack leg also EXECUTED).** With 15 GiB free immediately before it, the exact command `npm run perf:android -- --feedback` ran `buildRelease()` → `perfBuildEnv`, completed a clean Expo prebuild and Gradle release build, passed `validateWorkload`, completed 10 measured runs, and exited 0. Its JSON has `"perfScreen": "game"`, `"configurationVerifiedFromFreshBuild": true`, and the unchanged six default phases (`codex/A1-fresh-gradle-df.txt`, `.log`, `.json`).
  - The earlier repack command `npm run perf:android -- --feedback --skip-build --apk artifacts/P-02/apk/stageA-game-perf.apk --runs 1 --warmups 0` also exited 0 and its log has `validating deterministic workload` (`stageA/A1-game.json`, `A1-game.log`).
  - The APK was built through `perfBuildEnv(parseArgs(['--feedback']))` (`artifacts/P-02/scripts/perf-repack.sh`), so the Stage A env passthrough itself produced `EXPO_PUBLIC_PERF_SCREEN=game`.
  - `DEFAULT_PHASES` diff (`stageA/A1-DEFAULT_PHASES-diff.txt`): the only change on that line is the added `export`. Its value is `["zoomIn","panHorizontal","panVertical","zoomOut","blocked","exit"]`, the same as da93dcd lines 71-72, and the node test asserts equality.
- **A2 (EXECUTED).**
  - **Menu build:** the benchmark `--phases menu` passed `validateScreen` (`stageA/A2-menu.log`). The dump excerpt `stageA/A2-menu-head-uiautomator-excerpt.txt` has `text="Play"`, `text="rrows"`, `text="Level 1"`, `content-desc="perf-reduced-motion-1"` and no `perf-board`. Screenshot: `A2-menu-head-screenshot.png`.
  - **Splash build:** screenshots at scale 1, 1.2 s after launch, show the wordmark and the arrow drawing (`A2-splash-scale1-vs-menu.png`, `A2-splash-head-scale1-t1000ms.png`; I looked at both).
  - The logcat of the HEAD splash build reads `[capture-diag] perf-reduced-motion-0 screen=splash` at 06:03:45.695, then `screen=menu` at 06:03:48.414 (`A2-splash-head-logcat.txt`).
  - The benchmark `--phases splash` passed `validateScreen`, with the splash handing over to the menu.
  - **Instrument limit found:** `uiautomator dump` fails with "could not get idle state" while the splash (or the menu's breathing pill) animates at scale 1. The splash therefore has no dump excerpt taken during the splash. The dump at scale 0 already shows the menu, because the splash completes instantly under reduced motion (`A2-splash-scale0-first-dump.xml`). The logcat line is the textual evidence instead.

## Stage B: motion settings set, applied, read back

- `scripts/perf/android/device.mjs` holds the shared plumbing: `capture`, `adb`, `delay`, `resolveActivity`, `physicalDisplaySize`, `readWm`, the launch helpers, `applyMotionScale`, `readAppReducedMotion`, and the screenrecord helpers. `benchmark.mjs` imports it, and its own copies were removed.
- `--motion-scale <0|1>` defaults to 0. `validateDevice` no longer puts the scales. After install, `applyMotionScale` puts all three, force-stops, launches, reads back, and reads the app value. The environment block gains the read-back scales, `relaunchedAfterScaleChange: true`, `appReducedMotion`, and `reducedMotionVariantMeasured: true` when reduced; a warning is printed. The harness aborts on a mismatch.
- **App-reported value (ruling F01).** `perf-reduced-motion-0|1` is on the `App.tsx` root (the pre-ready View and `GestureHandlerRootView`) when `PERF_MODE || EXPO_PUBLIC_CAPTURE_DIAG=1`. The app also logs one `[capture-diag] … screen=<screen>` line per screen, because uiautomator cannot dump animating screens. The harness reads the current PID's logcat line first, then the label.
- **B1 (EXECUTED).** One saved APK `stageB-game-perf.apk` (`3f3afaa8…`), run at `--motion-scale 0` and `1` (`stageB/B1-environment-diff.txt`). The environment blocks differ **only** in:
  - `windowAnimationScale` / `transitionAnimationScale` / `animatorDurationScale`: 0 vs 1;
  - `appReducedMotion`: true vs false;
  - `reducedMotionVariantMeasured`: true vs absent.
  Both carry `relaunchedAfterScaleChange: true`. Label via uiautomator also EXECUTED: `content-desc="perf-reduced-motion-0"` on the root at scale 1 (`B3-label-uiautomator-scale1-game.xml`).
- **B2 (EXECUTED).**
  - Bogus keys: the device was at `1 1 1` and the request was `--motion-scale 0 --diagnostic-bogus-scale-keys`. The JSON records `1/1/1`, `appReducedMotion: false`, `motionDiagnostic: "bogus-scale-keys"`. The `*_p02_bogus` keys read `0` and were deleted afterwards (`stageB/B2-*`).
  - Skip relaunch: `--motion-scale 0 --diagnostic-skip-relaunch` exits 1 with `Error: appReducedMotion=false disagrees with transition_animation_scale=0: the app process started before the scale change`, and no JSON is written (`B2-skip-relaunch.log`).
- **B3 (EXECUTED grep).** `docs/perf-tap-forgiveness-ab-2026-09-08.md` starts with the ERRATUM block. `docs/perf-harness.md` names `transition_animation_scale`, the `exit` phase (Rule 1, Rule 2) and `docs/perf-exit-native-slither-2026-09-01.log`.

## Stage C: capture mode

- `scripts/perf/android/capture.mjs` (`npm run capture:android`):
  - `--motion-scale` is required;
  - `--apk`; `--wm-size`/`--wm-density` are applied, read back, and reset in `finally` and on SIGINT;
  - `--shot`, `--record --seconds`, `--record-size` (default half the physical size, 720x1560), `--frames` (ffmpeg split plus ffprobe times);
  - `--step blocked|exit|won|lost`, `--build-env KEY=VALUE` (ruling F23);
  - `manifest.json` has every Stage C field.
- `benchmark.mjs --record <label>` forces `runs=1`, adds `perfNumbersInvalid: true` and `summary: null`, writes one mp4 per phase, and cannot be combined with soak.
- **Tap hold (EXECUTED).**
  - `getevent -lt` during `input tap` shows no events, only the device list: injected events bypass `/dev/input`.
  - Measured instead through the `dumpsys input` RecentQueue DOWN→UP `eventTime`: `input tap` 0,0,0,0,0 ms; ArrowsWorkload tap 40,36,36,38,35 ms. Both are < 64 ms.
  - Scripted single taps use `input tap`. ArrowsWorkload's JVM exits ~0.9 s after its UP (EXECUTED: 944–974 ms end to end vs 14–23 ms for `input tap`), which would put a following screencap past the flash.
- **C1 (EXECUTED).**
  - PERF: `capture.mjs --apk stageB-game-perf.apk --motion-scale 1 --shot P-02-C1-perf-game` gives `appReducedMotion: false` (logcat).
  - Non-PERF: `capture.mjs --apk head-nonperf-release-repack.apk --motion-scale 0 --wm-size 1080x1920 --wm-density 480 --settle-ms 4000 --shot P-02-C1-nonperf-menu-1080x1920-480` gives `appReducedMotion: "unavailable: non-PERF build"`. The wm read-back is override `1080x1920` / `480`, and the PNG is 1080x1920 and shows the menu (I looked at it).
  - Both manifests contain git (head plus dirty), APK sha256, serial/AVD/API, wm read-back, the three scales, `relaunchedAfterScaleChange`, `appReducedMotion`, `tapHold` and `recorderFramePeriod`.
  - **After both runs,** `wm size` reads `Physical size: 1440x3120` with no override (`stageC/C1-wm-after-both.txt`).
  - Extra runs: the error path (a `--step` on a non-PERF menu makes `findBoardBounds` fail) still reset wm (`P-02-C1-error-path-wm-reset/manifest.json`, `wmAfterReset` physical). A non-PERF `EXPO_PUBLIC_CAPTURE_DIAG=1` build at scale 1 reports `appReducedMotion: false` via logcat, the F01 non-PERF path.
- **C2 (EXECUTED)** `calibrate.mjs recorder` / `idle`, table in `docs/perf-capture-calibration.md`:
  - frames inside the 650 ms flash: 40, 40, 39, 40, 40;
  - median periods: 16.589, 16.589, 16.906, 16.500, 16.733 ms;
  - reconciliation: observed 39–40 vs 650 ÷ median = 38.4–39.4;
  - idle board 2 s: **1 frame**, which confirms emit-on-change.
- **C3 (EXECUTED).** `benchmark --record P-02-C3-record --phases blocked,exit --motion-scale 1`: `grep -c '"perfNumbersInvalid": true'` → 1, `grep -cE '"p9[59]"'` → 0, case-insensitive `p9[59]` → 0. `measuredRuns: 1`, `summary: null`, two mp4s (143 and 114 frames).

## Stage D: pixel-difference floor

- `pixel-diff.mjs`: `decodePng`, `resolveRegion` (board minus the header strip, device→recording scale), `parseMask`, `diffImages` (max channel delta > tolerance 24), and `exitMotion` / `trailMotion`. There is a CLI.
- **D1 (EXECUTED TDD).** RED: `artifacts/P-02/gates/tdd-red-pixel-diff.txt` (module not found), then GREEN. The synthetic tests cover:
  - 7 changed pixels plus 5 exactly at tolerance give 7/1200;
  - a 68 px mask removes exactly 40 changed + 28 unchanged pixels;
  - the board crop excludes a synthetic header strip;
  - the half-scale rect;
  - centroid displacement;
  - exit motion: moving dash → 16 px; stationary fade → no trail, 0 px, changed > 0 (RED `tdd-red-exit-motion.txt`).
- **D2 (EXECUTED)** on the base da93dcd PERF APK, n = 10 per set, raw values in the doc:
  - blocked board px: A 0×10, B (empty board) 0×10, **B0 (base, scale 0) 6×10**, C 2933–3009. ~~Floor = 6 + 6 = 12 px.~~ **Superseded in fix round 1:** a held-out cell (31,14) reads 14 px at scale 0, so the floor is now 28 px (see Fix round 1).
  - screen region: B 3122–3758 (heart pip) vs C 6370–7259. **Does not separate**, documented, not used.
  - menu idle: scale 0 unmasked 0; scale 1 unmasked 10160–10544 (floor 10928 px); pill-masked 0 at both scales.
  - exit: A/B displacement 0 and trail 0; B0 trail 18 px, displacement 0; C displacement 164.5–180.3 px at the trail floor of 36. Floors: trail 36 px, displacement 1 px (resolution), changed fraction 0.
  - Kill condition **did not fire**.
- Superseded attempts are kept and disclosed: ArrowsWorkload-tap pairs (C read 0, mid frame too late), Text-node pill mask, and centroid-vs-pre exit metric (no separation).

## Stage E: effect-rendered gate

- `benchmark.mjs --assert-rendered blocked,exit` is opt-in. It runs the probes shared with `calibrate.mjs`, reads the floors from the doc's `renderedFloors` block, and records `assertRendered.results.<phase> = { ran, passed, measured, floor }`. Exit code 3 on a failure.
- **E1 (EXECUTED, fresh runs, not the calibration samples).**

| APK | read-back | blocked | exit |
|---|---|---|---|
| base `fea8a939` | 1 1 1 | **PASS** 2944 px > 12 (fix round 1: 3008 > 28) | **PASS** 180.3 px > 1, changed 1.156e-3 |
| base `fea8a939` | 0 0 0 | **FAIL** 6 px (calibration-fitted; held-out (30,21): 9 px ≤ 28 FAIL) | **FAIL on moved check** 0 px; changed fraction **9.879e-4 > 0** (printed) |
| base empty board `46c57ef7` (B) | 1 1 1 | **FAIL** 0 px | **FAIL** 0 px, changed 0 |

- The first validation attempt with the A ∪ B floor of 0 px (`stageE/attempt1-floor0/`) had the scale-0 blocked tap **PASS on 6 px**. That result led to B0; see Decisions.
- Flip check (EXECUTED): HEAD APK with the W0-04 fix, scale 0, blocked: **PASS 2735 px**.
- **E2 (EXECUTED).** Same APK, same args (`--feedback --skip-build --runs 1 --warmups 0 --phases blocked`), run with da93dcd's harness files (temporary copy, deleted) and with HEAD's. The structural key diff (`stageE/E2-structural-diff.txt`): nothing is only in da93dcd. Only in HEAD: `benchmark.perfScreen` and `environment.{windowAnimationScale, transitionAnimationScale, animatorDurationScale, relaunchedAfterScaleChange, appReducedMotion, reducedMotionVariantMeasured}`. Top-level and `benchmark` key order are equal apart from `perfScreen`. No `perfNumbersInvalid`/`assertRendered`/`recordings`/`motionDiagnostic`.

## Gates

- EXECUTED `node --test scripts/perf/android/*.test.mjs`: **51 pass / 0 fail**. Base was 16: +7 capture, +9 device, +8 pixel-diff, +11 benchmark-args.
- EXECUTED `npx tsx --test scripts/perf/android/soak-plan.test.ts`: 3/3. `soak-plan.ts` gained a `dir` per tap; the checksums are unchanged (`solveChecksum 672ed387` computed from row,col only).
- EXECUTED `npx tsc --noEmit`: exit 0.
- EXECUTED `npx jest`: **26 suites / 548 tests** pass. Base `a8fb421` was 25 / 543; +1 suite `src/__tests__/perfMode.test.ts` with 5 tests. RED `gates/tdd-red-perfMode*.txt`.
- F33 (`--selectProjects ui`) does not apply: GameScreen.tsx and ads.tsx are not edited, and jest.config has no projects.
- EXECUTED `git status --porcelain` is clean after the report commit. `artifacts/` and `android/` are ignored, and nothing under `artifacts/` or `credentials/` is committed.

## Decisions (routine calls, recorded)

1. **Original evidence APKs used JS repacks.** The recipe is in `artifacts/P-02/scripts/repack.sh` / `perf-repack.sh`. The host is `artifacts/W0-04/perf/apk/fix-gradle-perf.apk` (`21110213…`, profileable, native unchanged since da93dcd: `git diff da93dcd..HEAD -- modules plugins` is empty, `app.json` changes only iOS `supportsTablet`). The non-PERF host is `artifacts/base/da93dcd-release-arm64.apk`. Ruling E-2 accepts repacks for JS-only evidence. The APKs are debug-signed. A1's required fresh leg was later run through Gradle; see Codex continuation.
2. **Label placement (F01 over B3's `perf-game-screen`).** The label is on the App root, plus a logcat line, so splash and menu expose it and `GameScreen.tsx` is untouched.
3. **A base PERF APK without the label** records `appReducedMotion: "unavailable: PERF build without the P-02 reduced-motion label (built before P-02)"` and does not abort. In that case `reducedMotionVariantMeasured` comes from the read-back `transitionAnimationScale == 0`.
4. **Screen phases:** `SCREEN_PHASE_WINDOW_MS` splash 3000 / menu 1500 (OWNER-PICKED STARTING VALUE). A splash/menu build with no `--phases` measures its own screen.
5. **Exit metric.** Trail centroid outside the footprint, dilated 2 px; second frame ≥ 60 ms later (brief deviation 2 above).
6. **B0 control** (brief deviation 3 above). Each floor uses the controls that are negative for its own property: rendered/moved use A ∪ B ∪ B0; "pixels changed" uses A ∪ B (B0 does draw). The displacement floor is raised from 0 to 1 recording px (instrument resolution).
7. **`input tap` for single scripted taps and probes; ArrowsWorkload for benchmark phases and the `won` solve.**
8. **Recording size is half resolution (720x1560).** `screenrecord` cannot encode 1440x3120 and falls back to a distorted 720x1280 (EXECUTED).
9. **`.gitignore` was not edited** (F34: `/artifacts/` already covers `artifacts/captures/`).
10. **New files beyond the brief's list:** `scripts/perf/android/calibrate.mjs` (so every doc number names a committed script), `capture.test.mjs`, `device.test.mjs`, `benchmark-args.test.mjs`, `src/__tests__/perfMode.test.ts`. `scripts/perf/android/soak-plan.ts` also gained the per-tap `dir` field that the exit gate needs.
11. **OWNER-PICKED STARTING VALUES** (all marked in code): tolerance 24, n = 5 recordings and n = 10 per set, mid-phase delay 0.15 s, menu gap 0.5 s, exit second-frame offset 60 ms, footprint dilation 2 px, magenta onset 40 px, sample settle 2.5 s, capture settle 1.5 s, record lead-in 800 ms, app reduced-motion timeout 8 s, pill-mask growth 3% + 8 px.

## Evidence labels and limits

- UNVERIFIED-DEVICE: every floor, frame period and tap-hold number belongs to this software-GPU emulator on this Mac and does not transfer to a phone. Thermal/GPU behaviour. iOS was not built (P-02b/P-02c).
- EXECUTED (Codex continuation): the fresh `npm run perf:android -- --feedback` path ran Expo prebuild, Gradle, install, device validation and all six default phases; see `artifacts/P-02/codex/A1-fresh-gradle.*`.
- INFERRED: non-PERF store builds behave the same. `CAPTURE_DIAG_ENABLED` is inlined `false`, the label is `undefined` and the log effect is a no-op. The non-PERF capture's dump showed no label (`unavailable: non-PERF build`, EXECUTED), but no store-build UI regression run was done.
- INFERRED: the 6 px B0 difference is feedback-layer antialiasing redraw. The pixel coordinates sit on the tapped arrow and the blocker (EXECUTED listing); the cause was read, not instrumented.
- The ffmpeg image2 muxer prints "non monotonically increasing dts" warnings when splitting. Frame-file count equals the ffprobe pts count for every analysed recording (asserted in code), so no frame was lost.

## Concerns

- ~~The blocked floor of 12 px rests on a deterministic 6 px control.~~ Fix round 1 confirmed this concern: the 12 px floor passed a blackout on cell (31,14). The floor is now 28 px, held out on one cell. See Fix round 1.
- The exit gate is calibrated for the one exit-phase arrow (head (3,16), up) at 180 ms. The blocked floor is fitted on two cells of level 3827 and has now failed one actual held-out assertion at (30,21), but no other level or arrow shape is covered.
- `benchmark.mjs` grew from 1627 to about 1900 lines. It was already large; the new code is split into `device.mjs` / `capture.mjs` / `calibrate.mjs` where possible.
- Disk fell to 3.0 GB free during the run (other sessions). The P-02 APKs under `artifacts/P-02/apk` take about 290 MB and can be deleted once the owner has accepted.

## Emulator state left behind

`artifacts/P-02/emulator-restore-state.txt`:

- scales `1 1 1`, as found at boot;
- bogus keys `null`;
- `wm size`/`density` physical 1440x3120 / 560;
- refresh rates 60.0;
- installed package: `head-menu-perf.apk`.

The emulator was shut down with `adb -s emulator-5556 emu kill`.

## Owner acceptance needed

1. `docs/perf-capture-calibration.md` (Stage D/E): **yes/no**. Accept the trail-centroid-outside-footprint exit metric in place of the brief's centroid of all changed pixels, which was measured not to separate (141 px at scale 1 vs 126 px at scale 0). Recommended: yes.
2. `docs/perf-capture-calibration.md` renderedFloors: **yes/no**. Accept adding B0 (base APK at scale 0, n = 10) to the floor controls, so blocked > 28 px (fix round 1; was 12), exit trail > 36 px and displacement > 1 px. The blocked scale-0 FAIL rests on this. The exit scale-0 moved FAIL does not (fix round 1 re-score). Recommended: yes.
3. OWNER-PICKED starting values listed in Decision 11: **yes/no** to keep them as the instrument defaults. Recommended: yes.
4. Eyeball `artifacts/P-02/prefix/prefix-scale0-vs-scale1-after-100ms-crop.png`, `artifacts/P-02/stageA/A2-splash-scale1-vs-menu.png` and `artifacts/captures/P-02-C1-nonperf-menu-1080x1920-480/P-02-C1-nonperf-menu-1080x1920-480.png`: **yes/no**. They show no flash vs flash, the splash vs the menu, and the 360×640 dp menu respectively.

## Fix round 1 (2026-09-17, base `8e094c6`)

Evidence: `artifacts/P-02/fix1/` (gitignored). Device: `emulator-5556` / `fleet_floor_api31` / API 31. It was booted without a wipe (read-back at boot: scales `1 1 1`, 1440x3120 @560), restored to `1 1 1` and shut down with `emu kill` afterwards (`emulator-state-at-boot.txt`, `emulator-restore-state.txt`). An `emulator-5554` from another session appeared during the run and was not touched. Every JSON records `serial: emulator-5556`.

### Finding by finding

1. **A1 gradle fresh build: now marked BLOCKED** (status summary, deviation 1, and the A1 line above).
   - The gradle leg `buildRelease()` → `perfBuildEnv` has **not been executed** (UNVERIFIED).
   - Disk gate, EXECUTED (`artifacts/P-02/fix1/A1-disk-gate-df.txt`):
     ```
     -- df -h / BEFORE in-repo cleanup
     /dev/disk3s1s1   460Gi    16Gi   3.3Gi    83%    459k   35M    1%   /
     -- git check-ignore
     .gitignore:41:/android	android/build
     .gitignore:41:/android	android/app/build
     -- sizes
     184K	android/build
     ls: android/app/build: No such file or directory
     -- removed android/build (gitignored, regenerable)
     -- df -h / AFTER in-repo cleanup
     /dev/disk3s1s1   460Gi    16Gi   3.3Gi    83%    459k   35M    1%   /
     ```
   - To close it once df ≥ 5 GB: `df -h /` then `npm run perf:android -- --feedback` (no `--skip-build`); check `validateWorkload` passes and `"perfScreen": "game"` is in the JSON.
2. **Exit metric deviation:** no code change. The deviation is disclosed, and owner acceptance #1 stays open.
3. **B0 control:** owner acceptance #2 stays open. The floor number changed; see finding 7.
4. **Extra files:** no change, still justified. This round adds one more: `scripts/perf/android/calibrate.test.mjs`. It covers the new `--cell` option and pins the committed floor against every measured blackout.
5. **B3 label placement (F01):** compliant, no change.
6. **F34 `.gitignore`:** compliant, no change.
7. **Blocked scale-0 FAIL was fitted, not held out: confirmed, and the floor was wrong.**
   - The reviewer's attack succeeded. The same probe on cell **(31,14)**, base APK `fea8a939`, read-back `0 0 0`, gives **14 px ×10**, above the 12 px floor. The old gate would have PASSED a reduce-motion blackout on that cell.
   - The protocol was then written to `artifacts/P-02/fix1/PREREGISTRATION.txt` **before** any (30,21) sample: refit on (35,19) + (31,14), test on (30,21), no second refit.
   - **Refit:** max(A ∪ B ∪ B0(35,19) ∪ B0(31,14)) = 14, plus spread 14, gives **28 px**.
   - **Held out on (30,21):**
     - scale 0 gives **9 px ×10**, ≤ 28, so FAIL (correct);
     - scale 1 gives **5144–5167 px ×3**, > 28, so PASS (correct).
   - **Positive control on (31,14), scale 1:** 3075–3171 px.
   - Where the scale-0 pixels sit (EXECUTED listing; the antialiasing cause is INFERRED):
     - (31,14): one column x = 653, y 2106–2125, on the tapped arrow's head edge;
     - (30,21): 9 px at the tapped arrow's corners.
   - **End-to-end gate re-run with the new floor**, fresh, EXECUTED:
     - scale 0: `assert-rendered blocked: FAIL (changed 6 px vs floor 28)`, exit 3;
     - scale 1: `assert-rendered blocked: PASS (changed 3008 px vs floor 28)`, exit 0.
   - **E1 row 2 is now labelled "calibration-fitted".** Rows 2h/1h (the held-out cell) were added as the held-out evidence.
   - **Re-scored, not re-run:** row 3 (0 px) and the W0-04 flip check (2735 px > 28). The blocked probe never reads the floor.
   - **The reviewer's note on exit is confirmed (EXECUTED).** The saved E1 scale-0 `exit.mp4` re-scored at trail floor 0 gives displacement **0 px**, the same as at 36. The exit scale-0 moved FAIL does not rest on B0. My earlier concern overstated that; corrected in owner acceptance #2. At scale 1, the trail floor only removes the tail artefact: 518.4 px at floor 0, 180.3 px at floor 36 (`exit-rescore-trail-floor.txt`).
   - **Limits, still open:**
     - the held-out evidence is one cell of level 3827 on this emulator, deterministic per cell, with a 9 px vs 28 px margin;
     - an arrow shape, level or redraw path that repaints more than 28 px of antialiasing without drawing the effect would pass;
     - the gate always taps (35,19) of level 3827.
     These limits are stated in `docs/perf-capture-calibration.md`.

### What changed

- `scripts/perf/android/calibrate.mjs`:
  - `blocked-pairs --cell row,col`, default `DEFAULT_BLOCKED_CELL = [35, 19]` (the gate's cell);
  - exported `parseCalibrateArgs` (was the private `parseCli`) and `calibrationOutputName`, so a held-out cell never overwrites the gate cell's JSON;
  - the result JSON records `cell`.
  - The Stage E gate code (`benchmark.mjs`) is unchanged. It reads the floor from the doc.
- `scripts/perf/android/calibrate.test.mjs` (new), 4 tests:
  - default and explicit cell;
  - `--cell` validation;
  - output name;
  - the committed `renderedFloors` must FAIL 6/14/9 px blackouts and PASS 2933/3075/5144 px flashes through `renderedVerdict`.
- `docs/perf-capture-calibration.md`:
  - "Held-out cells" subsection with raw values, command, pixel locations and limits;
  - floor arithmetic 12 → 28 px;
  - E1 table: floor column, rows 2h/1h, the row-2 "calibration-fitted" label, the end-to-end re-run, the exit re-score;
  - flip check re-scored;
  - `renderedFloors.blocked.changedPixelsFloor` 12 → 28, with the new arithmetic string.
- `docs/perf-harness.md`: blocked gate text `> 12 px` → `> 28 px`, with a pointer to the limits.
- This report: A1 marked BLOCKED, floor numbers corrected inline (struck through where superseded), this section added.

### Commands, covering tests, output (all EXECUTED)

- **TDD for `--cell`.**
  - RED: `node --test scripts/perf/android/calibrate.test.mjs` gives `SyntaxError: The requested module './calibrate.mjs' does not provide an export named 'DEFAULT_BLOCKED_CELL'` (`fix1/tdd-red-calibrate-cell.txt`). Expected: the option did not exist.
  - GREEN: `# tests 3 # pass 3 # fail 0` (`fix1/tdd-green-calibrate-cell.txt`).
- **TDD for the floor.**
  - RED, against the committed 12 px doc: `not ok 4 … B0 (31,14) fitted 14 px must FAIL … expected: false actual: true`, `# tests 4 # pass 3 # fail 1` (`fix1/tdd-red-floor-rejects-blackouts.txt`). That is the original failing path: the committed floor passed a measured blackout.
  - GREEN, after the doc change: `# tests 4 # pass 4 # fail 0`.
- **Held-out captures:** `node scripts/perf/android/calibrate.mjs blocked-pairs --apk artifacts/P-02/apk/base-da93dcd-perf.apk --set B0 --motion-scale 0 --n 10 --cell 31,14 --out artifacts/P-02/fix1/heldout`. The same command was also run with `--set C --motion-scale 1 --n 3 --cell 31,14`, `--set B0 --motion-scale 0 --n 10 --cell 30,21` and `--set C --motion-scale 1 --n 3 --cell 30,21`. Output lines:
  - `blocked set B0 scale 0 #1..#10: board 14/4091040`
  - `blocked set C scale 1: board 3075, 3077, 3171`
  - `blocked set B0 scale 0 #1..#10: board 9/4091040`
  - `blocked set C scale 1: board 5163, 5144, 5167`
  APK sha `fea8a939`. Read-back `0 0 0` / `1 1 1` per set. Gap from input end to mid screencap 158–191 ms (`fix1/heldout-summary.txt`).
- **Gate re-run:** `npm run perf:android -- --feedback --skip-build --apk artifacts/P-02/apk/base-da93dcd-perf.apk --runs 1 --warmups 0 --phases blocked --motion-scale <0|1> --assert-rendered blocked --label P-02-fix1-E1-base-scale<s> --output artifacts/P-02/fix1/E1-fix1-base-scale<s>.json`. Output as quoted in finding 7.
- **Gates** (`fix1/gates.txt`):
  - `node --test scripts/perf/android/*.test.mjs`: **55 pass / 0 fail** (51 before; +4 in `calibrate.test.mjs`);
  - `npx tsx --test scripts/perf/android/soak-plan.test.ts`: 3/3;
  - `npx tsc --noEmit`: exit 0;
  - `npx jest`: **26 suites / 548 tests** passed, the same as at `8e094c6`;
  - F33 does not apply (GameScreen.tsx and ads.tsx untouched).

### Status after fix round 1

**BLOCKED** only on A1's gradle leg (df 3.3 GB < 5 GB after the allowed cleanup). Everything else is done.

At that point owner acceptance #3 (A1 repack or later Gradle run) was still open. The Codex
continuation below closes it with the requested Gradle execution. The other owner acceptances stay
open.

## Codex continuation

2026-09-19, starting at git HEAD `fdb60575ff493f128e2a65ca8bfc8379f43c4f83`. Evidence is under
`artifacts/P-02/codex/`. Only `emulator-5556` was used; the other session's `emulator-5554` was not
touched.

### Finding 1: A1 fresh Gradle leg

- **EXECUTED disk gate:** `df -h /` was the command immediately before the build and reported 15 GiB
  available (`A1-fresh-gradle-df.txt`), above the binding 5 GB minimum.
- **EXECUTED exact command:** `npm run perf:android -- --feedback`, with neither `--skip-build` nor
  `--apk`. Expo prebuild completed, then `:app:assembleRelease` reported `BUILD SUCCESSFUL in 5m 24s`
  with 636 actionable tasks (608 executed, 28 up-to-date). The command installed that newly built APK,
  printed `validating deterministic workload`, ran one warmup plus 10 measured runs across
  `zoomIn,panHorizontal,panVertical,zoomOut,blocked,exit`, and exited 0 (`A1-fresh-gradle.log`).
- **EXECUTED result:** `A1-fresh-gradle.json` records `perfScreen: "game"`,
  `configurationVerifiedFromFreshBuild: true`, `nativePrebuildRecreated: true`, `emulator-5556`, and
  APK SHA-256 `c8f227c266c08e3c59333a1bfed66b2f8c0950f8575d79851ad9188770046bea`.
  This closes the old disk-blocked A1 leg; accepting the repack as a substitute is no longer needed.

### Finding 2: independent held-out blocked assertion

- Fix round 1 fitted the 28 px floor only on B0 cells (35,19) and (31,14). Cell (30,21) of level
  3827 was not used to fit the floor. The floor remains **28 px**; it was not changed after observing
  this continuation sample.
- `benchmark.mjs` now accepts the evidence-only option
  `--assert-rendered-blocked-cell <row,col>`. The default remains (35,19), an explicit cell must be
  inside the 39×39 grid and may only be supplied when `--assert-rendered` includes `blocked`, and the
  measured cell is written into the result JSON. This was necessary to exercise the real gate at a
  held-out cell rather than only calling the calibration probe and verdict separately.
- **EXECUTED held-out gate, one fresh sample:** base APK `fea8a939…`, read-back scales `0 0 0`,
  force-stop/relaunch, level 3827, cell (30,21). The command was:
  `npm run perf:android -- --feedback --skip-build --apk artifacts/P-02/apk/base-da93dcd-perf.apk --runs 1 --warmups 0 --phases blocked --motion-scale 0 --assert-rendered blocked --assert-rendered-blocked-cell 30,21 --label P-02-codex-heldout-cell30-21-scale0 --output artifacts/P-02/codex/heldout-cell30-21-scale0.json`.
- **EXECUTED outcome:** `--assert-rendered blocked` reported **FAIL**, the intended blackout verdict:
  **9 changed board pixels out of 4,091,040** (`2.1999296e-6`) versus the unchanged **28 px** floor,
  and exited 3 as specified for a failed assertion. The gate therefore did not miss this held-out
  blackout. No floor adjustment was made.
- Evidence: `heldout-cell30-21-scale0.json`, `.log`, and
  `heldout-cell30-21-scale0/{blocked-pre.png,blocked-mid.png}`. I inspected both PNGs: the board has
  no visible blocker flash; the lost heart is outside the calibrated board region. The first install
  attempt was setup-only and reached no probe because Android rejected replacing the fresh release
  APK with the differently signed base APK; it is retained as `heldout-install-signature-failure.log`.
  After uninstalling only `com.danteb.arrows` from `emulator-5556`, the command above ran normally.

### TDD and final gates

- **RED:** `node --test scripts/perf/android/benchmark-args.test.mjs` failed 1 of 13 because
  `assertRenderedBlockedCell` did not exist (`tdd-red-assert-rendered-blocked-cell.txt`).
- **GREEN:** the same command passed 13/13 after the option, validation and measured-cell recording
  were implemented (`tdd-green-assert-rendered-blocked-cell.txt`).
- **EXECUTED:** `node --test scripts/perf/android/*.test.mjs`: **57 pass / 0 fail**;
  `npx tsx --test scripts/perf/android/soak-plan.test.ts`: **3/3**; `npx tsc --noEmit`: exit 0;
  `npx jest`: **26 suites / 548 tests**. Logs are `gate-*.txt` under the Codex evidence root.
- **EXECUTED restore:** scales read back `1 1 1`, display read back physical 1440×3120 @560, the
  three bogus settings keys are `null`, and `emulator-5556` was shut down with `emu kill`
  (`emulator-restore-state.txt`).

### Decisions and remaining concerns

- The held-out override is intentionally limited to the blocked assertion; exit behaviour is
  unchanged. The standard gate still defaults to its calibrated (35,19) cell.
- One deterministic held-out cell on level 3827 does not prove generalization to another level,
  arrow shape, device or host. This is a stated evidence limit, not a reason to tune the floor.
- Owner acceptance #1 (trail-centroid exit metric) and #2 (adding B0 to the floors) remain pending;
  this continuation does not decide either ruling.
