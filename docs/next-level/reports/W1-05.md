# W1-05 — Teach the blocked-lane rule at first encounter

**Status:** DONE_WITH_CONCERNS  
**Flag:** `FTUE_ENABLED` (default OFF; verification build used `EXPO_PUBLIC_FTUE=1`)  
**Base APK:** `artifacts/base/da93dcd-release-arm64.apk` (`7556f834c720842d47385e066da7aec7b3e05ce2808ed443fe7b794f8f32a428`)  
**Verification APK:** `artifacts/W1-05/post/apk/w105-flags-on-capture-fix1.apk` (`dbd03a137cb56f234b1a5a25042df42545b6755cfa45680735bfabb1c2da5e9f`)

## Outcome

- **EXECUTED:** T2 now gives exactly one free first *fresh* blocked tap per deal. That tap consumes tutorial grace, preserves all three hearts, plays the existing blocked feedback, and replaces the opening line with `Blocked. Clear what's in its way.`
- **EXECUTED:** A fresh blocked tap after grace has been consumed spends a heart. A ledger-refunded repeat remains free and does not consume tutorial grace.
- **EXECUTED:** The next successful removal hides the blocked-rule line. Losing and re-dealing T2 restores both the opening line and the one-shot grace.
- **EXECUTED:** Normal boards continue to pass the ledger decision through unchanged. The future `assist` mode also currently passes it through; W1-06 owns its exception.
- **EXECUTED:** The three FTUE strings are the brief's verbatim recommended copy, total 15 words, and contain no prohibited colour word.
- **EXECUTED:** The FTUE remains behind the existing default-OFF flag. No persistence key, save version, or migration changed.

## What changed

- `src/ui/tapRules.ts`
  - Added the pure `blockedTapCost` policy and the `normal | tutorialGrace | assist` mode contract.
- `src/ui/__tests__/tapRules.test.ts`
  - Added the four named policy scenarios required by the brief.
- `src/ui/ftueCopy.ts`
  - Added `T2_BLOCKED_LINE` verbatim.
- `src/ui/__tests__/ftueCopy.test.ts`
  - Pins exactly three exported strings, their exact wording, their string type, and the prohibited-colour regex.
- `src/ui/GameScreen.tsx`
  - Added per-deal tutorial grace, blocked-line state transitions, re-deal reset, and T2-only policy selection.
  - Added tutorial-only flex constraints so the two-line rule and all three hearts fit without changing the flags-OFF header layout.
- `src/ui/contrastAudit.ts`
  - Mechanically updated source-line tripwires shifted by the `GameScreen.tsx` insertion; no contrast rule or expected value changed.

## Persistence impact

- **INFERRED:** None. Grace and tutorial copy are component/ref state scoped to the current deal. No persisted schema, storage key, or migration path changed.
- **INFERRED:** Rollback is the existing `FTUE_ENABLED` OFF path.

## Pre-fix reproduction

### Normal motion

- **EXECUTED:** Fresh-installed the base APK on `emulator-5556`; all three animation scales were set to and read back as `1`, followed by force-stop/relaunch. Evidence: `artifacts/W1-05/pre-fix/motion/scales.txt`.
- **EXECUTED:** On base level 1, a blocked tap changed the heart row from 3 to 2 while the blocker flash appeared. Evidence: `artifacts/W1-05/pre-fix/motion/level1-before.png`, `artifacts/W1-05/pre-fix/motion/level1-after-120ms.png`, and `artifacts/W1-05/pre-fix/motion/w105-pre-motion.mp4`.

### Reduce motion

- **EXECUTED:** All three animation scales were set to and read back as `0`, followed by force-stop/relaunch. Evidence: `artifacts/W1-05/pre-fix/reduce-motion/scales.txt`.
- **EXECUTED:** The blocked tap still changed the heart row from 3 to 2, but no blocker flash was visible at 120 ms or 1 s. Evidence: `artifacts/W1-05/pre-fix/reduce-motion/level1-before.png`, `artifacts/W1-05/pre-fix/reduce-motion/level1-after-120ms.png`, and `artifacts/W1-05/pre-fix/reduce-motion/level1-after-1s.png`.

## TDD evidence

- **EXECUTED — RED:** `npx jest --selectProjects core --runTestsByPath src/ui/__tests__/tapRules.test.ts src/ui/__tests__/ftueCopy.test.ts --runInBand --no-watchman` failed before implementation because `blockedTapCost` was absent and only two copy exports existed. Evidence: `artifacts/W1-05/tdd/red.txt`.
- **EXECUTED — GREEN:** The same focused command passed 2 suites / 9 tests after implementation. Evidence: `artifacts/W1-05/tdd/green-focused.txt`.

## Device verification

### Capture build and method

- **EXECUTED:** Produced a JS-only capture APK by embedding the final Hermes bundle in the already-built W1-04 flags-on native host. Environment: `EXPO_PUBLIC_FTUE=1`, `EXPO_PUBLIC_CAPTURE_DIAG=1`. No Gradle/native build was run. Evidence: `artifacts/W1-05/post/apk/build-env-and-copy.txt`.
- **EXECUTED:** SHA-256 is `dbd03a137cb56f234b1a5a25042df42545b6755cfa45680735bfabb1c2da5e9f`; APK signature verification passes v2 and v3. Evidence: `artifacts/W1-05/post/apk/fix1-hash.txt` and `artifacts/W1-05/post/apk/fix1-signature.txt`.

### Normal motion sequence

- **EXECUTED:** P-02 reports status `ok`; requested/read-back scales are `1/1/1`, relaunch is recorded, and app-reported reduced motion is `false`. The native display is the emulator's physical `1440x3120 @ 560 dpi` (`411.43x891.43 dp`), with no size/density override. Evidence: `artifacts/W1-05/post/p02/w105-normal-final/manifest.json`.
- **EXECUTED:** The first fresh blocked tap keeps 3/3 hearts, plays the bump/blocker flash, and shows the exact rule line. Evidence: `artifacts/W1-05/post/p02/w105-normal-final/first-blocked-120ms.png`.
- **EXECUTED:** A fresh blocked tap on a different arrow spends one heart and displays its blocker feedback. Evidence: `artifacts/W1-05/post/p02/w105-normal-final/second-blocked-120ms.png`.
- **EXECUTED:** The next successful removal hides the header line. Evidence: `artifacts/W1-05/post/p02/w105-normal-final/after-successful-removal.png`.
- **EXECUTED:** The complete sequence and the heart row are recorded in `artifacts/W1-05/post/p02/w105-normal-final/w105-normal-final.mp4`; a compact sequence sheet is `artifacts/W1-05/post/p02/w105-normal-final/contact-8-24s.png`.
- **EXECUTED:** After exhausting hearts and re-dealing T2, the opening line and 3/3 hearts return; the new deal's first fresh blocked tap is free and switches to the blocked line. Evidence: `artifacts/W1-05/post/redeal-restored.png` and `artifacts/W1-05/post/redeal-first-blocked-free.png`.

### Reduced motion at 360x640 dp

- **EXECUTED:** P-02 reports status `ok`; requested/read-back scales are `0/0/0`, relaunch is recorded, app-reported reduced motion is `true`, and `reducedMotionVariantMeasured` is `true`. The requested/read-back override is `1080x1920 @ 480 dpi` (`360x640 dp`), and P-02 reset it afterward. Evidence: `artifacts/W1-05/post/p02/w105-reduce-360-correct/manifest.json`.
- **EXECUTED:** At 200 ms, W0-04's static blocker is visible, the exact rule line is legible in two lines without ellipsis, and hearts remain 3/3. Evidence: `artifacts/W1-05/post/p02/w105-reduce-360-correct/static-blocker-200ms.png`.
- **EXECUTED:** At at least 1 s, the rule line remains legible and hearts remain 3/3, but the static blocker is no longer mounted. Evidence: `artifacts/W1-05/post/p02/w105-reduce-360-correct/after-blocked-1s.png`.
- **INFERRED:** The missing blocker at at least 1 s is the expected consequence of the consumed W0-04 interface: `BLOCKER_FLASH_MS` is 650 ms and cleanup occurs 40 ms later. Extending that lifetime would violate this task's non-goal against changing W0-04 visual timings. This makes the brief's simultaneous “static blocker visible” and “taken ≥1 s” requirements internally incompatible.

### Header fit and board viewport

- **EXECUTED:** The exact two-line blocked copy has no ellipsis and all three heart pips fit at both measured targets: `360x640 dp` and the controller-corrected native `411.43x891.43 dp`. Evidence: the reduced-motion and normal screenshots above.
- **EXECUTED:** With the tutorial header rendered at `360x640 dp`, diagnostic logs measured T1 board viewport `360x560` and T2 board viewport `360x551.3333129882812`. Evidence: `artifacts/W1-05/viewport/t1-360x640-logcat.txt` and `artifacts/W1-05/viewport/t2-360x640-logcat.txt`.
- **EXECUTED:** The W1-01 metrics tool reports `cellPt 67.68` for both 5x5 tutorial boards, above the required 48 dp. Evidence: `artifacts/W1-05/viewport/cell-metrics.txt` and screenshots `artifacts/W1-05/viewport/t1-360x640.png`, `artifacts/W1-05/viewport/t2-360x640.png`.
- **INFERRED:** `67.68 dp` is ready for W1-10 to record in its own future task document; W1-05 does not pre-edit W1-10's report.
- **INFERRED:** The brief's `514x1114 dp` native target is stale. W1-01's controller amendment established actual native `411.43x891.43 dp`, which this capture verifies from physical pixels and density.

## Automated verification

- **EXECUTED:** `npx jest --selectProjects ui --runInBand --no-watchman` — 2 suites / 2 tests passed after the final `GameScreen.tsx` edit. Evidence: `artifacts/W1-05/final/jest-ui-final.txt`.
- **EXECUTED:** `npx jest --runInBand --no-watchman` — 36 suites / 626 tests passed. Evidence: `artifacts/W1-05/final/jest-all-final.txt`.
- **EXECUTED:** `npx tsc --noEmit` — passed. Evidence: `artifacts/W1-05/final/tsc-final.txt`.
- **EXECUTED:** `npx tsx scripts/contrast-audit.ts` — 78 gated rows, 0 failures. Evidence: `artifacts/W1-05/final/contrast-audit.txt`.
- **EXECUTED:** `git diff --check` — passed before report creation and again in the final worktree check.

## Owner acceptance needed for W1-11

- `artifacts/W1-05/viewport/t1-360x640.png` — **YES:** approve `Tap an arrow.` verbatim. **NO:** supply replacement wording or select the wordless alternative.
- `artifacts/W1-05/viewport/t2-360x640.png` — **YES:** approve `One arrow is free. Find it.` verbatim. **NO:** supply replacement wording or select the wordless alternative.
- `artifacts/W1-05/post/p02/w105-normal-final/first-blocked-120ms.png` — **YES:** approve `Blocked. Clear what's in its way.` verbatim. **NO:** supply replacement wording or select the wordless alternative.

## Concerns and unverified scope

- **CONCERN:** The brief's reduced-motion screenshot requirement at at least 1 s conflicts with W0-04's binding 690 ms blocker lifetime. Evidence captures the static state while mounted at 200 ms and its expected absence after 1 s; timing was intentionally not changed.
- **UNVERIFIED-DEVICE:** Fredoka wrapping on physical phones at non-default font scale.
- **UNVERIFIED-DEVICE:** TalkBack and VoiceOver behavior.
- **UNVERIFIED-DEVICE:** iOS and physical Android hardware; all behavioral captures were on the assigned Android emulator.

## Final emulator state

- **EXECUTED:** `emulator-5556` remains running with the final capture APK focused, physical size/density restored to `1440x3120 @ 560`, and animation scales restored to `1/1/1`. Evidence: `artifacts/W1-05/final/emulator-state.txt`.
- **EXECUTED:** The emulator was not booted, killed, wiped, or snapshotted by this task. App data was cleared once while preparing a fresh tutorial viewport measurement.
- **EXECUTED:** No commit was created.
