# W6-12 + W6-13 report

- **Status:** DONE.
- **Base (HEAD before the first change):** `79a71bc14bccff3573582c75a65d9ba13491a8c0` on `next-level`.
  EXECUTED `git status --porcelain` at base: clean.
- **Commits:**
  - `823b772` docs(W6-12): decision rules each telemetry metric must resolve
  - `0895b04` docs(W6-13): analytics disclosure draft for W7-05/W7-06 input
- **Head:** `0895b0413e4161b851d7e3747c2150067298478b`

Both tasks are documentation-only (`Flag: none`), touch no runtime code, and
have no TDD or device verification requirement — both briefs say so
explicitly ("TDD: not applicable", "UNVERIFIED-DEVICE: not applicable").
Nothing in this batch required a native build, an emulator, or the JS repack
recipe.

## What I implemented

### W6-12 — `docs/telemetry-decision-rules.md` (new)

One row per decision named in the brief (W1 tutorial-teaches, W3 on-ramp,
W3 curve-rises, W4 daily-retention, ads shown/not-ready/killed), each
carrying: the exact `EventSchema`/`TelemetryEnvelope` fields used, the
computation, a minimum sample size and time window (each marked
`// OWNER-PICKED STARTING VALUE`), the action for each result (stated as
"report the measured value to the owner" rather than a chosen pass/fail
number, per the brief's non-goal "choosing thresholds on the owner's
behalf"), and what the row cannot tell. A closing table greps every named
field against `src/telemetry/events.ts` by line number.

### W6-13 — `docs/analytics-disclosure-draft.md` (new)

Proposed Analytics policy text; a 39-row Play Data safety table (one row
per `EventSchema` field, verified by script below) plus one additional row
for the install id; the kill-switch config request's IP/UA exposure
(quoting `docs/remote-config.md`'s existing privacy note); the four
endpoint data rules W6-15 must enforce; and seven open questions for the
legal reviewer. `store/privacy-policy.md` and `docs/privacy-policy.html`
are untouched.

## Pre-fix reproduction (both are new-document tasks — "pre-fix" reads as "the gap that makes the document necessary")

- **W6-12:** EXECUTED — before this document, no file in the repo stated how
  any W1/W3/W4/ads metric would be computed or what sample size/window
  applied; `docs/next-level/tasks/W6.md` and the SDD ledger only assert that
  such rules are needed. Grepping the repo for `telemetry-decision-rules`
  before this task found nothing (the file did not exist).
- **W6-13:** EXECUTED — `store/privacy-policy.md:14` was read at base and
  quoted verbatim in the new document ("The app has no accounts, no
  sign-in, and collects no personal information itself.") as the sentence
  that must change no later than the W6-15 release. `git diff --stat` for
  `store/` and `docs/privacy-policy.html` between base and head is empty,
  confirming that sentence is still unedited at head.

## Verification

### W6-12 acceptance criteria

1. **"Every row names only fields present in `src/telemetry/events.ts`."**
   EXECUTED — each field cited in the five decision rows (`app_open.cold`,
   `TelemetryEnvelope.sessionIndex`, `TelemetryEnvelope.ts`,
   `level_end.levelIndex/outcome/taps/tapsExit/tapsBlocked/heartsLost/durationMs/mode`,
   `ad_request.format/placement`, `ad_result.format/placement/outcome`) is
   grepped against `events.ts` with a line number in the document's closing
   table; I ran each grep myself while writing the table (see the raw
   command/output pattern used for W6-13's field check below — the same
   method, applied by hand per field since W6-12's field list is short).
2. **F36 (pre-flight, binding): the W1 `tapsBlocked`-before-first-`tapsExit`
   sub-metric cannot be computed from `level_end`'s totals.** Confirmed by
   reading `events.ts:76-80`: `taps`, `tapsExit`, `tapsBlocked`, `tapsGhost`,
   `tapsMiss` are all `num()` — per-level totals, with no field recording
   tap order or per-tap timestamps. **Decision (recorded below): dropped**,
   not added to the schema, because this batch's Files list is a single new
   document, and the row's other metric (level-0 cleared-fraction) already
   answers the same underlying W1 question without needing tap order.
3. **"No row contains an unlabelled number."** Every numeric sample-size and
   window value is followed by `// OWNER-PICKED STARTING VALUE`; band width
   in the curve row is likewise marked. INFERRED (read back my own file) —
   there is no automated linter for this, so this is a manual re-read, not
   a tool run.
4. **"Every row states its non-inferences."** Each row has a "What it cannot
   tell" cell — verified by re-reading the file (INFERRED).

### W6-13 acceptance criteria

1. **"Every field in `EventSchema` appears in exactly one table row, and
   every row maps to a field."** EXECUTED with a script, not by eye:
   - Ran a Node script that parses `schemaDef` out of `src/telemetry/events.ts`
     with a regex over the object literal and lists every `(event, field)`
     pair it finds → **39 pairs**, in event-declaration order (command run;
     output captured to `/tmp/schema_pairs.txt`, a scratch file, not
     committed).
   - Counted the data rows in the draft's Play Data safety table with an
     `awk` one-liner scoped to the header-to-next-non-`|`-line span →
     **39 rows**.
   - Diffed the two lists by eye (39 vs 39, same order, same names) — they
     match field-for-field: `app_open.{cold,sessionIndex,appVersion,bucket}`,
     `screen_view.screen`, `level_start.{levelIndex,arrowCount,shapeName,heartsMax,mode}`,
     `level_end.{levelIndex,mode,outcome,taps,tapsExit,tapsBlocked,tapsGhost,tapsMiss,heartsLost,heartsLeft,heartsMax,durationMs,hintsUsed,continuesUsed}`,
     `session_end.{durationMs,levelsStarted,levelsCleared,lastLevelIndex}`,
     `ad_request.{format,placement}`, `ad_result.{format,placement,outcome}`,
     `ad_reward.{placement,earned}`, `error.{kind,nameMessageHash,screen,count}`.
   - The document originally said "37 rows" in its own prose while the table
     already had 39 — caught by the same `awk` count and corrected before
     committing (self-review catch, not a reviewer catch).
   - The install-id row is the one row not derived from `EventSchema` at
     all; the document says so explicitly and explains why (the schema
     comment at `events.ts:148-149` says the id is transport-attached, never
     part of the envelope type), so it is not counted against this
     criterion.
2. **"`git diff --stat` shows no change to `store/` or
   `docs/privacy-policy.html`."** EXECUTED: `git diff --cached --stat --
   store/ docs/privacy-policy.html` printed nothing, both before and after
   the W6-13 commit.

## Decisions (routine judgment calls made without stopping to ask)

1. **W6-12, F36's uncomputable W1 sub-metric: dropped, not schema-extended.**
   The brief's own acceptance criterion offers exactly two resolutions —
   add a field to `events.ts` in the same PR, or drop the row. I dropped it.
   Reasoning: this batch's Files list for W6-12 is a single new document;
   adding an ordering/timestamp-per-tap field to `EventSchema` is a schema
   change with emission-site and schema-version implications for other,
   already-shipped tasks (W6-04/W6-05), and is a bigger change than "write
   decision rules." The row's other named metric (fraction of first-`app_open`
   installs reaching `level_end{cleared}` on level 0) already answers "does
   the tutorial teach" without needing tap order, so nothing about the
   underlying decision goes unmeasured — only the specific "before" framing
   is gone. If a future task adds an ordered tap log for an unrelated
   reason, this row should be revisited to use it.
2. **W6-12, install id as a join key, not an `EventSchema` field.** Rows 1
   and 4 need to group events "by install" to compute a fraction or a
   before/after comparison. `TelemetryEnvelope` deliberately excludes the
   install id (`events.ts:148-149`). I listed the install id as a separate
   "join key" note per row, explicitly not counted against the "only fields
   present in `events.ts`" criterion, rather than either (a) silently
   assuming a field that doesn't exist, or (b) refusing to write rows that
   need cross-event grouping at all. Same reasoning applied to W6-13's Data
   safety table, where the brief itself names "Device or other IDs for the
   install id" as a required row — so there I added it as the one explicit
   exception the brief itself calls for.
3. **W6-12, bands/thresholds are not chosen.** The brief's non-goal
   "Choosing thresholds on the owner's behalf" reads as: pick a starting
   sample size and window (both marked OWNER-PICKED, per the acceptance
   criteria), but do not pick the pass/fail cutoff that turns a measured
   number into a verdict. Every row's "action" cell reports the measured
   value(s) to the owner rather than computing a go/no-go itself.
4. **W6-13, retention number.** The brief asks for "retention, as an
   OWNER-PICKED STARTING VALUE" without naming one. I picked 180 days for
   raw events, marked explicitly as needing an owner yes, listed again as
   open question 1.
5. **W6-13, "Optional" column is "No" on every row.** There is no analytics
   opt-out UI in the app (W7-03 is out of scope for this task). Rather than
   guess at a nuanced per-row answer, I set every row to "No" and raised it
   as open question 2 — whether shipping without an opt-out is acceptable
   for this release, or whether W7-03 needs to land alongside W6-14/W6-15.
6. **W6-13, purpose column is "Analytics" uniformly.** All 39 fields belong
   to one channel with one purpose; a per-field purpose distinction (e.g.
   treating `error.*` as "App functionality" instead of "Analytics") looked
   like manufactured precision the brief didn't ask for, so I kept one
   value and let the category column (App activity vs App info and
   performance) carry the real distinction.

## Gates

- EXECUTED `npx jest`: **25 suites / 543 tests passed**, output ends
  "Ran all test suites." with no failures. This batch added no test files
  (documentation only), so this is the same count the branch already had at
  base; it confirms the two new commits did not touch or break any suite.
- EXECUTED `npx tsc --noEmit`: exit code `0`, no output.
- EXECUTED `git status --porcelain`: empty after both commits.
- F33 (jest --selectProjects ui after a task touching `GameScreen.tsx` or
  `ads.tsx`) does not apply: neither file was touched by this batch.

## Self-review findings

- Caught and fixed before committing: W6-13's prose said "37 rows" while
  the table already had 39 (see Verification §W6-13 item 1). Fixed to 39
  and re-verified with the same `awk` count before the commit that is now
  in the log — there is no separate "fix commit" because this was caught
  pre-commit, not post-review.
- Re-read both documents once end-to-end after writing them, checking each
  brief's acceptance-criteria line against the document text directly
  (rather than trusting my own memory of what I wrote).

## Concerns

- None that block DONE. The one genuine judgment call with a plausible
  alternative reading is Decision 1 (drop vs. extend the schema for the W1
  ordering sub-metric) — flagged above with the reasoning, in case the
  owner or a reviewer would rather see the field added instead.

## Owner acceptance needed

- `docs/telemetry-decision-rules.md`: every minimum sample size, time
  window, and the curve row's band width are `OWNER-PICKED STARTING VALUE`s
  (200/500 events, 7/14/30-day windows, bands of 5) — needs an owner yes or
  replacement numbers before these rules are used to gate a real decision.
- `docs/telemetry-decision-rules.md`, Decision 1 above: accept the dropped
  W1 tapsBlocked-before-tapsExit sub-metric (recommended), or ask for an
  ordering/timestamp field to be added to `EventSchema` in a follow-up task
  instead.
- `docs/analytics-disclosure-draft.md`, §1: the 180-day raw-event retention
  figure is an `OWNER-PICKED STARTING VALUE` — needs an owner yes or a new
  number before W7-05 uses this draft.
- `docs/analytics-disclosure-draft.md`, §5 (open questions 1-7): all seven
  need a legal reviewer's answer before W7-05/W7-06 finalize the published
  policy and Data safety form. None of these are decisions this task made
  on the owner's or legal's behalf — they are surfaced, not answered.
