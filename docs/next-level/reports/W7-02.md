# W7-02 report: GATE 0, certified CMP on RN 0.86.3 with the new architecture

- **Status:** BLOCKED-ON-DISK. No candidate was installed, built or run.
- **Date:** 2026-09-16
- **Branch:** `next-level`. Base commit `7c83d4d`. This report is the only commit.
- **Result in one line:** The pre-build checks ran: npm metadata and Google's certified-CMP list. The GATE 0 question is still open. Does a certified CMP compile and run in a release build on RN 0.86.3 with the new architecture? That needs a native release build, and the disk is below the 5 GB gate.

## Why this is blocked

- EXECUTED: `df -h /` at 2026-09-16T14:47:37Z showed **1.8Gi available**. `diskutil info /` showed Container Free Space 1.9 GB (1935519744 bytes). Saved to `artifacts/W7-02/disk-precheck.txt`.
- The gate is ≥ 5 GB before a native build. That is the brief's Change step 1 and the global run rule (OWNER-PICKED STARTING VALUE). The run rule also says: "Never delete files outside the repo to free space — report BLOCKED instead."
- EXECUTED: I measured what I could free inside the repo (`du -sh`).
  - `node_modules`: 7.3G. The build needs it.
  - `ios`: 1.2G. It is gitignored and generated.
  - `artifacts`: 81M. It holds the shared base APK and earlier tasks' evidence.
  - `android`: 18M. Build outputs were already removed.
  - Even deleting all of `ios/` would reach about 3.1 GB, which is still under the gate. I deleted nothing.
- INFERRED (read from earlier reports, not re-measured): free space has dropped across this run. It was 11Gi before P-04a, 5.3Gi at P-01, 4.1Gi at P-05 and 1.8Gi now. What used the space outside the repo was not investigated, since reading or freeing it is outside this task. The AVD directory `~/.android/avd/fleet_floor_api31.avd` measures 4.9G (EXECUTED `du -sh`). No earlier value exists to compare with.
- EXECUTED: `adb devices` lists **two** emulators, `emulator-5556` and `emulator-5580`. The run rules only mention 5556. I did not touch either one.

## Pre-fix reproduction (brief Verification 1): the starting state

The gap is that no compatibility evidence exists. EXECUTED `npm view <pkg> version time.modified peerDependencies codegenConfig dependencies --json` at 2026-09-16 ~14:47Z, saved to `artifacts/W7-02/npm-metadata.txt`. Project: `react-native` 0.86.3, `expo` ~57.0.18, `newArchEnabled=true` (`android/gradle.properties:38`), `@expo/config-plugins` 57.0.9 installed.

| Package | Version | Last modified | peerDependencies | codegenConfig (new-arch spec) | Runtime deps |
|---|---|---|---|---|---|
| `react-native-google-mobile-ads` | 16.5.0 | 2026-08-18 | `expo >=47.0.0` | yes: `RNGoogleMobileAdsSpec`, type `all`, `./src/specs` | `@iabtcf/core ^1.5.6`, `use-deep-compare-effect` |
| `@usercentrics/react-native-sdk` | 2.31.0 | 2026-09-10 | `react *`, `react-native *` | yes: `RNUsercentricsModule`, type `all`, `./src/fabric` | none |
| `@didomi/react-native` | 2.35.0 | 2026-09-14 | `react *`, `react-native >=0.77.0` | yes: `RNDidomiSpec`, type `modules`, `includesGeneratedCode: false` | `@expo/config-plugins ^54.0.2` |
| `react-native-ad-consent` | 5.3.0 | 2024-09-02 | `react-native >=0.60.0` | **none in metadata** | n/a |
| `react-native-google-ump` | 1.0.0 | 2022-05-14 | `react *`, `react-native *` | **none in metadata** | n/a |

What this shows, and what it does not:
- EXECUTED: all three maintained candidates publish a codegen spec. The two stale wrappers publish none.
- INFERRED (not built): a codegen spec means the package targets the new architecture. It does **not** prove the package compiles against RN 0.86.3, and no package states a tested RN range covering 0.86. That is the question GATE 0 exists to answer, and it is still open.
- INFERRED (not built): `@didomi/react-native` depends on `@expo/config-plugins ^54.0.2`, while the project has 57.0.9. npm would nest a second copy of config-plugins for its plugin. Whether its config plugin then works under SDK 57 prebuild is unknown until someone runs it.

## Certified-CMP list check (brief step 6, done ahead of the build)

EXECUTED: `curl -sSL 'https://support.google.com/admanager/answer/13554116?hl=en'` returned HTTP 200 with 1,651,626 bytes at 2026-09-16T14:48Z. The HTML was stripped to text and searched. Excerpts are in `artifacts/W7-02/certified-cmp-list-check.txt`. Rows as they appear (name / TCF CMP ID / surfaces):

- `Google LLC CMP` / 300 / `Web and app`. This is Google's own UMP, reached by `react-native-google-mobile-ads`'s consent API.
- `UserCentrics CMP` / 5 / `Web, app, and CTV`
- `Didomi CMP` / 7 / `Web, app, and CTV`

INFERRED: the list certifies a CMP vendor, not a specific React Native wrapper. `react-native-ad-consent` and `react-native-google-ump` are third-party wrappers around Google's UMP SDK. The certified CMP there is still Google's, but the wrappers are unmaintained (see the table above). The page's text says the list is updated weekly and shows no date, so this check holds only for 2026-09-16.

## What was not done (all pending the disk gate)

For each candidate, the brief's steps 1–5 were not run:
- install on a scratch branch;
- `expo prebuild --clean`;
- `expo run:android --variant release`;
- the bridge round-trip call and log;
- the menu screencap;
- APK size;
- `aapt2 dump permissions` diffed against `artifacts/base/da93dcd-release-arm64.apk` (the file exists; its SHA-256 file reads `7556f834…a428`);
- the `releaseRuntimeClasspath` conflicts;
- the temporary unconditional LevelPlay init log.

`docs/cmp-gate0-2026-09-16.md` was **not** created. With no candidate built, a PASS/FAIL table would have only "not run" rows. That could be misread as a result, and acceptance 4's "no candidate passes" does not apply to candidates that were never tried.

Acceptance 3 still holds, since nothing was installed. EXECUTED `git diff da93dcd..HEAD -- package.json app.json` before this commit. It printed one hunk, P-04c's `ios.supportsTablet` `true` → `false`. `| grep -ciE 'consent|mobile-ads|usercentrics|didomi|ump'` printed `0`, so there is no CMP dependency.

## Evidence list

- EXECUTED: `git rev-parse HEAD` returned `7c83d4d9cf9954029499dea2ef8199b0a41ec1f5`. The branch was `next-level` and `git status --porcelain` was empty before work.
- EXECUTED: disk gate check. See "Why this is blocked".
- EXECUTED: npm metadata for 5 packages. See the table.
- EXECUTED: Google certified-CMP list fetch and text search. See above.
- EXECUTED: `npx jest` and `npx tsc --noEmit` results are recorded under "Gates" below.
- UNVERIFIED-DEVICE, and not attempted on the emulator either: build success, menu reach, bridge round-trip, and LevelPlay init in the same process, for every candidate.

## Concerns for the controller (brief vs run rules)

1. **Scratch branch vs "Branch `next-level` ONLY".** The brief puts the dependency install on a local scratch branch that is never merged or pushed, then deleted. The run rules say both "Branch `next-level` ONLY" and "never [install a dependency] for a scratch experiment on next-level". These two readings cannot both hold for this task. A local, never-pushed, deleted scratch branch is the only way to meet both. The rerun needs a ruling that allows it. (Uncommitted edits on `next-level` would break the second rule.)
2. **`npm install` (brief step 2) vs `npx expo install` (run rules).** `expo install` pins an SDK-compatible version only for packages in Expo's bundled module list. For packages outside it, it installs the latest version, so the result should match. I recommend `npx expo install` to satisfy both.
3. **The finish recipe needs disk too.** `npm ci` (Risk and rollback) deletes and re-creates a 7.3G `node_modules`. P-05 measured `npm ci` in a fresh clone using about 2.1 GB of net space. On a disk near 2 GB, a failed `npm ci` would leave the main checkout without a working `node_modules`. The rerun should confirm headroom for the rollback as well as the build before starting.
4. **Two emulators attached.** `emulator-5580` is present alongside `emulator-5556`. The rerun must pass `--device emulator-5556` and `adb -s emulator-5556` everywhere.
5. **Test app id for the UMP candidate.** INFERRED (stable knowledge, not re-read at run time): `react-native-google-mobile-ads`'s config plugin requires an Android app id. Google's documented sample AdMob app id is `ca-app-pub-3940256099942544~3347511713`. The rerun must confirm it against Google's docs page at that time, as the brief requires, and never use an owner id.

## Owner / controller actions needed to finish

- Free disk until `df -h /` shows ≥ 5 GB (≥ 5 GB plus about 2 GB is safer, for the `npm ci` rollback). This must happen outside the repo, which the agent may not touch. Then re-dispatch W7-02.
- Rule on concern 1, the scratch branch.
- The recommendation for W7-03 (UMP vs commercial CMP vs no ads in the EEA/UK) cannot be made from this run. The metadata and list check do not rank the candidates.

## Gates

- EXECUTED `npx jest`: 13 of 13 suites and 129 of 129 tests passed. This matches P-05's recorded counts, and no app code changed.
- EXECUTED `npx tsc --noEmit`: exit 0.
- EXECUTED `df -h /` at the end showed 2.0Gi available, still under the 5 GB gate.
- `git status --porcelain` must be clean after the commit. `artifacts/W7-02/` is ignored by `.gitignore:52` (`/artifacts/`), as EXECUTED `git check-ignore -v` confirmed.
