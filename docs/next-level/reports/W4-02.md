# W4-02 — forgiving day streak with automatic freezes

- **Status:** DONE_WITH_CONCERNS
- **Branch / inspected HEAD:** `next-level` / `f6831a6`
- **Implementation:** uncommitted, as required by the Codex global constraints; Claude owns git.
- **Flag:** `META_STREAK_FREEZE`, default OFF through
  `process.env.EXPO_PUBLIC_META_STREAK_FREEZE === '1'`.
- **Evidence root:** `artifacts/W4-02/` (gitignored).

## What changed

- `src/core/streak.ts` adds the pure `advanceStreak` and `readStreak` rules. With the flag OFF they
  reproduce the shipped arithmetic. With it ON they spend enough freezes to cover all missed days,
  preserve the bank when it cannot cover the gap, handle a backwards clock without spending, and
  earn one freeze on positive multiples of 7 up to a cap of 2.
- `src/core/saveSystem.ts` adds the injectable `useClock`, public local-day `today`, bounded
  `streakFreezes`, pending `streakSavedDay`, and its clear operation. `dayStreak` and
  `registerSolve` now delegate their day-chain arithmetic to the pure rules. A rescue writes the
  existing P-01 key `arrows_streak_saved_day = today`.
- `src/featureFlags.ts` adds the controller-amended env-driven flag.
- `src/ui/HomeScreen.tsx` captures the whole stats string in `useState` on mount. With the flag ON,
  a non-zero pending saved day appends `   ·   streak saved` to that existing Text element and an
  effect clears the pending key. A theme rerender keeps the captured text; the next mount does not.
- `src/core/__tests__/streak.test.ts` covers every rule row and runs the same state table through an
  old-formula oracle when the flag is OFF.
- `src/core/__tests__/saveSystem.test.ts` drives gaps 0, 1, 2, 3, and negative through a real
  `SaveSystem` store with an injected clock and asserts the stored day streak, last day, freeze
  bank, and pending saved day.
- `src/ui/__tests__/storage.test.ts` contains the F13-amended cold-start case. It never touches the
  29-key exact-array assertion.
- `src/ui/__tests__/HomeScreen.streak.test.tsx` covers no streak at one solve, exactly one saved copy,
  persistence through a theme rerender, consumption, and absence on the next mount.
- `src/ui/contrastAudit.ts` only moves its seven HomeScreen source-line references to the same
  colour and font-size sites after HomeScreen gained ten lines.

No P-01 registry value, persistence-key array, schema version, or reset set changed. The two keys
were already registered by P-01. Migration is therefore additive/absent-key-default only: existing
chains are not rewritten at boot, and flipping the flag OFF leaves banked freezes inert. There is no
new wipe risk.

## Pre-fix reproduction

- **EXECUTED:** archived only `src/core/saveSystem.ts` from `da93dcd` into an `mktemp` directory with
  `git archive`; the working tree was never checked out or changed.
- **EXECUTED:** ran the archived module with `npx tsx -e` and a Map-backed `IntStore`. On local day
  2454 it seeded `arrows_day_streak=14` and `arrows_last_play_day=2452`. `SaveSystem.dayStreak`
  printed `0`; after `registerSolve(true)`, the stored streak printed `1`.
- Evidence: `artifacts/W4-02/pre-fix-reproduction.txt`.

## TDD evidence

- **EXECUTED RED:** before forgiveness logic, the pure helper contained only the shipped
  characterization arithmetic. `npx jest src/core/__tests__/streak.test.ts --runInBand` gave
  1 failed / 11 passed. The only failure was gap 2 with one freeze: expected
  `{streak:15, freezes:0, savedToday:true}` and received the shipped reset
  `{streak:1, freezes:1, savedToday:false}`. Every flag-OFF old-formula row passed.
- Evidence: `artifacts/W4-02/tdd-red-streak.txt`.
- **EXECUTED GREEN:** after implementation, the final required command
  `npx jest src/core/__tests__/streak.test.ts src/core/__tests__/saveSystem.test.ts` passed
  2 suites / 51 tests.
- **EXECUTED:** the pure enabled table covers gap 0, gap 1, covered/uncovered gap 2,
  covered/uncovered gap 3, first solve, negative gap, earning at 7, earning at 14 through a rescue,
  cap 2, and never spending below 0. Its 11 flag-OFF rows compute expected outputs by executing the
  old formulas in the test rather than copying expected numbers.
- Evidence: `artifacts/W4-02/tdd-green-and-gates.txt`.

## Verification

### Persistence and code gates

- **EXECUTED:** `npx jest src/ui/__tests__/storage.test.ts` — 1 suite / 13 tests passed. The W4-02
  cold-start row hydrates a live 14-day chain with no freeze keys, reads 14, stores 15 after a solve,
  and reads 0 freezes.
- **EXECUTED:** `npx jest` — 45 suites / 705 tests passed across the core and UI projects.
- **EXECUTED:** `npx tsc --noEmit` — exit 0, no output.
- **EXECUTED:** `git diff --check` — exit 0, no output.
- **EXECUTED:** the required banned-copy grep returned no matches.
- **EXECUTED:** the first full Jest run caught seven stale line-number pointers in the existing
  contrast instrument (1 failed suite, 7 failed / 698 passed). After the mechanical pointer update,
  the focused contrast suite passed 142/142 and the final full run passed as above.
- **EXECUTED:** the UI project ran in the final full gate: 3 suites / 4 tests passed. F33's extra
  condition does not apply because neither `GameScreen.tsx` nor `ads.tsx` was edited.

### Emulator visuals

- **EXECUTED:** used P-02's `scripts/perf/android/capture.mjs` on `emulator-5556`, AVD
  `fleet_floor_api31`, API 31. Every final capture requested and read back `1080x1920 @480`
  (360x640 dp), then reset to the physical `1440x3120 @560`.
- **EXECUTED:** every final capture requested and read back window/transition/animator scales
  `0/0/0`, force-stopped and relaunched, and reported `appReducedMotion=true` through the app
  diagnostic. Handback restored/read back `1/1/1`.
- **EXECUTED:** the final PNGs are all exactly 1080x1920 pixels:
  - `artifacts/W4-02/captures/W4-02-on-fresh-one-solve/W4-02-on-fresh-one-solve.png` — one solve,
    `1 puzzle solved`, no streak text.
  - `artifacts/W4-02/captures/W4-02-on-saved-first-mount/W4-02-on-saved-first-mount.png` — the
    existing stats line reads `15 puzzles solved · 15-day streak · streak saved`, with the phrase
    exactly once.
  - `artifacts/W4-02/captures/W4-02-on-saved-next-mount/W4-02-on-saved-next-mount.png` — the next
    mount keeps the 15-day streak and omits the saved copy.
  - `artifacts/W4-02/captures/W4-02-off-saved-seed/W4-02-off-saved-seed.png` — the same pending seed
    with the flag OFF omits the saved copy.
- **EXECUTED:** after the first flag-ON mount, reinstalling the debuggable host and querying the real
  RKStorage database returned `arrows_streak_saved_day|0`.
- **EXECUTED:** UIAutomator dumps at the capture geometry contain 23 nodes both flag ON and flag OFF.
  The ON dump contains `streak saved` once and the OFF dump zero times. Thus the feature changes the
  existing stats Text only; it adds no element.
- Evidence summary and exact APK hashes: `artifacts/W4-02/visual-summary.txt`. Each capture directory
  also contains P-02's `manifest.json` with serial, AVD, API, wm read-back, all three motion scales,
  relaunch, app-reported reduced motion, APK sha256, git state, and build env.
- **INFERRED:** no new icon, colour role, or animation exists: the diff appends a string to the
  existing line, and the equal UIAutomator node counts support that source reading.
- **UNVERIFIED-DEVICE:** whether the transient copy feels calm rather than warning-like on a physical
  phone. The API 31 software-GPU capture is the stand-in.
- **UNVERIFIED-DEVICE:** real on-device day rollover. The controller forbids `adb root`, so the
  system clock was not changed; injected-clock pure and SaveSystem tests close the arithmetic.

P-02 Stage D's kill condition did not fire, but no calibrated pixel-floor claim is made here. This
acceptance is the requested text/state capture plus an exact native element-count comparison.

## Decisions

1. `useClock` returns the previous function, matching `useStore`, so tests and later W4 consumers
   can restore scoped clocks without a separate reset API.
2. Enabled-mode freeze reads are bounded to integer `0..cap`; disabled mode bypasses normalization
   so the old arithmetic and inert stored value are exact. An insufficient bank is never partially
   spent.
3. HomeScreen captures the complete string in the state initializer, while clearing happens in a
   mount effect. That satisfies theme-toggle stability without changing persistence during render.
4. The capture states were seeded at the real AsyncStorage SQLite boundary. The arithmetic that
   produces those states is independently exercised through `SaveSystem` with the injected clock.
5. Final APKs were JS-only Hermes repacks of P-02's release native host; no Gradle/native build ran.
   Bundle inspection found `META_STREAK_FREEZE = true` in the ON bundle and `false` in the OFF bundle.

## Concerns and handback

1. **F23 provenance:** the final capture manifests record `dirty:true`. Codex is forbidden to
   commit, so a capture containing this implementation cannot also come from this repository's clean
   committed tree. The env and exact diff are recorded; Claude can repeat the same capture after its
   commit if strict clean-tree provenance is required.
2. The installed release APK at task start used a different signer from the debuggable capture host.
   Replacing it removed P-01's documented synthetic test save; it was not recoverable. The task ends
   with the flag-OFF release repack installed and force-stopped.
3. A failed web fallback started Expo on port 8097. The preview could not produce a screenshot because
   local Chrome aborted with exit 134. `kill 61120` was rejected by sandbox process controls, so that
   detached preview remains listening. Port 8081 was stopped and its adb reverse was removed.
4. **EXECUTED handback:** emulator state is `device`; physical wm is `1440x3120 @560`; animation
   scales are `1/1/1`; the emulator was left running.

## Owner acceptance needed

- Review
  `artifacts/W4-02/captures/W4-02-on-saved-first-mount/W4-02-on-saved-first-mount.png` and answer:
  **Does the transient stats line read as calm rather than a warning, and should
  `META_STREAK_FREEZE` be flipped on? yes/no.**

