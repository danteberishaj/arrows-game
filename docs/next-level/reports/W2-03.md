# W2-03 report: lose panel waits for the fatal blocker explanation

- **Status:** DONE_WITH_CONCERNS. Acceptance criteria 1–5 pass. Criterion 6, the owner's
  beat-versus-hang judgment, remains open.
- **Base:** `958df3620ea9255d996968ba1b104ac1bdeed11c` on `next-level`.
- **Device:** `emulator-5556` / `fleet_floor_api31` / API 31. Claude booted it; this task
  left it running.
- **Evidence root:** `artifacts/W2-03/`.

## What changed

`src/ui/feedbackCurves.ts`
- Exported `FEEDBACK_CLEANUP_MARGIN_MS = 40`, naming the existing cleanup margin without
  changing its value.

`src/ui/BoardView.tsx`
- Both existing feedback cleanup lifetimes now use `FEEDBACK_CLEANUP_MARGIN_MS`: the
  `BLOCKED_BUMP_MS` lifetime and the `BLOCKER_FLASH_MS` lifetime.

`src/ui/gameSessionLifecycle.ts`
- Exported `LOSE_PANEL_DELAY_MS = BLOCKER_FLASH_MS + FEEDBACK_CLEANUP_MARGIN_MS` (690 ms).
- Exported `WON_PANEL_DELAY_MS = 450`, naming the existing win delay.

`src/ui/GameScreen.tsx`
- Campaign and tutorial clears use `WON_PANEL_DELAY_MS`.
- Campaign loss and tutorial re-deal use `LOSE_PANEL_DELAY_MS`, satisfying controller ruling F17.
- `Ads.registerGameFinished()` remains synchronous and unchanged.

`src/ui/__tests__/gameSessionLifecycle.test.ts`
- Added the required arithmetic/order assertion and a source check rejecting numeric literals in
  `beginTerminalTransition('won'|'lost', ...)` calls.

No feature flag is added because this is a defect fix. No persistence, native code, dependency,
payload, ad pacing, win timeline, or panel entrance motion changed.

## Pre-fix reproduction

- **EXECUTED:** Before the first tracked source edit, ran:
  `npm run capture:android -- --motion-scale 1 --apk artifacts/P-02/apk/base-da93dcd-perf.apk --record pre-da93dcd-motion --seconds 5 --frames --step lost --out-root artifacts/W2-03`.
- **EXECUTED:** The APK SHA-256 was
  `fea8a9390cc539cd82eed3738dcf139654ff391b8cc9f87097d3743ac7d12ec0`.
  The capture tool force-stopped and relaunched after setting the three animation scales, then read
  back `1 / 1 / 1`. The old APK predates P-02's in-app reduced-motion label, as P-02 documents.
- **EXECUTED:** `artifacts/W2-03/analyse-lost-timeline.mjs` measured the first overlay at frame 70,
  2.350289 s, with 270 blocker-pink pixels still present. The blocker remained through frame 73,
  2.395544 s. There were **4 overlap frames**, and the last blocker frame was **45.255 ms after**
  the first overlay frame. It cleared 63.300 ms after overlay onset.
- **EXECUTED:** Evidence:
  `artifacts/W2-03/pre-da93dcd-motion/pre-da93dcd-motion.mp4`,
  `artifacts/W2-03/pre-da93dcd-motion/timeline-analysis.json`, and
  `artifacts/W2-03/pre-da93dcd-motion/overlay-overlap-strip.png`.

## TDD evidence

- **EXECUTED RED:**
  `npx jest src/ui/__tests__/gameSessionLifecycle.test.ts --runInBand` failed before implementation
  with `TS2305` for missing `FEEDBACK_CLEANUP_MARGIN_MS`, `LOSE_PANEL_DELAY_MS`, and
  `WON_PANEL_DELAY_MS`. Log: `artifacts/W2-03/tdd-red.txt`.
- **EXECUTED GREEN:** The same command passed **1 suite / 7 tests** after implementation.
  Log: `artifacts/W2-03/tdd-green.txt`.
- **EXECUTED:** `rg -n "beginTerminalTransition\\(" src/ui/GameScreen.tsx` shows all three calls
  use `WON_PANEL_DELAY_MS` or `LOSE_PANEL_DELAY_MS`; no call contains a numeric delay.

## Candidate build and device verification

The fix is JS-only. The device candidate is a JS repack into P-02's SDK-57 PERF host,
`artifacts/P-02/apk/stageB-game-perf.apk`, using `expo export:embed`, Hermes, zipalign, and the local
debug key. No Gradle/native build ran. The exact harness environment came from
`benchmark.mjs`'s `perfBuildEnv(parseArgs(['--feedback']))`.

- **EXECUTED:** Candidate:
  `artifacts/W2-03/apk/w203-fixed-perf.apk`, SHA-256
  `59152c308d4447403bf34116d0406ecb880a138c73794802b7e431d93184c217`.
  `apksigner verify --print-certs` passed and identified the Android Debug certificate.
- **EXECUTED:** Packaging recipe and environment:
  `artifacts/W2-03/repack-perf.sh` and `artifacts/W2-03/apk/w203-fixed-perf.apk.env.txt`.

### Motion on

- **EXECUTED:** `capture.mjs` read back `1 / 1 / 1`; the running app reported
  `appReducedMotion: false` after relaunch.
- **EXECUTED:** The last blocker-magenta frame was frame 86 at 2.595967 s. The first overlay frame
  was frame 92 at 2.694044 s with zero blocker pixels. The overlay therefore began **98.077 ms
  after** the last blocker-magenta frame, with zero overlap frames.
- **EXECUTED:** Evidence:
  `artifacts/W2-03/post-motion/post-motion.mp4`,
  `artifacts/W2-03/post-motion/timeline-analysis.json`, and
  `artifacts/W2-03/post-motion/owner-beat-motion-strip.png`.

### Reduced motion

- **EXECUTED:** `capture.mjs` set all three scales, force-stopped and relaunched, read back
  `0 / 0 / 0`, and the running app reported `appReducedMotion: true`.
- **EXECUTED:** The first overlay frame and last solid-blocker frame were both frame 15 at
  2.700989 s. The blocker was absent on the next recorded frame, frame 16 at 2.716811 s:
  **15.822 ms** after overlay onset, within the recording's **33.389 ms median frame period**.
  This meets the brief's explicit one-recorded-frame allowance.
- **EXECUTED:** Evidence:
  `artifacts/W2-03/post-reduced-motion/post-reduced-motion.mp4`,
  `artifacts/W2-03/post-reduced-motion/timeline-analysis.json`, and
  `artifacts/W2-03/post-reduced-motion/reduced-motion-cleanup-strip.png`.

### Input remains locked

- **EXECUTED:** `artifacts/W2-03/verify-locked-tap.mjs` used the required fatal cells
  `(35,19)`, `(31,14)`, and `(30,21)`, then scheduled the harness plan's first exitable arrow
  `(3,16)` 500 ms after the fatal tap. Host-observed tap-start latency after the fatal tap command
  completed was 509 ms.
- **EXECUTED:** The accessibility label was `250 left` before the fatal sequence and still
  `250 left` after the attempted exitable tap and panel appearance. Computed removals: **0**.
  `Out of hearts` was present.
- **EXECUTED:** Evidence: `artifacts/W2-03/locked-tap/summary.json`,
  `artifacts/W2-03/locked-tap/locked-tap.mp4`, and the before/after PNG/XML pairs in that directory.

### Workload validation

- **EXECUTED:**
  `npm run perf:android -- --feedback --skip-build --apk artifacts/W2-03/apk/w203-fixed-perf.apk --runs 1 --warmups 0 --phases blocked --motion-scale 1 --label W2-03-validate --output artifacts/W2-03/perf-validate.json`
  exited 0. It printed `validating deterministic workload`, reached the measured run, and wrote the
  result, proving the loss-panel assertion still completes inside the harness's 800 ms wait.
  Log: `artifacts/W2-03/perf-validate.log`.

## Gates

- **EXECUTED:** `npx jest --runInBand`: **39 suites / 646 tests passed**.
  Log: `artifacts/W2-03/jest-all-final.txt`.
- **EXECUTED (F33):** `npx jest --selectProjects ui --runInBand`: **2 suites / 2 tests passed**.
  Log: `artifacts/W2-03/jest-ui-final.txt`.
- **EXECUTED:** `npx tsc --noEmit`: exit 0. Log: `artifacts/W2-03/tsc-final.txt`.
- **EXECUTED:** `git diff --check`: exit 0.
- **EXECUTED:** `docs/next-level/reports/W1-10.md` and `artifacts/W1-10/` do not exist, so ruling
  F17's conditional Scenario B re-capture was not triggered.
- **EXECUTED:** Final emulator state in `artifacts/W2-03/emulator-final-state.txt`: `device`, scales
  `1 / 1 / 1`, candidate installed, emulator left running.

Two in-progress full-Jest runs exposed the contrast audit's source-line anchors moving when imports
added lines to `GameScreen.tsx` and `BoardView.tsx`. The imports were kept on the original line count;
no contrast audit file or usage value changed. The final full gate above passed.

## Decisions

1. Reused P-02's capture instrument and existing PERF host instead of adding production-only
   instrumentation. The new artifact analysis isolates the lose title and the fatal blocker region.
2. Kept the lose delay derived from the flash plus cleanup margin. There is no independent 690 ms
   literal to drift.
3. Applied ruling F17 through the shared tutorial/campaign call sites. No W1-10 artifact existed to
   re-capture.
4. Preserved the contrast audit's existing source-line anchors by keeping the changed import blocks
   at their original line counts; widening scope to edit the audit was unnecessary.

## Concerns and limits

- **OWNER ACCEPTANCE OPEN:** the 340 ms longer locked board beat is visible in the motion-on MP4 but
  only the owner can decide whether it reads as intentional or as a hang.
- **UNVERIFIED-DEVICE:** feel on a physical phone. The timeline itself was verified on the required
  emulator release/PERF path.
- **EXECUTED / within tolerance:** reduced motion shows the pale static blocker in the first overlay
  frame and clears it 15.822 ms later. This is within the brief's one-recorded-frame allowance, not a
  claim of strict zero-frame overlap for reduced motion.
- Free disk was 3.5 GiB before the JS repack and 3.3 GiB at finish. No native build was started below
  the 5 GiB gate. `artifacts/W2-03/` occupies 247 MiB.

## Owner acceptance needed (criterion 6)

Watch `artifacts/W2-03/post-motion/post-motion.mp4` (the compact companion is
`artifacts/W2-03/post-motion/owner-beat-motion-strip.png`) and answer exactly:

- **YES — beat:** the longer locked pause reads as an explanatory beat.
- **NO — hang:** the longer locked pause reads as a hang.

If the answer is NO, the brief's fallback is to shorten `BLOCKER_FLASH_MS` while keeping
`LOSE_PANEL_DELAY_MS` derived from the flash plus cleanup margin; do not introduce a new lose-delay
literal.
