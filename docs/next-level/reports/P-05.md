# P-05 report: CI runs jest and tsc on every push to `next-level`

- **Status:** DONE. One step is still open and needs the owner: acceptance 4, the first run on GitHub, happens only after the owner says yes to pushing `next-level` (owner ruling O-2).
- **Base:** `51318c93910a75fadc25b73d0de38152ab08cf65` (`next-level`, clean tree).
- **Commits:** `51eb9a5` ci(P-05): run tsc and jest on every push to next-level, plus the commit that adds this report.
- **Result:** A fresh clone of the committed branch passed `npm ci`, `npx tsc --noEmit` and `npx jest --ci`, all with exit code 0. The counts matched the working copy: 13 of 13 suites and 129 of 129 tests. When I made one assertion false, `jest --ci` exited 1. Nothing has run on GitHub yet.

## What changed

- New file `.github/workflows/ci.yml`, and no other tracked file changed. It does what the brief lists:
  - It runs `on: push: branches: [next-level]` and on `workflow_dispatch`.
  - It sets `permissions: contents: read`.
  - Its concurrency group is `ci-${{ github.ref }}` with `cancel-in-progress: true`.
  - It has one `ubuntu-latest` job with `timeout-minutes: 15`, commented `# OWNER-PICKED STARTING VALUE`.
  - The steps are: `actions/checkout@v7`, then `actions/setup-node@v7` (`node-version: 20.19.4`, `cache: npm`), then `npm ci`, `npx tsc --noEmit` and `npx jest --ci`.
- Action majors, checked on 2026-09-16: `gh api repos/actions/checkout/releases/latest` returned `v7.0.1` (2026-07-20), and `gh api repos/actions/setup-node/releases/latest` returned `v7.0.0` (2026-07-14). `git ls-remote --tags` on both repos agrees. The `action.yml` of `setup-node@v7` still has the `cache` input.
- Outside git (in the ignored `artifacts/` folder): `artifacts/P-05/replica-log.txt`, `artifacts/P-05/yaml-parse.txt` and `artifacts/P-05/import-case-check.txt`.

## Evidence

- EXECUTED: pre-fix reproduction. `ls .github` printed `ls: .github: No such file or directory`. A read-only `gh api repos/danteberishaj/arrows-game/actions/workflows --jq .total_count` returned `0`, and `gh run list --repo danteberishaj/arrows-game` listed nothing. No CI existed, so no run could exist.
- EXECUTED: working-copy baseline before the change. `npx tsc --noEmit` exited 0. `npx jest --ci` passed 13 of 13 suites and 129 of 129 tests. The brief quotes 11 suites and 61 tests "at `da93dcd`". The difference comes from P-01 and P-03, which landed later and added `saveSystem.test.ts` and `noDeadEnd.test.ts`.
- EXECUTED: acceptance 1, local replica (`artifacts/P-05/replica-log.txt`).
  - Command: `git clone --branch next-level /Users/gentlegen/Desktop/Projects/arrows-game <scratchpad>/ci-replica`. The replica's HEAD was `51eb9a5`, the workflow commit, and it had no `node_modules`.
  - `npm ci` (Node v20.19.4, npm 10.8.2) added 811 packages. `patch-package` printed `@shopify/react-native-skia@2.6.2 ✔`. Exit code 0.
  - `npx tsc --noEmit` exited 0.
  - `npx jest --ci` passed 13 of 13 suites and 129 of 129 tests, the same as the working copy. Exit code 0.
  - Why this exercises the path CI will run: these are the workflow's three `run:` commands, in order, on a checkout that has only committed files and lockfile dependencies. So a pass cannot come from the 7.3 GB of warm `node_modules` in the working copy.
- EXECUTED: `df -h /` at each stage. Free space was 4.1Gi before the clone and 2.0Gi after `npm ci`. It was 2.2Gi after the tests. After I removed the replica directory, it was back to 4.1Gi.
  - I removed only `<scratchpad>/ci-replica`, the directory this task created at 14:37Z. No file that existed before this task was deleted.
  - Leaving the directory in place would have left 2.2 GB free on a disk that is already below the 5 GB native-build gate.
- EXECUTED: acceptance 2, failing direction, in the replica only.
  - I changed `src/ui/__tests__/tapRules.test.ts:10` from `toBe(true)` to `toBe(false)` (`git diff` showed exactly that one line).
  - `npx jest --ci` printed `FAIL src/ui/__tests__/tapRules.test.ts`, `Expected: false`, `Received: true`, `Tests: 1 failed, 128 passed, 129 total`, and exited 1.
  - I reverted with `git checkout --`. `git status --porcelain` then had 0 lines, and `jest --ci` passed 129 of 129 again with exit code 0.
- EXECUTED: extra negative control, beyond the brief, for the tsc step. I appended `const p05TypeProbe: number = "not a number"` to the same test file in the replica. `npx tsc --noEmit` printed `error TS2322` and exited 2. After the revert it exited 0. So the tsc step can also fail the job.
- EXECUTED: acceptance 3, the YAML parses (`artifacts/P-05/yaml-parse.txt`). `actionlint` is not installed (`which actionlint` found nothing), and I did not install it.
  - A Node script parsed the file with both parsers already in `node_modules`: `yaml` 2.9.0 in strict mode with unique keys, and `js-yaml` 4.3.2.
  - Under both parsers it confirmed each setting listed under "What changed", including `on.push.branches` equal to `["next-level"]`.
  - Negative control: the same script run on a copy with `branches: [main]` threw `AssertionError ... on.push.branches` and exited 1.
- EXECUTED: a partial check for Linux case-sensitivity (`artifacts/P-05/import-case-check.txt`).
  - Across 63 tracked `.ts`/`.tsx` files, all 149 relative imports resolve to a file whose on-disk casing matches exactly. There were 0 problems.
  - Negative control: `TapRules.test.ts` exists on this case-insensitive Mac, but the exact-case check correctly reports it as missing.
  - This covers relative imports only, not package imports.
- INFERRED: `package-lock.json` has entries for `@img/sharp-linux-x64`, `@img/sharp-libvips-linux-x64` and `lightningcss-linux-x64-gnu`. So `npm ci` on the ubuntu runner has the Linux binaries it needs from the lockfile and does not need a separate download step. Not run on Linux.
- INFERRED: GitHub runs push workflows when a push creates a branch, as long as the pushed commit contains the workflow. `next-level` is not on the remote yet (`git ls-remote --heads origin` lists only `refs/heads/main`), so the first push of the branch should start the first run.
- UNVERIFIED-DEVICE (needs the owner's push): acceptance 4. The following have not been observed:
  - the GitHub-hosted runner downloading Node 20.19.4;
  - `npm ci` and `patch-package` on Linux;
  - the actual run result.
  - `gh run list --branch next-level --workflow ci.yml` must show the pushed sha as `completed / success`, and the run URL must be recorded in a follow-up note.
- EXECUTED: account visibility, read-only. `gh api repos/danteberishaj/arrows-game` gives `gentritr1` the permissions `pull: true, push: true, triage: true, admin: false`, and says the repo is public. `GET .../actions/permissions` returned 403, because that endpoint needs admin. Listing workflows and runs works. So `gentritr1` should be able to read run results with `gh run list`, but cannot see or change whether Actions is enabled on the repo.
- EXECUTED: gates. See the final check below for `npx jest`, `npx tsc --noEmit` and `git status --porcelain`.

## Concerns

- **The brief's counts are out of date.** It says "11 suites / 61 tests at `da93dcd`", but the branch now has 13 suites and 129 tests because P-01 and P-03 added suites. The acceptance criterion ("equal the working copy's") is met. This is only a note.
- **npm version.** `package.json` declares `packageManager: npm@11.16.0`, but local npm and the npm bundled with Node 20.19.4 on the runner are both 10.8.2. Corepack is not enabled, so the field has no effect either locally or in CI, and both run the same npm. I kept the workflow matching the local setup, as the brief asks. If the owner wants npm 11 enforced, that is a separate decision.
- **Actions may be turned off for the repo.** `gentritr1` does not have admin rights, so it cannot check whether Actions is enabled on `danteberishaj/arrows-game`. If the first push starts no run, check that setting first.
- `npm ci` reported 18 audit vulnerabilities (11 moderate, 7 high) and deprecated `glob@7`. These are out of scope and are not a CI gate.

## Owner acceptance needed

- **A yes to push `next-level`** (owner ruling O-2). After the push: `gh run list --branch next-level --workflow ci.yml` must show the pushed sha as `completed / success`. Record the run URL in a follow-up note. That closes acceptance 4.
- **Optional:** confirm the 15-minute job timeout (OWNER-PICKED STARTING VALUE). A cold `npm ci` took about 2 minutes locally, and jest took about 20 s.
- **Optional, per ruling P-14:** whether to add the one-line `node --test scripts/perf/android/*.test.mjs` step. It is not included.

## Final check

- EXECUTED at `c24d662`, the report commit:
  - `npx tsc --noEmit` exited 0.
  - `npx jest` passed 13 of 13 suites and 129 of 129 tests, with exit code 0.
  - `git status --porcelain` printed 0 lines. `--ignored` shows only `artifacts/`.
- The only change in this final commit is this section.
