# W1-04 report: tutorial mode inside GameScreen

- **Status:** DONE_WITH_CONCERNS. The implementation, pre-fix reproduction, Android flows, persisted-key read-back, exact flags-OFF comparison, PERF routing check, Jest gates, and TypeScript gate pass. The concern is that the managed sandbox could not sustain the brief's literal Expo dev-server command, so the same web bundle was verified through a static Expo export in a fresh Android Chrome profile instead.
- **Base (HEAD before first change):** `490817cc68fc3fa1760f614524bb7bf086114cc1` on `next-level`.
- **Evidence directory (gitignored):** `artifacts/W1-04/`.
- No commit, branch change, publish, release, PR, or EAS command was made.

## What changed

- `App.tsx`
  - Computes `ftueRoute` only when Play is pressed, using the exact flag, PERF, stage, level, and solve inputs from the brief.
  - Passes T1/T2 as `tutorialId` into the existing game screen. Splash, menu, PERF boot, and navigation structure remain unchanged.
- `src/ui/gameSessionLifecycle.ts`
  - Adds `mode` and optional `tutorialId` to `LevelSession`.
  - Campaign sessions use `mode: 'campaign'`; `createTutorialSession` uses `buildTutorialLevel`, the persisted campaign index, and `mode: 'tutorial'` without calling `LevelGenerator.generate`.
- `src/ui/GameScreen.tsx`
  - Starts the initial and subsequent level aggregators from `session.mode`.
  - Tutorial clears call aggregator `end('cleared')`, persist stage 1/2 immediately, wait the existing 450 ms, and load T2 or the current campaign level without a panel, campaign progress write, or ad-pacing write.
  - Tutorial terminal losses call aggregator `end('out_of_hearts')`, wait the existing 350 ms, and re-deal the same authored board with three hearts without a panel or ad-pacing write.
  - Tutorial headers show the required two-line-capable copy, keep hearts, and hide the hint control. Normal headers and controls return on the real board.
  - All board changes use the shared session-loading path.
- `src/ui/ftueCopy.ts`
  - Adds the exact constants `Tap an arrow.` and `One arrow is free. Find it.`
- `src/ui/__tests__/gameSessionLifecycle.test.ts`
  - Pins zero generator calls, T1 identity, tutorial mode, and authored arrow count; campaign session mode is also pinned.
- `src/ui/contrastAudit.ts`
  - Mechanically updates the existing GameScreen source-line tripwire after the component grew. No token, role, or contrast gate changed.

### Persistence impact

- **Migration:** none. W1-03 already registered and hydrated `arrows_ftue_stage`.
- **Writes:** tutorial clears write only the existing FTUE stage key. The real level resumes the existing campaign writes.
- **Wipe risk:** none. No key, migration, reset, delete, or backup behavior changed.

## Pre-fix reproduction

- **EXECUTED:** installed `artifacts/base/da93dcd-release-arm64.apk` fresh on the already-running `emulator-5556`, set and read back animation scales `1 / 1 / 1`, and launched the app.
- **EXECUTED:** Play opened `LEVEL 1`, `Normal · Circle · 60 left`, with the hint visible (`pre-fix/02-level1.png`).
- **EXECUTED:** three distinct blocked-arrow taps exhausted the three hearts and opened `Out of hearts` with `Continue +♥ (ad)` inside the first minute (`pre-fix/03-lose-panel.png`, `pre-fix/w104-pre.mp4`, 30.05 s). This is the pre-W0 ad surface named by the brief; W0-01's already-verified TEST AD modal was not re-verified.

## TDD evidence

- **EXECUTED — RED:** added the `createTutorialSession('T1', 0)` contract first and ran `npx jest --selectProjects core src/ui/__tests__/gameSessionLifecycle.test.ts --runInBand`. The intended suite failed because `createTutorialSession` was not exported; 32 other suites and 610 existing tests passed.
- **EXECUTED — GREEN:** after implementation, `npx jest --selectProjects core --runTestsByPath src/ui/__tests__/gameSessionLifecycle.test.ts --runInBand` passed 1 suite / 5 tests.

## Verification evidence

### Release builds

- **EXECUTED:** checked `df -h /` immediately before every native build; each build started above the 5 GiB gate.
- **EXECUTED — flags OFF:** `env -u EXPO_PUBLIC_FTUE npx expo run:android --variant release --device fleet_floor_api31` completed Gradle release assembly (`BUILD SUCCESSFUL in 3m 48s`). Expo's wrapper later exited while trying to start its DevTools/Metro follow-up, so the produced APK was installed explicitly. Artifact: `apk/flags-off-release.apk`, SHA-256 `ed14a060923a3e22ca26eba93887265f7771c47b236462e71cb27fe55b1f5c1f`.
- **EXECUTED — flags ON:** after a native clean, `EXPO_PUBLIC_FTUE=1 ... ./gradlew :app:assembleRelease -PreactNativeArchitectures=arm64-v8a` printed `BUILD SUCCESSFUL in 1m 55s`. Artifact: `apk/flags-on-release-arm64.apk`, SHA-256 `152283ffd24c3887695996fd930ca69f3031bc150e3727d4e5808c0afffbc506`. The release bundle contains one copy of `Tap an arrow.`

### Flags-ON Android flow

- **EXECUTED:** fresh-installed the flags-ON APK and recorded from the Play tap through the first real-board tap (`emulator/w104-on-flow.mp4`, 84.78 s; contact sheet `emulator/w104-on-flow-contact.png`).
- **EXECUTED:** T1 showed `Tap an arrow.`, three hearts, and no hint (`emulator/on-02-t1.png`). Clearing it hard-cut to T2 with `One arrow is free. Find it.` and three hearts (`emulator/on-03-after-t1-t2.png`).
- **EXECUTED:** clearing T2 hard-cut to the real `LEVEL 1 · Normal · Circle · 60 left` header with the hint restored (`emulator/on-04-after-t2-real-level1.png`), then the recording captured the first real-board arrow tap (`emulator/on-05-first-real-tap.png`).
- **EXECUTED:** frame/contact-sheet inspection shows no win/lose panel, ad, or TEST AD surface during either tutorial; after Play, all interaction taps in the recorded flow are board arrows.
- **EXECUTED — loss:** on T2, three distinct blocked arrows visibly reduced hearts `3 → 2 → 1 → 0`; after the 350 ms delay the same five-arrow T2 board returned with three hearts and no panel (`emulator/w104-t2-loss.mp4`, 19.23 s; before/after screenshots and contact sheet in `emulator/`).

### Persisted-key web read-back

- **EXECUTED:** the literal `EXPO_PUBLIC_FTUE=1 npx expo start --web --port 8097` could not remain up in the managed sandbox: React Native DevTools installation was blocked, the headless fallback exhausted file watchers (`EMFILE`), and Watchman could not write its macOS LaunchAgent.
- **EXECUTED — equivalent bundle path:** `EXPO_PUBLIC_FTUE=1 npx expo export --platform web --output-dir artifacts/W1-04/web/dist`, followed by `npx expo serve ... --port 8097`, succeeded. A freshly cleared Android Chrome profile on `emulator-5556` loaded the served app through CDP. The verification script and complete log are `web/verify-ftue.mjs` and `web/verify-ftue.log`.
- **EXECUTED:** after T1, `arrows_ftue_stage === "1"`; `arrows_total_solved`, `arrows_current_level`, `arrows_day_streak`, and `arrows_last_play_day` were all `null`.
- **EXECUTED:** after T2, stage was `"2"`; the same four campaign keys remained `null`.
- **EXECUTED:** after clearing real level 1, stage remained `"2"`, `arrows_total_solved === "1"`, `arrows_current_level === "1"`; the existing streak keys then populated as expected. Screenshots `web/02-t1.png` through `web/05-real-level1-cleared.png` show the corresponding UI states.

### Flags-OFF exact baseline (controller amendment F04)

- **EXECUTED:** built an archive of immediate pre-task HEAD `490817cc` outside the worktree with FTUE unset, using Expo prebuild and the same all-ABI release Gradle path as the flags-OFF APK. Both contain `arm64-v8a`, `armeabi-v7a`, `x86`, and `x86_64`. Artifact: `apk/baseline-head-490817cc-release.apk`, SHA-256 `2cba5f184ef36ade319d3f4fed26d2fe2ed42cea85cb6fb0fd930018d1af3c3b`.
- **EXECUTED:** fresh installs of that baseline and the W1-04 flags-OFF APK both opened `LEVEL 1 · Normal · Circle · 60 left` with three hearts and the hint (`emulator/baseline-head-490817cc-level1.png`, `emulator/flags-off-level1.png`).
- **EXECUTED:** an RGB pixel comparison found **0 differing pixels below the 72 px status bar**. The full-frame difference bounding box is only `(71, 25)–(94, 62)`, the status-bar clock. This closes F04 against the immediate pre-task commit rather than `da93dcd`; the recorded result is `baseline-comparison.txt`.

### PERF routing

- **EXECUTED:** an initial `--skip-prebuild` diagnostic build correctly failed the harness preflight because the then-generated native tree lacked the profileable manifest plugin output.
- **EXECUTED:** reran the supported command with Expo prebuild: `npm run perf:android -- --runs 1 --warmups 0 --phases exit --screenshot artifacts/W1-04/emulator/perf-routing.png --output artifacts/W1-04/perf-routing.json`.
- **EXECUTED:** release build and harness passed. The fresh build booted `perf-game-screen`; the screenshot shows `LEVEL 3828` for `PERF_LEVEL=3827`. Harness JSON records `configurationVerifiedFromFreshBuild: true`, `nativePrebuildRecreated: true`, and all three animation scales as `0`; the app reported reduced motion true. This check is routing evidence, not a shipped-motion result.

### Code gates

- **EXECUTED:** exact Expo SDK 57 versioned documentation was read before code changes. The implementation uses the existing project APIs and adds no dependency.
- **EXECUTED:** initial plain Jest startup was blocked before discovery because Watchman tried to write `~/Library/LaunchAgents`. `npx jest --runInBand --no-watchman` then passed **35 suites / 617 tests / 0 snapshots** across both projects.
- **EXECUTED — F33:** `npx jest --selectProjects ui --runInBand --no-watchman` passed **2 suites / 2 tests**.
- **EXECUTED:** `npx tsc --noEmit` exited 0.
- **EXECUTED:** `git diff --check` passed.
- The concise TDD and closing-gate record is `artifacts/W1-04/test-results.txt`.

## Decisions and tradeoffs

1. `LevelSession.mode` is the single source for aggregator mode at every start. This makes campaign/tutorial ownership explicit now and leaves W4's future daily extension local to session creation.
2. Tutorial terminal callbacks reuse the existing guarded delay machinery. Phase stays `playing` while `terminalPending` locks the board, so no tutorial panel can render before re-deal/advance.
3. Tutorial progress is written after the transition is accepted and before its delay, matching the brief's crash-safe ordering without starting campaign stats.
4. The T1→T2→real and loss re-deal remain hard cuts. That is the explicitly accepted W1 state; W2 owns transition treatment and delay constants.
5. The tutorial header adds only an 18 pt override to the existing level-label style. The flags-OFF 24 pt level header remains byte-for-byte visually unchanged below the status bar.

## Concerns

1. The exact Expo dev-server form could not run under this managed macOS sandbox. The static Expo web export exercised the same compiled flag-on app and real browser storage, but it is not evidence for dev-server/HMR behavior.
2. Watchman is unusable in this sandbox because its LaunchAgent write is outside the writable roots. Final Jest results use Jest's supported `--no-watchman` switch.
3. The task's Android captures are emulator evidence only. A physical Android phone and iOS are **UNVERIFIED-DEVICE**, as required by the brief.

## Final device and cleanup state

- `emulator-5556` remains running; it was never booted or killed by this task.
- The fix-round flags-ON release repack is installed fresh and left on T2. Animation scales were restored and read back as `1 / 1 / 1`.
- The initial task's temporary archived source/build directory was deleted after its baseline APK and screenshots were preserved under `artifacts/W1-04/`; the fix-round bundle/repack evidence remains under `artifacts/W1-04/fix1/`.

## Owner acceptances needed

- `T1_LINE`: `Tap an arrow.` — decide at W1-11. **Yes:** approve this string verbatim. **No:** supply replacement wording or choose the named **wordless** alternative: no `ftueCopy` strings; the free first blocked tap and W0-04's static blocker state do the teaching.
- `T2_LINE`: `One arrow is free. Find it.` — decide at W1-11. **Yes:** approve this string verbatim. **No:** supply replacement wording or choose the same named **wordless** alternative.

## Fix round 1

### Findings resolved

1. **Tutorial-header contrast.** Kept the plan-mandated 18 pt bold size and changed only the tutorial branch from `accentLight` to the existing `accentText` role. No palette token changed.
   - **EXECUTED:** `npx tsx scripts/contrast-audit.ts --solve accentLight bg 4.5` showed that Daylight `accentLight` would need a token change from `#8F76F0` to `#7B5EED` (`dL -5.2`) to reach 4.50:1; Ink Night already measured 7.09:1.
   - **EXECUTED:** `npx tsx scripts/contrast-audit.ts --solve accentText bg 4.5` returned both current colours unchanged (`dL +0.0`): Daylight `#6D4AEF` at **5.42:1** and Ink Night `#8C70F6` at **5.12:1**.
   - Added the `game-tutorial-line` `USAGES` row (`accentText` on `bg`, 18 px, bold) and updated the mechanically shifted `GameScreen` source tripwires.
2. **Copy approval routing.** Replaced “Owner acceptances needed: None” above with separate W1-11 yes/no decisions for `T1_LINE` and `T2_LINE`, and named the plan's **wordless** alternative.
3. **Shared FTUE stages.** Exported `T1_CLEARED_STAGE` and `ASSIST_STAGE` from `ftueRoute.ts`; `GameScreen` now writes those constants instead of literals `1` and `2`.

### Verification

- **EXECUTED:** `npx tsx scripts/contrast-audit.ts` passed **78 gated palette rows / 0 FAIL**. The new tutorial row is 5.42:1 in Daylight and 5.12:1 in Ink Night against its 4.5 gate.
- **EXECUTED:** `npx jest --selectProjects core --runInBand --no-watchman` passed **33 suites / 618 tests / 0 snapshots**. This includes the contrast source-site tripwire and all FTUE-route/session tests.
- **EXECUTED:** `npx jest --selectProjects ui --runInBand --no-watchman` passed **2 suites / 2 tests / 0 snapshots**.
- **EXECUTED WITH CONCERN:** plain `npx tsc --noEmit` twice reached Node 20's approximately 4 GiB default heap ceiling after about 90 seconds and exited 134 before emitting any TypeScript diagnostic. `NODE_OPTIONS=--max-old-space-size=8192 npx tsc --noEmit` then exited **0** with no diagnostics. This is a verifier resource concern, not a reported type error.
- **EXECUTED:** `git diff --check` exited 0.
- **INFERRED, controller-directed:** the flags-OFF rendering path is unaffected and was not rebuilt. The colour change is inside the `activeTutorialId` branch, which flags OFF never enters; the stage constants preserve the exact values 1 and 2; the remaining changes are audit/report-only.

### Flag-ON recapture

- **EXECUTED:** `df -h /` reported 6.7 GiB free. Because all runtime changes are JavaScript/TypeScript-only, the capture used the permitted release JS-repack recipe: `EXPO_PUBLIC_FTUE=1` Expo `export:embed` (1,545 modules), `hermesc -O`, bundle replacement over the existing W1-04 flags-ON Gradle host (`152283ffd24c3887695996fd930ca69f3031bc150e3727d4e5808c0afffbc506`), zipalign, and debug signing. No Gradle/native build ran.
- **EXECUTED:** the repacked APK verifies with v2/v3 signatures; SHA-256 is `6d7258ac343e98c6b41609a5ed5cc4756397cdb8997b32999aea3f24d9b21641`. The release bundle contains one copy each of `Tap an arrow.` and `One arrow is free. Find it.`
- **EXECUTED:** fresh-installed on the already-running `emulator-5556`, force-stopped/relaunched after setting animation scales, and read back `window / transition / animator = 1 / 1 / 1`.
- **EXECUTED:** T1 screenshot `artifacts/W1-04/fix1/emulator/01-t1-accentText.png` shows `Tap an arrow.` with three hearts and no hint. T2 screenshot `artifacts/W1-04/fix1/emulator/02-t2-accentText.png` shows `One arrow is free. Find it.` with three hearts and no hint. Both preserve the 18 pt header size and render the darker Daylight role cleanly without clipping.
- **EXECUTED:** exact-pixel counts in the captures found `#6D4AEF` (`accentText`) **7,064** times on T1 and **13,182** times on T2, with **0** occurrences of old `#8F76F0` (`accentLight`) in either image.
- **EXECUTED:** the emulator remains running on T2; it was not booted, killed, wiped, or snapshotted. Full fix evidence, including the repacked APK, bundle, screenshots, UI dumps, hashes, and concise command outcomes, is under `artifacts/W1-04/fix1/`.
