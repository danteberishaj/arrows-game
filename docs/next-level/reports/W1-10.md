# W1-10 report: acceptance captures and owner review packet

- **Status:** DONE_WITH_CONCERNS. Scenarios A–F and the pre-fix reproduction are complete on `emulator-5556`; the owner packet is `docs/ftue-acceptance-2026-09-20.md`. Open items are owner review, the blocker-timing wording conflict, the literal Jest/Expo sandbox limitations, iOS, and physical Android.
- **Base:** `6f4d5dc2d643cfa3bbb4c874bcf61436fb50f3cc` on `next-level`, the exact immediate pre-task HEAD required by ruling F04.
- **Git:** no commit, add, checkout, restore, stash, reset, clean, push, PR, publish, release, or EAS command was run. Claude owns Git.
- **Evidence:** `artifacts/W1-10/`.

## What changed

`docs/ftue-acceptance-2026-09-20.md`

- Added the dated A–F acceptance packet, capture context for every group, build/signing matrix, owner review paths, exact yes/no decisions, and unverified-device labels.

`docs/next-level/reports/W1-10.md`

- Added this execution report.

No product source, persistence key, dependency, flag default, or native project file changed. The capture-only stall delay patch was restored immediately; `src/ui/ftueConfig.ts` remains `FTUE_STALL_HINT_MS: number | null = null`. Migration and wipe risk are **none**.

## Build evidence

- **EXECUTED:** Read the exact Expo SDK 57 versioned documentation before task actions. No Expo API or dependency was added.
- **EXECUTED:** Built BASE from a read-only `git archive da93dcd` scratch tree, OFF from `6f4d5dc` with flags absent, and ON from `6f4d5dc` with FTUE/assist/stall flags plus capture-only 4000 ms **TEST VALUE**. Each used fresh Expo prebuild and local Gradle release signing. Builds ran one at a time after a `df -h /` check at or above 5 GB.
- **EXECUTED:** The first scratch BASE Gradle attempt lacked `ANDROID_HOME` and stopped before compilation; the immediate retry supplied the SDK path and ended `BUILD SUCCESSFUL in 4m 31s`. Both logs are retained. This setup retry did not alter source or the emulator.
- **EXECUTED:** BASE fresh `5be79376…a4d0`, canonical BASE capture artifact `7556f834…a428`, OFF `088a520e…8d78`, and ON `ed5b91c8…0132`. All verify with certificate SHA-256 `24f7fa0…50f8`; `adb install -r` succeeded in C. Full matrix: `artifacts/W1-10/build-matrix.json`.
- **EXECUTED:** Repository `android/app/build` and `android/build` were removed after the builds. Only APKs and logs from the build process were retained under the task artifact root; capture evidence remains in its scenario directories.

## Pre-fix reproduction

- **EXECUTED:** The canonical BASE APK reproduced level 1's original three-pre-removal-blocked-tap loss: 3 → 2 → 1 hearts, then loss. Correct evidence is `pre-fix/base-loss-correct-heart2.png`, `base-loss-correct-heart1.png`, `base-loss-correct-panel.png`, and `base-level1-three-blocked-loss-correct.mp4` (12.004 s). Earlier probe/miscalibrated files are retained for audit but explicitly excluded.
- **EXECUTED:** BASE then cleared levels 1 and 2, producing the old-build `LEVEL 3` AsyncStorage used by C. Evidence: `base-level1-cleared-final.png`, `base-level2-cleared-final.png`, and `base-after-next2.png`.

## Acceptance evidence

### Capture context

- **EXECUTED:** Native groups use AVD `fleet_floor_api31`, API 31, software GPU, physical 1440×3120/560, override 1080×1920/480 (360×640 dp). A/B/C/F and BASE read back scales 1/1/1; D read back 0/0/0. P-02 reports app reduced motion unavailable for these non-PERF releases. Exact per-group context is in the acceptance packet.
- **EXECUTED:** E used emulator Chrome with CDP viewport 375×812 CSS px at device scale 2 while device scales were 0/0/0; `matchMedia` reported reduced motion true.
- **EXECUTED:** Cleanup restored physical display size/density and scales 1/1/1; scoped `adb -s emulator-5556 get-state` returned `device`. The emulator remains running.

### A — ruling-F04 flag-OFF baseline

- **EXECUTED:** Same header/board in `scenario-a/a-baseline-head-level1.png` and `a-off-level1.png`. Pixel comparison reports zero changed pixels below the 72 px status-bar exclusion; only clock/connectivity pixels differ. PASS.

### B — fresh ON first session

- **EXECUTED:** 51.363856 s current-HEAD recording: `scenario-b/scenario-b-full-flow-fast.mp4`; contact sheet `owner-review/scenario-b-video-contact-sheet.png`.
- **EXECUTED:** T1 3/3 hearts; T2 first blocked free with exact rule line, different second blocked costs one heart; no ad/panel before the real board's first tap; three pre-removal real-board blocks free; post-removal block costs; menu stats absent after T2 and `1 puzzle solved` after level 1. Screens `01`–`12` in `scenario-b/`.
- **EXECUTED:** Play-to-first-success = 759 ms, n=1, implementer knows the rules (`play-to-first-removal.json`).
- **EXECUTED:** Capture is from HEAD after W2-03, so amendment F17 is present. The required flow does not enter a tutorial terminal loss/redeal.

### C — old on-disk player state

- **EXECUTED:** BASE wrote level 3; update-install ON retained Level 3 and two solved puzzles, skipped tutorials, and charged the first blocked tap. Evidence: three `scenario-c-*.png` files. PASS.

### D — reduced motion

- **EXECUTED:** P-02 manifest reads scales 0/0/0 and release app-report unavailable. The fresh repeat captures static first/second blocker states at +327/+339 ms, clear post-event states at +1,341/+1,346 ms, and static stall hint at +5,352 ms. `scenario-d/contact-sheet.png` plus `event-timings-and-readback.json`. PASS with concern below.

### E — exact persisted values on web

- **EXECUTED:** `scenario-e/state-readback.json` asserts stage 1 after T1 with all campaign keys null; stage 2 after T2 with those keys null; stage 2/current 1/solved 1 after non-perfect level 1; free first block and stage 3/current 2/solved 2 after perfect level 2; stage 3 survives reload and level 3's first block costs. Eight screenshots accompany the trace. PASS.

### F — 360×640 chrome and copy

- **EXECUTED:** `scenario-f/composite-headers.png` shows T1, T2 lesson/rule, and real level-1 headers from 360×640 dp captures.
- **INFERRED from the consumed W1-05 measurement:** both tutorial boards have `cellPt 67.68 dp`, above 48; W1-10 did not duplicate that producer measurement.
- **EXECUTED:** Source has exactly three W1 copy exports and final Jest includes the exact-count test: `Tap an arrow.`, `One arrow is free. Find it.`, and `Blocked. Clear what's in its way.` PASS.

## TDD

- **EXECUTED:** None. This task is verification-only and changes no logic.

## Repository gates

- **EXECUTED:** literal `npx jest` exited before discovery because managed-sandbox Watchman could not write `~/Library/LaunchAgents/com.github.facebook.watchman.plist`.
- **EXECUTED:** supported fallback `npx jest --runInBand --no-watchman` passed **41 suites / 662 tests / 0 snapshots** across core and UI projects.
- **EXECUTED:** `npx tsc --noEmit` exited 0.
- **EXECUTED:** F33 did not trigger because W1-10 did not edit `GameScreen.tsx` or `ads.tsx`; the full fallback run nevertheless included both UI suites.
- **EXECUTED:** `git diff --check` and final worktree status are recorded after the two docs are added. Summary: `artifacts/W1-10/final-gates.txt`.

## Decisions and tradeoffs

1. Applied ruling F04 literally: A compares the immediate pre-task HEAD against OFF. `da93dcd` is restricted to pre-fix reproduction and C's over-install source.
2. Used the canonical BASE for behavioral reproduction so the old-state evidence remains byte-exact, while also completing the brief's requested fresh BASE Gradle build and confirming the same upload certificate.
3. Kept the 4000 ms value capture-only. The tracked delay remains `null`; the packet explicitly separates accepting the behavior from choosing W1-11's production delay.
4. Preserved early and late D screenshots instead of mislabeling a post-lifetime frame as the blocker state.
5. Reused W1-05's accepted `cellPt` measurement as a consumed interface rather than re-verifying producer work.
6. Retained superseded diagnostic captures for auditability, but the packet names only acceptance files. `scenario-b-full-flow-fast.mp4` supersedes the 180 s manual recording.

## Concerns and unverified items

1. The D wording is internally incompatible with W0-04's intentional sub-one-second blocker lifetime: a screenshot at least one second after the blocked event cannot still show that blocker. Early static and late post-event evidence are both provided; the hint meets the ≥1 s-after-onset condition.
2. Literal `npx jest` and literal non-CI Expo web startup are blocked by sandbox-owned Watchman/DevTools paths. The supported no-Watchman Jest run and CI Expo retry passed the requested logic/behavior.
3. **UNVERIFIED-DEVICE:** physical Android.
4. **UNVERIFIED:** iOS, per `docs/IOS_BOARD_PLAN.md`.
5. **UNMEASURED / non-goal:** teaching effectiveness and the real stall delay.

## Owner acceptance needed

The exact artifact paths and yes/no language are in `docs/ftue-acceptance-2026-09-20.md`:

1. **YES:** approve all three strings verbatim. **NO:** provide replacements or choose wordless.
2. **YES:** accept the T2/stage-2 one-free-fresh-blocked-tap assist shown. **NO:** reject it and keep the flag OFF.
3. **YES:** accept W0-04's static reduced-motion blocker/hint appearance and tutorial stall-hint behavior while keeping delay `null`. **NO:** reject the appearance and/or behavior and identify which state changes.

No owner decision here approves 4000 ms or enables a feature flag.
