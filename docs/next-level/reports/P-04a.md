# P-04a report: prove targetSdk from a locally built release artifact

- **Status:** DONE_WITH_CONCERNS (the concerns are small brief mismatches, not open proof)
- **Base:** `a1e2b43512959ba66fbacdd8908f44cc37e5bb24` (`next-level`, clean tree)
- **Commits:** `3a9c06a` release: record targetSdk 36 read from a locally built release artifact; plus the commit that adds this report
- **Result:** `targetSdkVersion:'36'` (APK, aapt) and `36` (AAB, bundletool). Because the value is already 36, `app.json` was not touched (brief step 5 did not apply).

## What changed

- `RELEASE.md`: new section "Target SDK proof (local release artifact)". It records the commit, date, exact commands, printed lines, APK and AAB sha256, where the base APK is kept, and the remaining unverified line (the EAS cloud `.aab`, P-04b).
- `.gitignore`: **no change.** `/artifacts/` is already there (line 52), added in `a1e2b43`. See concern 1.
- `app.json`: no change. The value read is 36, not below it.
- Outside git (ignored): `artifacts/base/da93dcd-release-arm64.apk` plus `.sha256`, and the logs and reads under `artifacts/P-04a/`.

## Evidence

- EXECUTED: pre-fix. `grep -n targetSdk app.json` gave 0 hits (exit 1). `android/app/build.gradle:94` reads `targetSdkVersion rootProject.ext.targetSdkVersion`. `node_modules/react-native/gradle/libs.versions.toml:4` is `targetSdk = "36"`. `node_modules/expo-modules-core/android/ExpoModulesCorePlugin.gradle:69` is `safeExtGet("targetSdkVersion", 36)`. All of that is config, so it stays INFERRED as proof. Log: `artifacts/P-04a/prefix.txt`.
- EXECUTED: `env | grep -E "ARROWS_PERF|EXPO_PUBLIC_PERF"` matched nothing. Both gradle runs also used `env -u CI -u ARROWS_PERF_BUILD`.
- EXECUTED: `df -h /` showed 11Gi free before the build, which passes the 5 GB gate. `pgrep` found no other gradle or xcodebuild process.
- EXECUTED: `npx expo prebuild --platform android --clean --no-install` exited 0 and `git status` stayed clean (`artifacts/P-04a/prebuild.log`). None of the generated outputs at `benchmark.mjs:415-421` existed after `--clean`; the `rm -rf` ran anyway.
- EXECUTED: the first `./gradlew :app:assembleRelease -PreactNativeArchitectures=arm64-v8a` **failed** with "SDK location not found". `--clean` removes `android/local.properties`, and `ANDROID_HOME` is unset in this shell (`artifacts/P-04a/assembleRelease-attempt1-no-sdk.log`). The rerun passed `ANDROID_HOME=$HOME/Library/Android/sdk` and printed `BUILD SUCCESSFUL in 8m 22s`, exit 0 (`artifacts/P-04a/assembleRelease.log`).
- EXECUTED: `~/Library/Android/sdk/build-tools/36.0.0/aapt dump badging app-release.apk | grep -E "sdkVersion|targetSdkVersion|versionCode"` printed:
  - `package: name='com.danteb.arrows' versionCode='6' versionName='1.0.0' platformBuildVersionName='16' platformBuildVersionCode='36' compileSdkVersion='36' compileSdkVersionCodename='16'`
  - `sdkVersion:'24'`
  - `targetSdkVersion:'36'`
- EXECUTED: `apkanalyzer manifest print app-release.apk | grep -c profileable` gave `0`. `apkanalyzer manifest target-sdk` gave `36`, `debuggable` gave `false`, and `application-id` gave `com.danteb.arrows`. `apksigner verify --print-certs` shows signer DN `CN=Arrows`, which is not the Android debug key. The only `lib/` ABI directory is `arm64-v8a` (`artifacts/P-04a/apk-reads.txt`).
- EXECUTED: control for the `profileable` count. The same dump pipelines count `<application` as 1 on both the APK and the AAB, so a 0 comes from a real manifest dump and not an empty one. Calling the exported `addProfileable` directly shows a perf build injects `profileable android:shell="true"` (`artifacts/P-04a/profileable-control.txt`). No perf build was made to show a count of 1 (INFERRED from the plugin and from `benchmark.mjs:448-449`).
- EXECUTED: APK sha256 is `7556f834c720842d47385e066da7aec7b3e05ce2808ed443fe7b794f8f32a428` (42035912 bytes). It was copied to `artifacts/base/da93dcd-release-arm64.apk`, and `shasum -a 256 -c da93dcd-release-arm64.apk.sha256` printed `OK`. `git check-ignore -v` matches `.gitignore:52:/artifacts/`.
- EXECUTED: bundle gate. After the APK, `df` showed 4.7Gi. That passes the brief's 3 GB gate but not the global 5 GB native-build gate, so I first recorded a skip. After `./gradlew --stop` and `rm -rf android/app/build` (767M), `df` showed 5.3Gi, which passes both gates, and I started `:app:bundleRelease -PreactNativeArchitectures=arm64-v8a`. It printed `BUILD SUCCESSFUL in 2m 27s` (74 tasks executed, 561 up-to-date), exit 0. Free space bottomed out at 4.7Gi (`artifacts/P-04a/bundle-gate.txt`, `bundleRelease.log`).
- EXECUTED: bundletool 1.18.1 `dump manifest --bundle app-release.aab`:
  - `--xpath /manifest/uses-sdk/@android:targetSdkVersion` gave `36`
  - `minSdkVersion` gave `24`
  - `/manifest/@android:versionCode` gave `6`
  - `| grep -c profileable` gave `0`
  - AAB sha256 is `ab0426acbbc1d7ff0aa86cbf4c3293325d244258599ebdf2db988a4d3aa40302` (34146058 bytes). Reads are in `artifacts/P-04a/aab-reads.txt`.
- EXECUTED: cleanup. `android/app/build` was removed after the bundle read, and `df` ended at 5.4Gi. The AAB was not kept (the brief keeps only the APK); its hash is recorded.
- EXECUTED: gates. `npx jest` ran 11 suites / 61 tests, all passing. `npx tsc --noEmit` exited 0. `git status --porcelain` printed 0 lines before this report was written.
- INFERRED: the APK and AAB come from two gradle invocations on the same prebuild output, so they are separate builds. Their manifests were each read directly, so the targetSdk claim does not depend on them matching.
- UNVERIFIED-DEVICE (really UNVERIFIED-EAS): the `.aab` that EAS builds in the cloud and that is actually uploaded. It is INFERRED to match because EAS runs the same prebuild and `bundleRelease`. P-04b closes it.

## Concerns

1. **Brief is stale on `.gitignore`.** Step 4 says to add `/artifacts/` "in the same commit". It is already at `.gitignore:52`, added in `a1e2b43`, so there was nothing to add. Its current comment already warns that release APKs there are signed with the upload key.
2. **The brief's build command does not run as written in this shell.** After `prebuild --clean`, gradle needs `ANDROID_HOME` (or `local.properties`). RELEASE.md records the working command. `scripts/perf/android/benchmark.mjs` has the same dependency on the caller's environment (INFERRED; not run).
3. **Two disk gates disagreed.** The brief allows the bundle build at 3 GB; the global run constraint forbids any native build under 5 GB. I applied the stricter gate and built the bundle only after cleanup brought free space back to 5.3Gi.
4. **The base APK is named `da93dcd` but was built at `a1e2b43`.** The run constraints allow this (app code is identical; only `docs/` and `.gitignore` differ). RELEASE.md states the real commit.
5. **The base APK is signed with the real upload key** (`CN=Arrows`, because `credentials/` is present). This matches what later over-install tests need. It must never leave `artifacts/` or be committed; it is currently ignored.

## Owner acceptances needed

- None to close P-04a. P-04b (owner-gated) must still read the EAS-built `.aab` for the targetSdk ≥ 36 and versionCode > 6 lines.
