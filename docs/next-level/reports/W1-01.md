# W1-01 implementation report

## Status

**DONE.** The implementation, release diagnostics, three geometry captures, calibration, and
repository gates are complete. In fix round 1, the controller ruled that the 1 dp criterion compares
the same visible area with system-bar insets included. Fresh device measurements now pass that
comparison at all three geometries. The controller also corrected the brief's native dp geometry;
the measured value is `411.43x891.43 dp`, not `514x1114 dp`.

## What changed

- `src/ui/BoardView.tsx`: logs `[board-viewport] w=<vw> h=<vh>` from the board's own `onLayout`
  callback only when `process.env.EXPO_PUBLIC_LOG_BOARD_VIEWPORT === '1'` was inlined at bundle time.
- `scripts/analysis/ftue-board-metrics.ts`: core-only CLI for generated levels and the future T1/T2
  core export. It prints dimensions, arrow count, initial clearable count/percentage, every greedy
  solve state's `remaining:legal` count, and optional `cellPt`.
- `src/ui/contrastAudit.ts`: shifted three BoardView source-line metadata entries by +3 after the
  guarded log insertion. No palette, contrast rule, or runtime behavior changed.
- `docs/board-viewport-measured.md`: raw measurements, formulas, ceilings, source constants, header
  heights, device scope, and the unresolved cross-check.

No persistence key, saved state, feature flag, ad behavior, player-visible UI, or motion changed.

## Pre-fix reproduction

- EXECUTED: `ls docs/board-viewport-measured.md` exited 1 with `No such file or directory`.
- EXECUTED: `grep -rn "board-viewport" src` exited 1 with no output (zero hits).
- EXECUTED evidence: `artifacts/W1-01/pre-fix-reproduction.txt`.

## Build and device method

- EXECUTED: Expo SDK 57 reference and Expo environment-variable guidance were read before code.
  The latter requires the exact dot-notation access used here for Metro inlining.
- EXECUTED: `adb -s emulator-5556 get-state` returned `device`; AVD read-back was
  `fleet_floor_api31`, API 31.
- EXECUTED: `df -h /` immediately before Gradle showed 11 GiB available, above the 5 GB gate.
- EXECUTED: diagnostic prebuild completed (`artifacts/W1-01/prebuild.log`).
- EXECUTED: the single native build was `:app:assembleRelease` for arm64-v8a with
  `EXPO_PUBLIC_LOG_BOARD_VIEWPORT=1`; it completed `BUILD SUCCESSFUL in 3m 3s`, 636 tasks
  (`artifacts/W1-01/assembleRelease-diagnostic.log`). The upload-signed APK SHA-256 is
  `7ae9e0e4cc25b09ba81e7a5b9769283f28215e1736d04c44f0bc7fd3981bcf65`.
- EXECUTED: the Gradle APK contained one `[board-viewport]` string. Its first direct over-install
  failed with `INSTALL_FAILED_UPDATE_INCOMPATIBLE` because the existing app used the Android debug
  signer while the build used `CN=Arrows`. No uninstall or data wipe occurred.
- EXECUTED: the installed APK was first preserved as `artifacts/W1-01/apk/device-before.apk`.
  The release APK was debug-signed, then installed with `-r`, preserving app data.
- EXECUTED: the first native bundle used the wrong, inert spelling
  `EXPO_PUBLIC_PERF_LEVEL_INDEX=0`, so it correctly opened the normal menu. For reliable
  `perf-board` UIAutomator identification, the same Gradle release host was repacked with a fresh
  release Hermes bundle made by Expo `export:embed --dev false` plus `hermesc -O`, using the correct
  existing switch `EXPO_PUBLIC_PERF_LEVEL=0`. Diagnostic capture APK SHA-256:
  `156cc0ad1b3c582a5855e4f7bcce1dbc8e694f4a26f1e6b9661309f02f402b19`.
- EXECUTED: the log-disabled check used another release Hermes repack over that same native host,
  with `EXPO_PUBLIC_PERF_LEVEL=0` and no viewport variable. Its SHA-256 is
  `1c6bcc6e30a3dec4b9e3015c324685f1841e2368aa40fd80239c41d3b3c02f2d`.
- EXECUTED: after verification, `wm size reset; wm density reset` was run. Final read-back is
  physical `1440x3120 @560`; `policy_control` is `null`; animation scales are `0/0/0`; emulator
  state is `device`.
- EXECUTED: the pre-task APK was reinstalled with `-r`. The installed APK and preserved APK both
  hash to `6bfde1ddbbcbd734edeb1650568518f78932133ca66bf66f91a72f0e60a2e7e9`, so the emulator binary
  was restored without deleting app data.

## Measurement evidence

- EXECUTED: native log `w=411.4285583496094 h=804.2857055664062`; UIAutomator board
  `411.428558x756.285706`; height delta 48 dp.
- EXECUTED: 1080x2340 @480 log `w=360 h=689`; UIAutomator board `360x633`; height delta 56 dp.
- EXECUTED: 1080x1920 @480 log `w=360 h=549`; UIAutomator board `360x493`; height delta 56 dp.
- EXECUTED: every UI root and board node stopped at physical-screen-bottom minus exactly 168 px,
  and every screenshot visibly contains the three-button navigation bar in that strip.
- INFERRED: React Native's edge-to-edge Yoga layout extends the board behind the navigation bar,
  while UIAutomator clips the accessibility bounds to the visible app area. This explains the exact
  raw delta. The controller's fix-round-1 ruling below specifies the like-for-like adjustment that
  now satisfies the <= 1 dp criterion.
- EXECUTED: the brief's native `514x1114 dp` parenthetical conflicts with literal
  `1440x3120 @560`; division by `560/160` gives `411.429x891.429 dp`. Measurements use the literal
  px/density values and flag the contradiction.
- UNVERIFIED-DEVICE: physical-phone Android/iOS insets, cutouts, notches, and gesture navigation.

Full measurements and the logged-value ceilings are in `docs/board-viewport-measured.md`.

## Instrument calibration and release-disable check

- EXECUTED: `npx tsx scripts/analysis/ftue-board-metrics.ts --level 0` printed the required
  `rows 20 cols 20 arrows 60 clearable 19 (31.7%)` and a reconciled 60-state greedy trace.
- EXECUTED: W3-01's independent probe printed the same 20x20 / 60 / 19 / 31.7% baseline.
- EXECUTED: the log-disabled APK's embedded Hermes bundle contained zero `[board-viewport]`
  strings; the diagnostic bundle contained one.
- EXECUTED: after installing and opening the log-disabled release board, the exact filtered command
  `/Users/gentlegen/Library/Android/sdk/platform-tools/adb -s emulator-5556 logcat -d -s ReactNativeJS:V | grep board-viewport`
  exited 1 with no output. Raw log: `artifacts/W1-01/disabled-logcat.txt`.
- INFERRED: player builds that do not set the variable have no diagnostic log branch or tag in the
  release bundle; the bundle grep and installed-device negative check cover this risk.

## TDD and repository gates

- TDD: none by brief; this is a measurement task. The level-0 cross-check is the instrument
  calibration.
- EXECUTED RED (gate, not logic TDD): first `npx jest` run produced 32 passing suites and one failing
  suite, with 581 passing / 3 failing tests. All failures were the contrast audit's exact BoardView
  line-address metadata shifted by the intended three-line insertion (`artifacts/W1-01/jest.txt`).
- EXECUTED GREEN: after updating only those three metadata line numbers, `npx jest` passed 33/33
  suites and 584/584 tests across both configured projects (`artifacts/W1-01/jest-final.txt`).
- EXECUTED: final `npx tsc --noEmit` exited 0 (`artifacts/W1-01/tsc-final.txt`).
- F33: not triggered because neither `GameScreen.tsx` nor `ads.tsx` was edited. The full Jest run
  nevertheless ran the UI project and both UI component suites passed.

## Decisions

1. Kept the exact measured geometry instead of silently changing native density to make the brief's
   incorrect dp parenthetical true.
2. Did not change `GameScreen` to apply a bottom inset. That would be a player-visible layout fix,
   contradicting W1-01's non-goal and expanding the task beyond measurement.
3. Did not invent T1/T2 boards. `--tutorial` recognizes both ids and reads an optional future
   `buildTutorialLevel` core export; until W1-02 lands it throws a precise unavailable message.
4. Kept the small metrics walk local to the W1 CLI. F30 prefers a shared pure function, but moving
   W3-01 analysis into production `src/core` would widen this measurement task and production API.
   The required independent cross-check was executed instead.
5. Used release-Hermes JS repacks only to correct the perf switch and create the flag-disabled arm;
   the one native release build required by the brief was completed first and remained the host for
   both arms.

## Concerns

1. **Resolved in fix round 1:** the device-reported system-bar inset makes the logged and
   UIAutomator visible areas agree within 1 dp at all three geometries.
2. **Resolved by controller ruling:** `1440x3120 @560` is `411.43x891.43 dp`; consumers use that
   measured geometry rather than the brief's incorrect parenthetical.
3. **Expected sequencing:** T1/T2 do not exist in the repository yet, so their CLI path cannot produce
   metrics until W1-02 exports them. Generated-level behavior is calibrated now.
4. **Watchman:** Jest printed the existing recrawl warning; tests still completed successfully.

## Owner/controller acceptance needed

None for W1-01. The controller answered both prior questions in the fix-round-1 rulings: compare
the same visible area using the measured inset, and use the measured `411.43x891.43 dp` geometry.

## Fix round 1

### Controller rulings applied

1. The viewport cross-check now compares the UIAutomator-visible board with the same visible part of
   the raw logged layout. The inset source is `dumpsys window displays`: `cur height - app height`.
   At native geometry, `cur` equals `init`. Raw values remain recorded without alteration.
2. `1440x3120 @560` is documented as `411.43x891.43 dp` (`560/160 = 3.5 px/dp`). The brief's
   `514x1114 dp` parenthetical is identified as wrong in `docs/board-viewport-measured.md`. W1-02,
   W1-04, and W3-08 must consume the measured geometry.
3. The document now says the T1/T2 tutorial metrics stay dormant until W1-02 exports those boards
   through `buildTutorialLevel`.

### Fresh per-geometry evidence

All rows are EXECUTED on the same `fleet_floor_api31` emulator, Android API 31. `Raw UI` is the
`perf-board` bounds divided by `density/160`. `Adjusted log` subtracts the measured vertical inset
from the raw logged height, so it covers the same visible area as UIAutomator.

| Geometry | API | Raw logged `w x h` (dp) | Raw UI bounds (px) | Raw UI `w x h` (dp) | Inset from `cur-app` | Adjusted comparison (dp) | Result |
|---|---:|---:|---:|---:|---:|---:|---|
| Native `1440x3120 @560` (`411.43x891.43 dp`) | 31 | `411.428558x804.285706` | `[0,305][1440,2952]` | `411.428571x756.285714` | `3120-2952=168 px = 48 dp` | log `411.428558x756.285706` vs UI `411.428571x756.285714`; max delta `0.000013` | **PASS** |
| `1080x2340 @480` (`360x780 dp`) | 31 | `360x689` | `[0,273][1080,2172]` | `360x633` | `2340-2172=168 px = 56 dp` | log `360x633` vs UI `360x633`; delta `0` | **PASS** |
| `1080x1920 @480` (`360x640 dp`) | 31 | `360x549` | `[0,273][1080,1752]` | `360x493` | `1920-1752=168 px = 56 dp` | log `360x493` vs UI `360x493`; delta `0` | **PASS** |

- EXECUTED: `artifacts/W1-01/fix1/native-window-displays.txt`,
  `360x780-window-displays.txt`, and `360x640-window-displays.txt` contain the raw `init`, `cur`,
  `app`, and density read-backs used above.
- EXECUTED: matching `*-logcat.txt`, `*-ui.xml`, `*.png`, `*-session.txt`, and `*-summary.txt` files
  under `artifacts/W1-01/fix1/` contain the raw logs, hierarchies, screenshots, API read-backs, and
  concise excerpts.
- EXECUTED: the three screenshots were inspected; each visibly contains the three-button navigation
  bar in the device-reported 168 px strip.

### Commands

The scoped adb binary was `/Users/gentlegen/Library/Android/sdk/platform-tools/adb`; every device
command included `-s emulator-5556`. The capture sequence was:

```text
ADB_BIN=/Users/gentlegen/Library/Android/sdk/platform-tools/adb
df -h /
$ADB_BIN -s emulator-5556 get-state
$ADB_BIN -s emulator-5556 install -r artifacts/W1-01/apk/board-viewport-diagnostic-perf-release.apk

# Native, then the two overrides:
$ADB_BIN -s emulator-5556 shell wm size reset
$ADB_BIN -s emulator-5556 shell wm density reset
$ADB_BIN -s emulator-5556 shell wm size 1080x2340
$ADB_BIN -s emulator-5556 shell wm density 480
$ADB_BIN -s emulator-5556 shell wm size 1080x1920
$ADB_BIN -s emulator-5556 shell wm density 480

# At each geometry, after setting and reading back all three animation scales as 0:
$ADB_BIN -s emulator-5556 shell settings put global window_animation_scale 0
$ADB_BIN -s emulator-5556 shell settings put global transition_animation_scale 0
$ADB_BIN -s emulator-5556 shell settings put global animator_duration_scale 0
$ADB_BIN -s emulator-5556 shell settings get global window_animation_scale
$ADB_BIN -s emulator-5556 shell settings get global transition_animation_scale
$ADB_BIN -s emulator-5556 shell settings get global animator_duration_scale
$ADB_BIN -s emulator-5556 shell am force-stop com.danteb.arrows
$ADB_BIN -s emulator-5556 logcat -c
$ADB_BIN -s emulator-5556 shell am start -W -n com.danteb.arrows/.MainActivity
$ADB_BIN -s emulator-5556 shell wm size
$ADB_BIN -s emulator-5556 shell wm density
$ADB_BIN -s emulator-5556 shell getprop ro.build.version.sdk
$ADB_BIN -s emulator-5556 shell dumpsys window displays
$ADB_BIN -s emulator-5556 logcat -d -s ReactNativeJS:V
$ADB_BIN -s emulator-5556 shell uiautomator dump /sdcard/w1-01-fix1.xml
$ADB_BIN -s emulator-5556 pull /sdcard/w1-01-fix1.xml <geometry>-ui.xml
$ADB_BIN -s emulator-5556 exec-out screencap -p

$ADB_BIN -s emulator-5556 shell wm size reset
$ADB_BIN -s emulator-5556 shell wm density reset
```

EXECUTED command/output transcripts are `artifacts/W1-01/fix1/preflight.txt`,
`device-capture-run.txt`, each geometry's `*-session.txt`, and `geometry-reset.txt`.

### Brief verification rerun

- EXECUTED: `df -h /` showed 10 GiB available. No new build was necessary: the committed diagnostic
  APK was reused and its SHA-256 remained
  `156cc0ad1b3c582a5855e4f7bcce1dbc8e694f4a26f1e6b9661309f02f402b19`.
- EXECUTED: `npx tsx scripts/analysis/ftue-board-metrics.ts --level 0` printed
  `rows 20 cols 20 arrows 60 clearable 19 (31.7%)`; the independent W3 probe again printed the same
  baseline (`artifacts/W1-01/fix1/instrument-calibration.txt`).
- EXECUTED: `npx tsx scripts/analysis/ftue-board-metrics.ts --tutorial T1` exited 1 with the expected
  `unavailable until W1-02 exports buildTutorialLevel` error
  (`artifacts/W1-01/fix1/tutorial-dormant.txt`).
- EXECUTED: after installing the existing log-disabled release APK, the exact filtered log check
  exited 1 with no `[board-viewport]` output (`artifacts/W1-01/fix1/disabled-logcat.txt` and
  `disabled-check-and-restore.txt`).
- EXECUTED: `npx jest` passed 33/33 suites and 584/584 tests across both configured projects
  (`artifacts/W1-01/fix1/jest.txt`).
- EXECUTED: `npx tsc --noEmit` exited 0 (`artifacts/W1-01/fix1/tsc.txt`).
- The conditional `npx jest --selectProjects ui` gate was not separately required because fix round
  1 did not touch `GameScreen.tsx` or `ads.tsx`; the full Jest gate nevertheless ran both UI suites.
- EXECUTED: the device was returned to native `1440x3120 @560`, `policy_control=null`, scales
  `0/0/0`, and `device` state. The original installed APK was restored byte-for-byte at SHA-256
  `6bfde1ddbbcbd734edeb1650568518f78932133ca66bf66f91a72f0e60a2e7e9`; the emulator remains running
  (`artifacts/W1-01/fix1/installed-apk-before.txt`, `disabled-check-and-restore.txt`, and
  `installed-apk-after.txt`).

### Fix-round decisions and remaining scope

1. Used `cur-app`, not physical `init-app`, for the overridden geometries because `cur` is the active
   `wm` size. At native geometry `cur=init`, so the same method covers all rows.
2. Preserved raw log and UI values and showed the arithmetic explicitly; only the comparison area is
   adjusted.
3. Reused the already-built release APKs because the controller asked for a verification rerun, not
   a second native build, and the APK hashes prove which binaries ran.
4. Physical Android/iOS insets, cutouts, notches, and gesture navigation remain UNVERIFIED-DEVICE as
   required by the original brief; this does not block the three emulator acceptance rows.
