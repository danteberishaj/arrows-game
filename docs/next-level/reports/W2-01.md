# W2-01 report: explicit reduced-motion policies and native exit review

- **Status:** DONE_WITH_CONCERNS. The implementation, policy TDD, Android census, motion-on comparisons, level-0/harness exit recordings, TypeScript, full Jest, and F33 UI Jest are complete. The web visual row is UNVERIFIED because Chrome aborts in this sandbox. Task closure still requires the owner's yes/no on the reduced-motion native exit.
- **Task-start HEAD:** `80134ab91585` on `next-level`.
- **Pre-fix reference:** `da93dcd`, as required by the brief.
- **Commits:** none. GLOBAL.md assigns commits to Claude.
- **Evidence:** `artifacts/W2-01/`; concise index in `artifacts/W2-01/SUMMARY.md`.

## What changed

`App.tsx`, `src/ui/SplashScreen.tsx`, `src/ui/HomeScreen.tsx`, and `src/ui/GameScreen.tsx`

- Decorative splash, screen FadeIn, Play-pill breathing, earned-star entrance/stagger, and heart-pip spring now state `ReduceMotion.System` explicitly.
- Durations, curves, callbacks, sounds, and component structure are unchanged.

`src/ui/BoardView.tsx`

- Hint recenter `withTiming` and pan `withDecay` now state `ReduceMotion.System` explicitly because they are vestibular motion.
- Web `ExitTrail` now states `ReduceMotion.Never`. Its existing `reducedMotion` branch holds translation at zero and computes `opacity = 1-k`, so only the opacity clock is forced to run. This is the sole intended runtime change.
- The W0-04 drivers in `BlockerArrow`, `ShakingArrow`, and `HintArrow` are untouched.

`src/ui/__tests__/reduceMotionPolicy.test.ts`

- Parses `src/**/*.ts(x)` except `__tests__`, plus `App.tsx`, with the TypeScript compiler API.
- Requires config-object policies for `withTiming`/`withSpring`/`withDecay`, trailing policies for `withDelay`/`withRepeat`, and `.reduceMotion(...)` for JSX entering/exiting builders.
- Names exactly the six W0-04 exemptions by file and component: `BlockerArrow`, `ShakingArrow`, and `HintArrow` in `BoardView.tsx` and `StaticBoardSurface.native.tsx`.
- Allows `ReduceMotion.Never` only at `BoardView.tsx#ExitTrail`.

`src/ui/contrastAudit.ts`

- Updated only its checked `file:line` inventory after the explicit policy blocks shifted existing UI sites. No contrast role or value changed.

No native renderer, native exit serializer, or `StaticBoardSurface.native.tsx` code changed.

## Policy census

| brief row | policy | reduced-motion Android evidence |
|---|---|---|
| Splash `withDelay` + spring/timing and skip | `System` | Menu reached: `android/W2-01-fix-boot-menu-game-scale0/frames/00024.png` |
| Hint recenter timings | `System` | Static hint remains visible after recenter: `android/W2-01-fix-pan-hint-scale0/frames/00054.png` |
| Pan momentum `withDecay` | `System` | Pan settles with the zoomed board still covering the viewport: same frame and recording |
| Web `ExitTrail` timing | `Never`; component supplies stationary fade | **UNVERIFIED-WEB**, see below |
| Heart-loss spring | `System` | Spent outline remains visible: `android/W2-01-fix-blocked-scale0/frames/00006.png` |
| Star delay + spring | `System` | All three stars reach full opacity: `android/W2-01-level0-stars-scale0/frames/00038.png` |
| Play-pill repeat + timing | `System` | Resting full-width pill, then successful tap: boot frames `00024.png` → `00027.png` |
| App screen FadeIn | `System` | Game is fully visible after Play: boot frame `00027.png` |

The pan/hint recording uses a capture-only hint injection because release rewarded inventory is unavailable. The injection was bundled into `fix-perf-hint-capture.apk`, then reverted before final gates; it is absent from the working source. The recording manifests show app-reported reduced motion `false`/`true` at scale 1/0.

## TDD and source-policy proof

- **EXECUTED RED on `da93dcd`:** `artifacts/W2-01/prefix/da93dcd-policy-red.txt`. The scan reports 21 missing-policy violations, covering every brief row, while the exact six W0-04 exemptions pass.
- **EXECUTED RED on task-start source:** `artifacts/W2-01/tdd/red-current.txt`, also 21 violations.
- **EXECUTED GREEN:** `artifacts/W2-01/tdd/green.txt`, 3/3 policy tests.
- **EXECUTED allow-list mutation:** `artifacts/W2-01/tdd/never-mutation-red.txt`. Temporarily changing `GameScreen#HeartPip` from `System` to `Never` fails with that extra occurrence; the edit was reverted and 3/3 passed again.

## Android motion-on comparison

P-02 protocol was used for every motion state: set all three scales, force-stop, relaunch, read back, then compare the app's `useReducedMotion()` report where available. Recordings are 720×1560 variable-rate `screenrecord`; frame counts mean emitted change frames, not nominal 60 Hz frames.

| effect/capture | base emitted frames | fixed emitted frames | delta | result |
|---|---:|---:|---:|---|
| splash visible | 107 | 108 | +1 | within ±2 |
| Play pulse, start to return | 109 | 109 | 0 | within ±2 |
| App game FadeIn, first board to settled | 3 | 2 | -1 | within ±2 |
| blocked/heart sequence | 172 | 173 | +1 | within ±2 |
| stars/clear sequence | 222 | 223 | +1 | within ±2 |
| zoomed pan + decay | 115 | 114 | -1 | within ±2 |
| harness native exit | 13 | 13 | 0 | unchanged |

The boot analysis is in each `*-boot-menu-game-scale1-cycle/boot-flow-analysis.json`. Other values are in the paired manifests. The base APKs cannot report the P-02 diagnostic label; per controller amendment F01, their scale read-back plus force-stop/relaunch is accepted and is labelled unavailable in the manifests. Fixed scale-1 manifests report `appReducedMotion: false`.

**INFERRED limit:** the motion-on hint was not independently injected into a `da93dcd` APK. Its `System` policy is a restatement, and the W0-04 hint driver is untouched; fixed scale-1 and scale-0 hint recordings exist. I do not promote that inference to an executed `da93dcd` duration comparison.

## Android reduced-motion result

Fixed scale-0 manifests read back `0/0/0`, record `relaunchedAfterScaleChange: true`, and report `appReducedMotion: true` from the running process. I inspected the cited frames:

- The splash hands over to the menu; Play is fully visible (`W2-01-fix-boot-menu-game-scale0/frames/00024.png`).
- The Play tap succeeds and the game is fully visible (`.../frames/00027.png`); neither screen FadeIn is stuck transparent.
- The spent heart outline remains visible (`W2-01-fix-blocked-scale0/frames/00006.png`).
- All earned stars reach full opacity (`W2-01-level0-stars-scale0/frames/00038.png`).
- The pan finishes with the board clamped inside its viewport, and the W0-04 static hint is accent-coloured (`W2-01-fix-pan-hint-scale0/frames/00054.png`). The matching scale-1 pan continues through ordinary decay; the no-hint base/fix pair differs by one emitted frame.

## Web verification

**UNVERIFIED-WEB.** The allowed verification fallback applies.

- EXECUTED pre-fix setup from a `git archive da93dcd` scratch tree: `CI=1 npx expo start --web --port 8097`; the preview returned Arrows HTML with HTTP 200.
- EXECUTED Chrome 153 with `--force-prefers-reduced-motion`; it exited 134/SIGABRT before a CDP target appeared. `--no-sandbox --disable-gpu` behaved the same.
- Therefore there is no trustworthy in-page `matchMedia(...).matches === true` check and no pre-fix/fixed web trail frame count. `artifacts/W2-01/web/chrome-sandbox-failure.txt` preserves the commands and outcome. Metro was stopped.
- **INFERRED from code, not counted as visual proof:** `ExitTrail` now keeps its clock running with `Never`, while its existing reduced-motion branch fixes travelled distance at zero and linearly lowers opacity.

## Native exit protocol and owner recording

This path is read-only in W2-01:

- `BoardView` passes `useReducedMotion()` into the native payload; `nativeExitAnimation.ts:24-39` serialises it as header field four.
- Android parses it at `ArrowsBoardView.kt:166-177`; reduced motion sets travelled distance to zero and opacity to `1-progress` at `:276-296`, so the dash and head remain stationary and fade linearly.
- The Swift mirror holds dash phase and translation stationary at `ArrowsBoardView.swift:166-188` and uses a linear `[1,0]` fade at `:190-199`. iOS was read, not run.

Four owner recordings use the same tap within each scale pair:

| board | scale 1 | scale 0 | tap |
|---|---|---|---|
| level index 0, 20×20 | `android/W2-01-level0-exit-scale1/W2-01-level0-exit-scale1.mp4` | `android/W2-01-level0-exit-scale0/W2-01-level0-exit-scale0.mp4` | cell `[16,11]`, `(822,2140)`; 15 frames each |
| PERF level 3827, 250 arrows | `android/W2-01-harness-exit-scale1/W2-01-harness-exit-scale1.mp4` | `android/W2-01-harness-exit-scale0/W2-01-harness-exit-scale0.mp4` | cell `[3,16]`, `(616,1157)`; 13 frames each |

Inspected stills: level-0 scale 1 frame `00003.png` shows the ordinary moving trail; level-0 scale 0 frame `00006.png` shows a partially faded stationary dash/head. Harness scale 0 frame `00006.png` shows the same stationary treatment on the dense board.

### Owner acceptance required

**Does the owner accept the reduced-motion native exit as a stationary dash and arrowhead that fade linearly in place, with no translation, as shown in the two scale-0 recordings above? yes/no.**

W2-02 records the decision in DESIGN.md. If the answer is no, create a follow-up task; W2-01 intentionally makes no native renderer change.

## Gates

- EXECUTED targeted policy + contrast suites after line-reference maintenance: 2 suites, 145 tests passed.
- EXECUTED `npx jest --watchman=false`: 39 suites, 644 tests passed.
- EXECUTED F33 `npx jest --selectProjects ui --watchman=false`: 2 suites, 2 tests passed.
- EXECUTED `npx tsc --noEmit`: exit 0.
- EXECUTED `git diff --check`: exit 0.

The first full Jest run exposed only stale line-number references in `contrastAudit.ts`, caused by the added policy blocks. Those references were updated without changing contrast data; the targeted and full reruns are green.

## Evidence labels, limitations, and concerns

1. **UNVERIFIED-WEB:** no Chrome/CDP session, in-page media-query read, or web trail frames. This is the main acceptance gap.
2. **INFERRED:** motion-on hint parity with `da93dcd`, for the reason stated above. Every other recorded motion-on comparison is within ±1 emitted frame.
3. **UNVERIFIED-DEVICE:** the physical Android Accessibility “Remove animations” toggle; emulator settings are assumed to drive the same global.
4. **UNVERIFIED iOS / BLOCKED-ON-DISK:** Swift was read only; no iOS build or recording.
5. Android APKs are JS-only repacks into the existing native hosts. Source changes are JS/TS only, and no native renderer file appears in the diff.
6. Captures required uninstalling only `com.danteb.arrows` when signing keys differed, deleting that emulator app's prior data. No emulator was booted or killed.
7. ffmpeg emitted known non-monotonic-DTS warnings while splitting variable-rate recordings. Frame-file counts match manifest counts for the owner exit pairs.

## Emulator state left behind

- `emulator-5556` remains running.
- The final fixed PERF APK is installed and force-stopped.
- `window_animation_scale`, `transition_animation_scale`, and `animator_duration_scale` each read back `1`.
