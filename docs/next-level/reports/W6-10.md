# W6-10 — Spike: can `.tsx` components be tested on Expo 57 / RN 0.86.3 / React 19.2.3?

**Status: DONE.**
**Verdict: RESOLVES** — full detail, evidence and caveats live in the durable spike doc:
`docs/tsx-test-spike-2026-09-17.md` (committed on `next-level` alongside this report, per the
brief's Files list). This report is the SDD process record; the spike doc is the technical
evidence artifact the brief asked for.

## What I did

1. Confirmed base state on `next-level`: `git rev-parse HEAD` =
   `487362e2f8ed0e84e456b6f2f59430b6ee02aeaa`, branch `next-level`, clean tree — EXECUTED.
2. **Pre-fix reproduction** (brief's Verification §1), on the base commit, before any change:
   `npx jest --listTests | grep -c "\.tsx"` → `0` — EXECUTED. Confirms the premise: no `.tsx`
   file is exercised by the current single-project `ts-jest` config.
3. Recorded the base test baseline at this task's own base commit (Ruling F22 — not the
   brief's stale "11 suites / 61 tests"): `npx jest` → `Test Suites: 25 passed, 25 total`,
   `Tests: 543 passed, 543 total` — EXECUTED.
4. Read the Expo SDK unit-testing guide (`https://docs.expo.dev/develop/unit-testing/`; the
   `/versions/v57.0.0/` reference index has no dedicated testing page — the guide is
   unversioned across SDKs). Recorded preset, renderer (`@testing-library/react-native`,
   replacing `react-test-renderer` for React 19+) and install commands.
5. Created scratch branch `spike/w6-10-tsx-test` and, on it: installed `jest-expo`,
   `@testing-library/react-native`, and (an install the guide doesn't name)
   `@react-native/jest-preset`; split `jest.config.js` into a `projects: [core, ui]` config;
   wrote one trivial test rendering and pressing `HeaderButton`.
6. Hit and resolved two real snags along the way (both written up in the spike doc, §3):
   - The guide's own `npm i -D @testing-library/react-native` command fails with `ERESOLVE` on
     this machine's Node `20.19.4`, because the package's peer-dependency-free `latest`
     (`14.0.1`) requires Node `^22.13.0`, and npm falls back to `13.3.3`, which still peers on
     `react-test-renderer` and pulls a version needing `react@^19.3.0` (ahead of our pinned
     `19.2.3`). Resolved with `--legacy-peer-deps`, which found a real, compatible
     `react-test-renderer@19.2.3` — no pinned package changed version.
   - `jest-expo`'s preset requires `@react-native/jest-preset`, not present by default;
     `npx expo install @react-native/jest-preset --dev` resolved it cleanly.
7. Verified the trivial test passes, the existing 25 suites/543 tests are byte-identical when
   run in isolation (`--selectProjects core`), and `npm ls react react-native
   react-native-reanimated` shows no version changes (only additive new packages).
8. Ran `npx tsc --noEmit` — clean, no output.
9. Committed the scratch-branch experiment (`jest.config.js`, `package.json`,
   `package-lock.json`, the trivial test) on `spike/w6-10-tsx-test` (commit `dd2a00a`, local
   only — not pushed, per global constraints). Switched back to `next-level`, confirmed the
   tree returned exactly to the base commit's state (`git diff --stat HEAD -- package.json
   package-lock.json jest.config.js` → empty), and confirmed `npx jest` on `next-level`
   reproduces the base 25/543 baseline unchanged.
10. Wrote `docs/tsx-test-spike-2026-09-17.md` and this report, both committed on `next-level`
    only. Per the brief's Files list and Risk/rollback note, nothing else merges.

## Test results

- Pre-fix repro (base commit): `npx jest --listTests | grep -c "\.tsx"` → `0` — EXECUTED.
- Base baseline (base commit): `Test Suites: 25 passed, 25 total`, `Tests: 543 passed, 543
  total` — EXECUTED.
- Scratch branch, `ui` project only: `Test Suites: 1 passed, 1 total`, `Tests: 1 passed, 1
  total` — EXECUTED.
- Scratch branch, `core` project only (isolation check): `Test Suites: 25 passed, 25 total`,
  `Tests: 543 passed, 543 total` — identical to base — EXECUTED.
- Scratch branch, both projects: `Test Suites: 26 passed, 26 total`, `Tests: 544 passed, 544
  total` — EXECUTED.
- `npx tsc --noEmit` (scratch branch): clean, exit 0 — EXECUTED.
- `next-level`, after switching back: `git status --porcelain` clean apart from the two new
  doc files; `npx jest` reproduces `25 passed / 543 passed` — EXECUTED.

TDD: not applicable — this is a spike per the brief's Verification §3.
UNVERIFIED-DEVICE: not applicable — this is JS/Node tooling, no device involved (brief's
Verification §4).

## Files changed (this branch, `next-level`)

- `docs/tsx-test-spike-2026-09-17.md` (new) — the spike's technical record: guide findings,
  install order, the two caveats (Node-engine-driven peer conflict; missing
  `@react-native/jest-preset`), the `npm ls` diff, test results, and native-module-mock notes
  for W6-11.
- `docs/next-level/reports/W6-10.md` (new, this file).

Scratch-only (not part of this commit, per brief): `jest.config.js`, `package.json`,
`package-lock.json`, `src/ui/__tests__/headerButtonSpike.test.tsx`, committed as `dd2a00a` on
local branch `spike/w6-10-tsx-test` (not pushed).

## Self-review findings

- Considered stopping the spike at the first `ERESOLVE` and reporting DOES NOT RESOLVE. Chose
  instead to try the documented `--legacy-peer-deps` workaround before concluding, since it
  changes no pinned dependency version (verified via the `npm ls` diff) and gives the
  controller a real choice for W6-11 rather than a dead end from the very first command.
  Recorded as Decision 1 in the spike doc so this judgment call is visible and reversible.
- Verified the "existing suites unchanged" claim by running `--selectProjects core` in
  isolation, not just reading the combined summary line — the combined run's suite count
  alone (26) doesn't prove the pre-existing 25 didn't change internally (e.g. a snapshot
  regenerated, a count that happens to still add up). Isolation run removes that doubt.
- Double-checked that no scratch-branch change leaked back into `next-level` after switching
  branches (`git diff --stat HEAD -- package.json package-lock.json jest.config.js` empty).

## Concerns

- None blocking. One forward-looking note for W6-11 (in the spike doc, §3): pin
  `@testing-library/react-native` to `^13.3.3` explicitly (rather than letting `npm i`
  resolve unpinned) and document `--legacy-peer-deps`, or upgrade Node to `>=22.13.0` first
  and use `^14` instead, dropping the `react-test-renderer` peer entirely. Neither choice was
  made here — it's a W6-11-scope decision, out of this spike's non-goals.

## Owner acceptance needed

None to FINISH this task — the brief's acceptance criteria (trivial test passes, counts
unchanged, `npm ls` diff pasted, verdict given) are all met and verified above; this task
closes as DONE without owner sign-off. Flagging for awareness, not blocking: the owner (or
whoever picks up W6-11) should read the `--legacy-peer-deps` caveat in
`docs/tsx-test-spike-2026-09-17.md` §3 before that task's install step, since it affects how
W6-11's `package.json` pins `@testing-library/react-native`.
