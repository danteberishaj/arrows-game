# P-04c report: set `ios.supportsTablet` to false

- **Status:** DONE (one owner check is still open, listed below. It does not block this change.)
- **Base:** `2487fa7b458670add55c3e27df4ce2d788f48158` (`next-level`, clean tree). `app.json` is the same here as at `da93dcd`/`a1e2b43`: `git diff --stat da93dcd HEAD -- app.json` printed nothing.
- **Commits:** `bfa67eb` ios: declare iPhone-only (supportsTablet false), plus the commit that adds this report.
- **Result:** Expo's device-family mod now gives `"1"`. Before the change it gave `"1,2"`.

## What changed

- `app.json`: `ios.supportsTablet` changed from `true` to `false`. The diff is this one line.
- No iOS build and no `expo prebuild` ran. `ios/` (generated, gitignored) was not written: `shasum ios/Arrows.xcodeproj/project.pbxproj` gave `864ee93d…` before and after.
- Outside git (ignored): `artifacts/P-04c/pre-fix.txt` and `artifacts/P-04c/post-fix.txt`.

## Evidence

- EXECUTED: pre-fix, with `app.json:10` set to `"supportsTablet": true`. The acceptance command from the brief printed `"1,2"`. `grep -n TARGETED_DEVICE_FAMILY ios/Arrows.xcodeproj/project.pbxproj` printed lines 388 and 419 with `"1,2"`, which matches the brief (`artifacts/P-04c/pre-fix.txt`).
- EXECUTED: pre-fix, write path. A scratch script copied the existing pbxproj into the session scratchpad, parsed it with the `xcode` package, ran `IOSConfig.DeviceFamily.setDeviceFamily(getConfig('.').exp, {project})` and wrote the copy back out. Lines 388 and 419 still read `"1,2"`. This checks that the script reproduces what prebuild wrote last time before it is trusted for the post-fix result.
- EXECUTED: post-fix. The same acceptance command printed `"1"`. **This meets the acceptance criterion** (`artifacts/P-04c/post-fix.txt`).
- EXECUTED: post-fix, write path. The same scratch mod run on a copy of the existing pbxproj changed exactly two lines, 388 and 419, from `TARGETED_DEVICE_FAMILY = "1,2";` to `TARGETED_DEVICE_FAMILY = "1";`. `diff` showed no other change.
- EXECUTED: `npx expo config --type public --json` resolves `ios` to `{"supportsTablet":false,"bundleIdentifier":"com.danteb.arrows"}`. There is no `app.config.*` that could override it (the glob matched nothing).
- EXECUTED: why this runs the same code prebuild runs. `node_modules/@expo/prebuild-config/build/plugins/withDefaultPlugins.js:132` includes `IOSConfig.DeviceFamily.withDeviceFamily` in the default iOS plugin chain, and that plugin calls `setDeviceFamily`, which is the function the scratch script called. I also looked for other writers: `grep -rniE "DEVICE_FAMILY|UIDeviceFamily|supportsTablet|isTabletOnly"` over `plugins/`, `src/`, `app.json` and `package.json` matched only `app.json:10`. No local config plugin overrides the device family.
- INFERRED: `getDeviceFamilies` (`DeviceFamily.js` in `@expo/config-plugins`) returns `[1]` when `supportsTablet` is falsy and `isTabletOnly` is unset, which matches the output above.
- UNVERIFIED-DEVICE (BLOCKED-ON-DISK, a non-goal): a `project.pbxproj` regenerated by a real `expo prebuild --platform ios`, and the `UIDeviceFamily` of a built `.app`. `df -h /` showed 5.5Gi free. No iOS build was attempted, as the brief says.
- EXECUTED: gates. `npx jest` passed 11 of 11 suites and 61 of 61 tests. `npx tsc --noEmit` exited 0. `git status --porcelain` was clean after the commits (see the final check below).

## Concerns

- The brief cites `DeviceFamily.js:67-84` for the mod. In the installed file, `setDeviceFamily` runs from about line 68 to line 88. The code is the same; the line numbers are slightly off. This does not affect the result.
- The stale local `ios/` still says `"1,2"` and still has `UISupportedInterfaceOrientations~ipad` in `Info.plist` (lines 66-69). It is gitignored and only changes at the next prebuild. The `~ipad` key is a non-goal in the brief.

## Owner acceptance needed

- Carried over from W7-15: confirm that no iOS build was ever submitted to App Store Connect with iPad support. Apple does not let an app that shipped with iPad support remove it. If one was submitted, revert `bfa67eb` (rollback: set the value back to `true`).
